---
phase: 04-closed-loop-sumo-evaluation
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - scripts/run_closed_loop_sumo.py
  - scripts/run_closed_loop_suite.py
  - tests/test_closed_loop_sumo.py
  - experiments/dual_sensitivity/block4_closed_loop_suite.json
autonomous: true
requirements:
  - CLOP-01
  - CLOP-02
  - CLOP-03
  - CLOP-04
  - CLOP-05
must_haves:
  truths:
    - "Closed-loop experiments cover single-intersection sanity, arterial main case, grid scalability, robustness/demand-shift, and a bottleneck/failure-mode scenario."
    - "Outputs include the requested practical, pressure-style, symbolic, placebo, and full-dual-symbolic policy rows where feasible."
    - "Arterial main and grid scalability comparisons complete at least 5 seeds for every core baseline and expose confidence-interval-ready per-seed results."
    - "At least one bottleneck scenario applies a real SUMO/TraCI demand, speed, lane, or capacity-proxy manipulation and completes max_pressure plus capacity_aware_pressure rows."
  artifacts:
    - path: "scripts/run_closed_loop_suite.py"
      provides: "Block 4 suite orchestrator for multi-network/multi-seed SUMO runs"
      exports: ["main", "build_suite_spec", "aggregate_results"]
    - path: "scripts/run_closed_loop_sumo.py"
      provides: "Expanded controller registry and scenario support"
      exports: ["main", "run_experiment", "choose_controller_action"]
    - path: "experiments/dual_sensitivity/block4_closed_loop_suite.json"
      provides: "Raw per-seed and aggregated closed-loop suite results"
      contains: "aggregates"
  key_links:
    - from: "scripts/run_closed_loop_suite.py"
      to: "scripts/run_closed_loop_sumo.py"
      via: "imports run_experiment and controller registry"
      pattern: "from run_closed_loop_sumo import"
    - from: "scripts/run_closed_loop_suite.py"
      to: "networks/arterial/arterial.sumocfg"
      via: "arterial main-case suite spec"
      pattern: "arterial.sumocfg"
    - from: "scripts/run_closed_loop_suite.py"
      to: "networks/grid_4x4/grid_4x4.sumocfg"
      via: "grid scalability suite spec"
      pattern: "grid_4x4.sumocfg"
---

<objective>
Expand the smoke runner into a credible closed-loop SUMO experiment suite.

Purpose: CLOP-01 through CLOP-05 require multi-network, multi-seed, full-baseline evidence with confidence-interval-ready outputs and at least one ordinary-pressure failure-mode scenario.
Output: an expanded runner, a suite orchestrator, tests, and the Block 4 raw/aggregate suite artifact.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-closed-loop-sumo-evaluation/04-CONTEXT.md
@.planning/phases/04-closed-loop-sumo-evaluation/04-VALIDATION.md
@.planning/phases/04-closed-loop-sumo-evaluation/04-01-SUMMARY.md
@scripts/run_closed_loop_sumo.py
@scripts/sample_sumo_states.py
@scripts/run_sparse_recovery.py
@experiments/dual_sensitivity/block3_static_kill_gate.json

<interfaces>
Plan 04-01 must provide:
- `run_experiment(...)` or equivalent reusable function that accepts network/scenario/controller/seed/step/action settings and returns JSON-serializable per-seed rows.
- Controller registry containing at least `fixed_time`, `max_pressure`, and `capacity_aware_pressure`.
- Metric row fields required by CLOP-04.

Extend controller names exactly:
- `fixed_time`
- `actuated_local_pressure`
- `max_pressure`
- `capacity_aware_pressure`
- `local_pilight`
  - Must execute a PI-Light-style local DSL/rule baseline if an existing runnable policy can be adapted; otherwise the row must be reported as `not_feasible` with `unsupported_reason`, and a separate runnable `local_queue_symbolic` helper may be used internally but must not be mislabeled as PI-Light.
- `raw_neighbor_symbolic`
- `all_neighbor_symbolic`
- `random_permuted_dual`
- `full_dual_symbolic`

Network paths to support:
- `single`: `networks/single_intersection/single_intersection.sumocfg`
- `arterial`: `networks/arterial/arterial.sumocfg`
- `grid_4x4`: `networks/grid_4x4/grid_4x4.sumocfg`
- `chengdu`: `networks/chengdu/chengdu.sumocfg` may be included only if runtime remains CPU-feasible; not required for CLOP-01.

Suite output contract:
- top-level `scenario_results`: raw per-seed rows
- top-level `aggregates`: grouped by network/scenario/controller with `n_seeds`, `mean`, `std_error`, `ci95_low`, `ci95_high` for each numeric metric
- top-level `baseline_coverage`: requested controller names with `run`, `not_feasible`, or `unsupported_reason`
- top-level `claim_framing`: pressure-equivalent generalized-pressure symbolic recovery wording
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand controller registry and scenario support</name>
  <files>scripts/run_closed_loop_sumo.py, tests/test_closed_loop_sumo.py</files>
  <behavior>
    - Controller registry exposes all CLOP-02 controller names exactly.
    - `actuated_local_pressure` behaves as a local pressure trigger with fixed-time fallback under low queues.
    - `raw_neighbor_symbolic`, `all_neighbor_symbolic`, and `random_permuted_dual` are deterministic symbolic scoring policies using existing Phase 2 atom meanings; unsupported LP/dual details are recorded as feasibility metadata rather than silently omitted.
    - `local_pilight` is either a real adapted local PI-Light/DSL policy row or explicitly `not_feasible`; it must not be silently replaced by an upstream-queue heuristic under the PI-Light name.
    - Network resolver accepts `single`, `arterial`, and `grid_4x4`; scenario metadata accepts `main`, `demand_shift`, and `bottleneck_failure_mode`.
  </behavior>
  <action>Modify `scripts/run_closed_loop_sumo.py` to support arterial and grid_4x4 network configs plus all requested controller names. Implement symbolic policies using the atom definitions in `run_sparse_recovery.ATOM_REGISTRY`: `raw_neighbor_symbolic` uses upstream queue minus downstream queue; `all_neighbor_symbolic` adds downstream slack/fullness; `random_permuted_dual` uses a deterministic seed-based permutation of pressure-like movement values as placebo; `full_dual_symbolic` uses `dual_sensitivity`-style scores only when a per-TLS Scenario conversion is feasible, otherwise emits `feasibility_status=not_feasible` with `unsupported_reason` in the row and skips unsafe action replacement for that controller. For `local_pilight`, first look for an existing local PI-Light/DSL rule implementation that can be safely adapted to SUMO; if none exists, emit `feasibility_status=not_feasible` with `unsupported_reason` and do not label an upstream-queue heuristic as PI-Light. Keep pressure/capacity-aware policies first-class, not hidden behind symbolic aliases. Add tests for registry completeness, unsupported full-dual metadata, and network/scenario resolver behavior.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 -m py_compile scripts/run_closed_loop_sumo.py tests/test_closed_loop_sumo.py && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 tests/test_closed_loop_sumo.py && python3 -c 'import sys; sys.path.insert(0,"/home/samuel/projects/pi_light_OR/scripts"); import run_closed_loop_sumo as r; expected={"fixed_time","actuated_local_pressure","max_pressure","capacity_aware_pressure","local_pilight","raw_neighbor_symbolic","all_neighbor_symbolic","random_permuted_dual","full_dual_symbolic"}; assert expected <= set(r.CONTROLLER_REGISTRY); print("controller coverage ok")'</automated>
  </verify>
  <done>Runner supports all required controller names and network/scenario metadata without adding dependencies or overclaiming infeasible full-dual execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create multi-seed suite orchestrator and confidence intervals</name>
  <files>scripts/run_closed_loop_suite.py, tests/test_closed_loop_sumo.py</files>
  <behavior>
    - Suite spec includes single sanity, arterial main, grid scalability, demand-shift robustness, and bottleneck failure-mode scenarios.
    - Arterial and grid default seed lists each contain 5-10 seeds.
    - Aggregation computes mean, standard error, and 95% confidence interval for CLOP-04 numeric metrics.
    - Baseline coverage reports each CLOP-02 controller as `run`, `not_feasible`, or with an explicit unsupported reason.
  </behavior>
  <action>Create `scripts/run_closed_loop_suite.py` with `main() -> None`, `build_suite_spec()`, and `aggregate_results()`. The CLI must accept `--profile smoke|main`, `--controllers`, `--arterial-seeds`, `--grid-seeds`, `--steps`, `--warmup`, `--action-interval`, `--out`, and `--route-json`. `smoke` profile should use short steps and one or two seeds for quick validation; `main` profile should target 5 seeds by default for arterial and grid, with CLI allowing 10. Scenario definitions must include `single_sanity`, `arterial_main`, `grid_scalability`, `arterial_demand_shift`, and `arterial_bottleneck_failure_mode`. Implement demand shift through an actual changed-demand mechanism: generate a temporary scaled route file, use supported TraCI vehicle insertion, or select a concrete alternate route demand file; record `demand_shift_mechanism` and changed demand parameters. Seed-only metadata is not a valid demand-shift mechanism. Implement bottleneck/failure-mode through an actual SUMO/TraCI manipulation such as deterministic edge max-speed reduction, lane closure, or a capacity-proxy action that changes downstream discharge during the run; record `failure_mode_mechanism`, target edge/lane/TLS, start/end time, and parameter values. Phase 4 completion requires at least one completed `arterial_bottleneck_failure_mode` scenario with completed `max_pressure` and `capacity_aware_pressure` rows; if the first chosen manipulation is unsupported, choose another executable manipulation rather than satisfying CLOP-05 with `not_feasible` metadata. Add tests for CI math and suite spec contents.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 -m py_compile scripts/run_closed_loop_suite.py tests/test_closed_loop_sumo.py && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 tests/test_closed_loop_sumo.py && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 -c 'import run_closed_loop_suite as s; spec=s.build_suite_spec(profile="main", controllers=["fixed_time","max_pressure"], arterial_seeds=[1,2,3,4,5], grid_seeds=[6,7,8,9,10], steps=100, warmup=20, action_interval=10); names={x["scenario_tag"] for x in spec}; assert {"single_sanity","arterial_main","grid_scalability","arterial_demand_shift","arterial_bottleneck_failure_mode"} <= names; assert len([x for x in spec if x["network"]=="arterial" and x["scenario_tag"]=="arterial_main"]) >= 5; print("suite spec ok")'</automated>
  </verify>
  <done>`run_closed_loop_suite.py` can build a full Phase 4 suite spec and aggregate raw per-seed results into CI-ready metrics.</done>
</task>

<task type="auto">
  <name>Task 3: Generate the Block 4 closed-loop suite artifact</name>
  <files>experiments/dual_sensitivity/block4_closed_loop_suite.json</files>
  <action>Run the suite in `smoke` profile first to validate all scenarios and controller metadata quickly, then run a CPU-feasible `main` profile command that completes at least 5 seeds for arterial main and grid scalability. Use all requested controllers; only `full_dual_symbolic` and `local_pilight` may be marked infeasible, and `local_pilight` may be infeasible only when no real PI-Light/DSL baseline can be safely adapted. Write `experiments/dual_sensitivity/block4_closed_loop_suite.json`. Ensure raw rows plus aggregate CIs are present and that pressure/capacity-aware baselines remain first-class rows. If the planned 900-step horizon is too slow, reduce steps only enough to complete 5 seeds for arterial/grid core baselines and record the shorter horizon; do not mark Phase 4 complete with fewer than 5 completed arterial/grid seeds for core baselines, and do not fabricate metrics.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 scripts/run_closed_loop_suite.py --profile smoke --controllers fixed_time max_pressure capacity_aware_pressure local_pilight raw_neighbor_symbolic all_neighbor_symbolic random_permuted_dual full_dual_symbolic --steps 300 --warmup 60 --action-interval 10 --out /tmp/block4_closed_loop_suite_smoke.json && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 scripts/run_closed_loop_suite.py --profile main --controllers fixed_time actuated_local_pressure max_pressure capacity_aware_pressure local_pilight raw_neighbor_symbolic all_neighbor_symbolic random_permuted_dual full_dual_symbolic --arterial-seeds 20260523 20260524 20260525 20260526 20260527 --grid-seeds 20260601 20260602 20260603 20260604 20260605 --steps 900 --warmup 120 --action-interval 10 --out experiments/dual_sensitivity/block4_closed_loop_suite.json && python3 -c 'import json; p=json.load(open("experiments/dual_sensitivity/block4_closed_loop_suite.json")); assert p["route_decision"]=="pressure-equivalent"; assert p.get("scenario_results"); assert p.get("aggregates"); rows=p["scenario_results"]; scenarios={r["scenario_tag"] for r in rows}; assert {"single_sanity","arterial_main","grid_scalability","arterial_demand_shift","arterial_bottleneck_failure_mode"} <= scenarios; coverage=p.get("baseline_coverage",{}); expected={"fixed_time","actuated_local_pressure","max_pressure","capacity_aware_pressure","local_pilight","raw_neighbor_symbolic","all_neighbor_symbolic","random_permuted_dual","full_dual_symbolic"}; assert expected <= set(coverage); core={"fixed_time","max_pressure","capacity_aware_pressure","raw_neighbor_symbolic","all_neighbor_symbolic","random_permuted_dual"}; completed=[r for r in rows if r.get("scenario_status","completed")=="completed" and r.get("feasibility_status","run") in {"run","completed"}]; assert all(len({r["seed"] for r in completed if r["scenario_tag"]==scenario and r["controller"]==controller})>=5 for scenario in ["arterial_main","grid_scalability"] for controller in core); assert any(r["scenario_tag"]=="arterial_demand_shift" and r.get("demand_shift_mechanism") and r.get("demand_shift_mechanism")!="seed_only" for r in completed); assert any(r["scenario_tag"]=="arterial_bottleneck_failure_mode" and r.get("failure_mode_mechanism") and r["controller"]=="max_pressure" for r in completed); assert any(r["scenario_tag"]=="arterial_bottleneck_failure_mode" and r.get("failure_mode_mechanism") and r["controller"]=="capacity_aware_pressure" for r in completed); assert "dual universally beats pressure" not in json.dumps(p).lower(); print("closed-loop suite artifact ok")'</automated>
  </verify>
  <done>`block4_closed_loop_suite.json` contains raw and aggregate closed-loop results/metadata for required networks, scenarios, baselines, seeds, metrics, and Phase 3 route discipline.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 4 | Credible closed-loop evidence against strong baselines | 04-01,04-02,04-03 | COVERED | This plan generates the main raw/aggregate suite.
REQ | CLOP-01 | single, arterial, grid, robustness/demand-shift | 04-01,04-02 | COVERED | 04-02 adds arterial/grid/demand shift; 04-01 covers single smoke.
REQ | CLOP-02 | fixed, actuated/local pressure, max-pressure, capacity-aware, PI-Light, symbolic, placebo, full dual where feasible | 04-02 | COVERED | All names required in registry and coverage metadata.
REQ | CLOP-03 | 5-10 seeds for arterial/grid CIs | 04-02 | COVERED | Main profile defaults to 5 and accepts 10.
REQ | CLOP-04 | travel time, delay, throughput, queues, spillback/blocking, switching, runtime | 04-01,04-02 | COVERED | 04-02 preserves metric schema and aggregates CIs.
REQ | CLOP-05 | pressure failure-mode scenario | 04-02 | COVERED | `arterial_bottleneck_failure_mode` is required in suite spec and artifact metadata.
RESEARCH | Phase 3 route | pressure-equivalent route | 04-02,04-03 | COVERED | Artifacts carry route_decision and claim_framing.
CONTEXT | No new deps | CPU/SUMO/TraCI only | 04-02 | COVERED | Uses existing scripts and stdlib math/json/csv only.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Suite CLI -> runner | User-supplied controllers, seeds, steps, and output path drive many SUMO runs. |
| Scenario manipulation -> interpretation | Demand-shift/bottleneck manipulations can be overstated if metadata is missing. |
| Raw rows -> aggregate CIs | Incorrect aggregation can produce misleading confidence intervals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | controller registry | mitigate | Tests assert exact CLOP-02 controller names and baseline coverage metadata. |
| T-04-06 | Repudiation | failure-mode scenario | mitigate | Record scenario manipulation fields and unsupported/not-feasible reasons in raw rows and top-level metadata. |
| T-04-07 | Tampering | aggregate CI calculations | mitigate | Unit-test mean/std-error/CI calculations on known synthetic rows before running SUMO. |
| T-04-08 | Denial of Service | main suite runtime | mitigate | Provide smoke profile; main profile uses explicit seed/step counts and records not-completed runtime-budget status rather than fabricating results. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Plan 04-02 is complete when controller coverage tests pass, suite spec contains all required scenarios, and `block4_closed_loop_suite.json` exists with raw rows, aggregate CIs, baseline coverage metadata, and route_decision=`pressure-equivalent`.
</verification>

<success_criteria>
- The Phase 4 suite covers CLOP-01 and CLOP-05 with actual completed closed-loop scenarios, not metadata-only placeholders.
- All CLOP-02 baselines are represented; only `full_dual_symbolic` and a non-adaptable real `local_pilight` baseline may be explicitly infeasible, never silently omitted.
- Arterial and grid raw rows complete at least 5 seeds for core baselines and aggregate outputs are confidence-interval-ready.
</success_criteria>

<output>
Create `.planning/phases/04-closed-loop-sumo-evaluation/04-02-SUMMARY.md` when done.
</output>
