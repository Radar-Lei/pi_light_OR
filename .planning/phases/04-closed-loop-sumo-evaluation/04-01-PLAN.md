---
phase: 04-closed-loop-sumo-evaluation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run_closed_loop_sumo.py
  - tests/test_closed_loop_sumo.py
  - experiments/dual_sensitivity/block4_closed_loop_smoke.json
autonomous: true
requirements:
  - CLOP-01
  - CLOP-02
  - CLOP-04
must_haves:
  truths:
    - "A real SUMO/TraCI closed-loop runner can control at least the single-intersection network end-to-end."
    - "Smoke outputs compare fixed-time, max-pressure/backpressure, and capacity-aware pressure without treating pressure as a strawman."
    - "Per-seed results include travel time, delay, throughput/completed vehicles, mean/max queues, spillback/blocking counts, switching counts, and controller runtime."
    - "Every output records Phase 3 route_decision=pressure-equivalent and avoids dual-superiority interpretation."
  artifacts:
    - path: "scripts/run_closed_loop_sumo.py"
      provides: "Closed-loop SUMO/TraCI signal-control runner and controller registry"
      exports: ["main", "run_experiment", "choose_controller_action"]
    - path: "tests/test_closed_loop_sumo.py"
      provides: "Fast behavior tests for metric aggregation, controller selection, and claim metadata"
      exports: []
    - path: "experiments/dual_sensitivity/block4_closed_loop_smoke.json"
      provides: "Single-intersection closed-loop smoke artifact"
      contains: "scenario_results"
  key_links:
    - from: "scripts/run_closed_loop_sumo.py"
      to: "scripts/sample_sumo_states.py"
      via: "reuse build_network_metadata for TLS movements and capacities"
      pattern: "from sample_sumo_states import build_network_metadata"
    - from: "scripts/run_closed_loop_sumo.py"
      to: "networks/single_intersection/single_intersection.sumocfg"
      via: "single scenario configuration"
      pattern: "single_intersection.sumocfg"
    - from: "experiments/dual_sensitivity/block4_closed_loop_smoke.json"
      to: "experiments/dual_sensitivity/block3_static_kill_gate.json"
      via: "route_decision copied into output metadata"
      pattern: "pressure-equivalent"
---

<objective>
Create the minimal real closed-loop SUMO evaluation path for Phase 4.

Purpose: Phase 4 needs executable closed-loop signal control before any multi-seed comparison or report is credible.
Output: a reusable SUMO/TraCI runner, fast tests, and a short single-intersection smoke artifact.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-closed-loop-sumo-evaluation/04-CONTEXT.md
@.planning/phases/04-closed-loop-sumo-evaluation/04-VALIDATION.md
@.planning/phases/03-static-pressure-failure-kill-gate/03-03-SUMMARY.md
@scripts/sample_sumo_states.py
@scripts/run_dual_sanity.py
@scripts/run_sparse_recovery.py
@experiments/dual_sensitivity/block3_static_kill_gate.json

<interfaces>
Use these existing contracts directly.

From `scripts/sample_sumo_states.py`:
- `build_network_metadata(net_file: Path) -> dict[str, Any]` returns `edge_capacity` and `tls_movements`.
- SUMO startup pattern: `traci.start(["sumo", "-c", str(sumocfg), "--seed", str(seed), "--no-step-log", "true", "--duration-log.disable", "true"])` with `try/finally: traci.close(False)`.
- Per-step queues use `traci.edge.getLastStepHaltingNumber(edge_id)` and vehicle counts use `traci.edge.getLastStepVehicleNumber(edge_id)`.

From Phase 3 route source of truth:
- `experiments/dual_sensitivity/block3_static_kill_gate.json` has `route_decision: pressure-equivalent` and `scope: static_sample_relative_only_no_closed_loop_claims`.
- Phase 4 must carry this metadata forward and frame outputs as closed-loop evidence for generalized-pressure symbolic recovery, not dual superiority.

New runner contract to create:
- CLI flags: `--network`, `--controllers`, `--seeds`, `--steps`, `--warmup`, `--action-interval`, `--out`, `--route-json`, `--scenario-tag`.
- Supported `--network` in this plan: `single` using `networks/single_intersection/single_intersection.sumocfg`.
- Controllers in this plan: `fixed_time`, `max_pressure`, `capacity_aware_pressure`.
- Output top-level fields: `experiment`, `route_decision`, `claim_framing`, `networks`, `controllers`, `seeds`, `scenario_results`, `metric_schema`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define closed-loop runner contracts and fast behavior tests</name>
  <files>scripts/run_closed_loop_sumo.py, tests/test_closed_loop_sumo.py</files>
  <behavior>
    - Controller registry contains `fixed_time`, `max_pressure`, and `capacity_aware_pressure` with deterministic action selection from queue/capacity/TLS movement data.
    - Metric aggregation returns average travel time, total delay, completed vehicles, throughput, mean queue, max queue, spillback count, blocking count, switching count, and controller runtime seconds.
    - Route metadata loader returns `pressure-equivalent` from the Phase 3 JSON and rejects missing route fields with a clear error.
    - Output claim_framing contains generalized-pressure symbolic recovery language and contains no dual-superiority phrases.
  </behavior>
  <action>Create `scripts/run_closed_loop_sumo.py` with pure helper functions first, then `main() -> None`. Add `tests/test_closed_loop_sumo.py` as direct Python tests using only stdlib assertions; do not require pytest. Test helper behavior without launching SUMO: controller registry completeness for this plan, action selection over synthetic TLS movements, metric aggregation over synthetic per-step observations, and route metadata loading from a temporary JSON. Keep imports compatible with `PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts`. Do not add Python dependencies, GPU paths, vllm usage, or external services.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 -m py_compile scripts/run_closed_loop_sumo.py tests/test_closed_loop_sumo.py && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 tests/test_closed_loop_sumo.py</automated>
  </verify>
  <done>Fast tests pass and prove the runner has deterministic controller contracts, metric schema, and Phase 3 claim metadata before any SUMO launch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and run the single-intersection closed-loop smoke path</name>
  <files>scripts/run_closed_loop_sumo.py, experiments/dual_sensitivity/block4_closed_loop_smoke.json</files>
  <behavior>
    - Command-line execution runs SUMO through TraCI on the single-intersection `.sumocfg` with one seed and multiple controllers.
    - Each action interval reads live queues and vehicle counts, chooses a TLS phase, applies it through TraCI, and records switching counts.
    - The smoke JSON contains one result per requested controller/seed and every result has the CLOP-04 metrics.
    - The output keeps `max_pressure` and `capacity_aware_pressure` as first-class rows next to `fixed_time`.
  </behavior>
  <action>Implement the closed-loop TraCI loop in `scripts/run_closed_loop_sumo.py`. Resolve network paths for `single`; infer the `.net.xml` path from the `.sumocfg` sibling files; reuse `build_network_metadata()` for capacities and movements. At each action interval compute phase scores by summing movement scores for movements compatible with each current TLS controlled-link phase. For `fixed_time`, cycle phases deterministically. For `max_pressure`, score movement as upstream queue minus downstream queue. For `capacity_aware_pressure`, subtract downstream fullness or blocked-downstream penalties using capacity from metadata. Record per-step queues, max queue, spillback/blocking events when downstream occupancy is near capacity and upstream queue remains positive, switching counts, controller decision runtime, completed/arrived vehicles from TraCI, and total travel time/delay by tracking departed vehicle IDs, per-step active vehicles, `traci.simulation.getArrivedIDList()`, and each arrived vehicle's elapsed simulation time when available; if a vehicle-specific arrival value is unavailable, use accumulated active-vehicle time and completed count with an explicit `travel_time_source` field rather than an ambiguous metric. Write `experiments/dual_sensitivity/block4_closed_loop_smoke.json` by running `--network single --controllers fixed_time max_pressure capacity_aware_pressure --seeds 20260523 --steps 300 --warmup 60 --action-interval 10 --out experiments/dual_sensitivity/block4_closed_loop_smoke.json`. If a SUMO route has too few completed vehicles in the short smoke, still write the metrics with zero-safe denominators and a `smoke_notes` field explaining sample size; do not replace this with a report-only path.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 scripts/run_closed_loop_sumo.py --network single --controllers fixed_time max_pressure capacity_aware_pressure --seeds 20260523 --steps 300 --warmup 60 --action-interval 10 --out experiments/dual_sensitivity/block4_closed_loop_smoke.json && python3 -c 'import json; p=json.load(open("experiments/dual_sensitivity/block4_closed_loop_smoke.json")); assert p["experiment"]=="block4_closed_loop_sumo"; assert p["route_decision"]=="pressure-equivalent"; rows=p["scenario_results"]; assert rows and {r["controller"] for r in rows} >= {"fixed_time","max_pressure","capacity_aware_pressure"}; required={"avg_travel_time","total_delay","completed_vehicles","throughput","mean_queue","max_queue","spillback_count","blocking_count","switching_count","controller_runtime_sec"}; assert all(required <= set(r) for r in rows); assert "dual universally beats pressure" not in json.dumps(p).lower(); print("closed-loop smoke ok")'</automated>
  </verify>
  <done>`block4_closed_loop_smoke.json` is produced by a real TraCI closed-loop run and proves the runner can compare practical and pressure-style baselines with the required metric schema.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 4 | Closed-loop SUMO evidence for Phase 3 selected route | 04-01,04-02,04-03 | COVERED | This plan creates the executable closed-loop path.
REQ | CLOP-01 | Single, arterial, grid, robustness/demand-shift coverage | 04-01,04-02 | COVERED | 04-01 covers single smoke; 04-02 covers arterial/grid/robustness/failure scenarios.
REQ | CLOP-02 | Baseline suite | 04-01,04-02 | COVERED | This plan starts fixed/max/capacity-aware; 04-02 adds the full requested suite.
REQ | CLOP-03 | 5-10 seeds for arterial/grid CIs | 04-02 | COVERED | Multi-seed suite belongs after runner exists.
REQ | CLOP-04 | Required closed-loop metrics | 04-01,04-02 | COVERED | This plan creates the metric schema and smoke artifact.
REQ | CLOP-05 | Failure-mode scenario for ordinary pressure | 04-02 | COVERED | Bottleneck scenario is generated in the suite plan.
RESEARCH | Phase 3 route | pressure-equivalent route and no overclaiming | 04-01,04-03 | COVERED | Output metadata and report guards carry route discipline.
CONTEXT | CPU/SUMO/TraCI | No GPU, vllm, external services, or new dependencies | 04-01,04-02,04-03 | COVERED | All tasks use stdlib plus existing SUMO/TraCI/SciPy stack.
CONTEXT | First plan | Minimal but real closed-loop runner/smoke path | 04-01 | COVERED | Smoke command launches SUMO and applies TLS actions.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI user input -> SUMO runner | Untrusted flags select network, controllers, seeds, step counts, and output path. |
| TraCI runtime -> JSON artifacts | Live simulator values become paper-facing metrics and must be schema-checked. |
| Phase 3 JSON -> Phase 4 metadata | Static route metadata constrains interpretation of closed-loop outputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `scripts/run_closed_loop_sumo.py` output path | mitigate | Validate controller/network names, create requested parent directory only, never delete unrelated files. |
| T-04-02 | Repudiation | closed-loop metric rows | mitigate | Include controller, seed, network, scenario_tag, steps, warmup, action_interval, and route source path in each artifact. |
| T-04-03 | Information Disclosure | SUMO/TraCI logs | accept | Outputs contain local experiment metrics only and no secrets or external service data. |
| T-04-04 | Denial of Service | large step/seed counts | mitigate | Keep smoke verification short; CLI validates positive integers and lets long runs be explicit. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Plan 04-01 is complete when `scripts/run_closed_loop_sumo.py` and `tests/test_closed_loop_sumo.py` compile, tests pass without pytest, and the single-intersection smoke command writes `block4_closed_loop_smoke.json` with all required metrics and `route_decision=pressure-equivalent`.
</verification>

<success_criteria>
- A real closed-loop SUMO/TraCI runner exists under `scripts/`.
- Single-intersection smoke output compares fixed-time, max-pressure, and capacity-aware pressure.
- Metrics satisfy CLOP-04 and output language satisfies Phase 3 pressure-equivalent claim discipline.
</success_criteria>

<output>
Create `.planning/phases/04-closed-loop-sumo-evaluation/04-01-SUMMARY.md` when done.
</output>
