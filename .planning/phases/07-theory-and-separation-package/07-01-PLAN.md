---
phase: 07-theory-and-separation-package
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_theory_separation.py
  - scripts/check_theory_separation.py
  - experiments/dual_sensitivity/phase7_theory_separation.json
  - refine-logs/THEORY_AND_SEPARATION.md
autonomous: true
requirements:
  - THRY-01
  - THRY-02
  - THRY-03
  - THRY-04
must_haves:
  truths:
    - "研究者可以验证 THRY-01：在无限/非绑定存储、无切换损失、固定转向比例、操作约束 slack 时，有限存储 primal-dual 评分 recover/tie classical max-pressure/backpressure。"
    - "研究者可以审计 THRY-02：一个显式 finite_storage_state 的 binding two-phase example 让 pressure action 与 finite-storage primal-dual action 分离。"
    - "研究者可以审计 THRY-03：binding separation example 在预声明 one-step constrained objective 上严格改进，且 objective_components 来自 Phase 6 schema。"
    - "研究者可以定位 THRY-04：仅选择 constrained LP oracle regret 作为后续 guarantee candidate，并明确其 finite-sample/oracle-relative 边界。"
    - "所有 Phase 7 输出通过 claim audit，不把 v1.0 pressure-equivalent evidence 表述为 superiority evidence。"
  artifacts:
    - path: "tests/test_theory_separation.py"
      provides: "Phase 7 red/green-compatible tests for slack recovery, binding separation, strict objective improvement, guarantee candidate, and claim-safe surfaces"
      min_lines: 80
    - path: "scripts/check_theory_separation.py"
      provides: "Deterministic checker/generator for Phase 7 static theory separation JSON"
      exports: ["build_and_check_phase7_payload", "main"]
    - path: "experiments/dual_sensitivity/phase7_theory_separation.json"
      provides: "Machine-readable THRY-01~THRY-04 artifact with explicit finite_storage_state/objective_components and computed action/objective comparison"
      contains: "theory_static_one_step_only_no_closed_loop_claims"
    - path: "refine-logs/THEORY_AND_SEPARATION.md"
      provides: "Technical memo with THRY-01~THRY-04 statements, assumptions, proof sketches, and bounded claim guardrails"
      contains: "constrained LP oracle regret"
  key_links:
    - from: "scripts/check_theory_separation.py"
      to: "scripts/finite_storage_schema.py"
      via: "import Phase 6 schema constants/validators"
      pattern: "validate_state_objective_sample"
    - from: "tests/test_theory_separation.py"
      to: "scripts/check_theory_separation.py"
      via: "direct helper import plus subprocess CLI"
      pattern: "build_and_check_phase7_payload"
    - from: "refine-logs/THEORY_AND_SEPARATION.md"
      to: "experiments/dual_sensitivity/phase7_theory_separation.json"
      via: "memo references generated checker artifact and matches THRY markers/objective definition"
      pattern: "phase7_theory_separation.json"
    - from: "scripts/audit_claim_discipline.py"
      to: "refine-logs/THEORY_AND_SEPARATION.md, experiments/dual_sensitivity/phase7_theory_separation.json"
      via: "claim audit verification command"
      pattern: "--paths refine-logs/THEORY_AND_SEPARATION.md experiments/dual_sensitivity/phase7_theory_separation.json"
---

<objective>
完成 Phase 7 的 Theory and Separation Package，覆盖 THRY-01~THRY-04：把 Phase 6 的 bounded claim policy、finite_storage_state schema、objective_components schema 转化为可测试、可审计、claim-safe 的静态理论/分离包。

Purpose: 为 v1.1 提供一个清晰边界的有限存储 primal-dual separation package：slack 时 recover/tie pressure；binding 时只在显式 finite-storage/spillback/switching/service/incident 状态和预声明 one-step objective 下说明 structural separation 与 strict local improvement。
Output: `tests/test_theory_separation.py`、`scripts/check_theory_separation.py`、`experiments/dual_sensitivity/phase7_theory_separation.json`、`refine-logs/THEORY_AND_SEPARATION.md`。
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/samuel/projects/pi_light_OR/.planning/PROJECT.md
@/home/samuel/projects/pi_light_OR/.planning/ROADMAP.md
@/home/samuel/projects/pi_light_OR/.planning/REQUIREMENTS.md
@/home/samuel/projects/pi_light_OR/.planning/STATE.md
@/home/samuel/projects/pi_light_OR/.planning/phases/07-theory-and-separation-package/07-CONTEXT.md
@/home/samuel/projects/pi_light_OR/.planning/phases/07-theory-and-separation-package/07-RESEARCH.md
@/home/samuel/projects/pi_light_OR/.planning/phases/07-theory-and-separation-package/07-PATTERNS.md
@/home/samuel/projects/pi_light_OR/.planning/phases/07-theory-and-separation-package/07-VALIDATION.md
@/home/samuel/projects/pi_light_OR/.planning/phases/06-claim-discipline-and-explicit-state-foundation/06-01-SUMMARY.md
@/home/samuel/projects/pi_light_OR/.planning/phases/06-claim-discipline-and-explicit-state-foundation/06-02-SUMMARY.md
@/home/samuel/projects/pi_light_OR/.planning/phases/06-claim-discipline-and-explicit-state-foundation/06-03-SUMMARY.md
@/home/samuel/projects/pi_light_OR/refine-logs/THEORY_AND_CLAIMS.md
@/home/samuel/projects/pi_light_OR/refine-logs/THEORY_AND_ATOMS.md
@/home/samuel/projects/pi_light_OR/scripts/finite_storage_schema.py
@/home/samuel/projects/pi_light_OR/scripts/claim_policy.py
@/home/samuel/projects/pi_light_OR/scripts/audit_claim_discipline.py
@/home/samuel/projects/pi_light_OR/tests/test_finite_storage_schema.py
@/home/samuel/projects/pi_light_OR/tests/test_claim_discipline.py

<interfaces>
Key contracts already available from Phase 6 and earlier theory artifacts. Use these directly; do not explore or change unrelated controller/SUMO code.

From `scripts/finite_storage_schema.py`:
```python
FINITE_STORAGE_STATE_FIELDS = {
    "downstream_storage",
    "residual_receiving_capacity",
    "spillback_blocking",
    "switching_loss_state",
    "service_urgency",
    "incident_capacity_drop",
}
OBJECTIVE_COMPONENT_FIELDS = {
    "delay",
    "unfinished_vehicle_penalty",
    "spillback_blocking_time",
    "switching_lost_time",
}
SCHEMA_VERSION = "phase6_explicit_state_v1"

def validate_state_objective_sample(sample: dict[str, Any], *, path: Path | None = None, sample_idx: int | None = None) -> None: ...
def validate_finite_storage_state(state: dict[str, Any], *, path: Path | None = None, sample_idx: int | None = None) -> None: ...
def validate_objective_components(components: dict[str, Any], *, path: Path | None = None, sample_idx: int | None = None) -> None: ...
def build_objective_components_from_metrics(metrics: dict[str, Any], *, horizon: float = 1.0, switching_lost_time_per_switch: float = DEFAULT_SWITCHING_LOST_TIME_PER_SWITCH) -> dict[str, float]: ...
```

From `scripts/claim_policy.py`:
```python
PERMITTED_CLAIM = (
    "recover_or_tie_max_pressure_when_constraints_slack; "
    "improvement_claims_only_for_explicit_binding_finite_storage_spillback_switching_service_incident_regimes"
)
FORBIDDEN_CLAIM_PATTERNS = [
    "dual universally beats pressure",
    "proves superiority",
    "deployable superiority",
    "static evidence proves closed-loop",
    "universal dominance",
    "universally dominates pressure",
]
```

From `refine-logs/THEORY_AND_ATOMS.md` / `refine-logs/THEORY_AND_CLAIMS.md`:
```text
Movement value sign convention: MovementValue(i,j) = lambda_up - lambda_down.
Pressure special case: when storage/operational constraints are slack or ranking-neutral, MovementValue(i,j) = x_up - x_down.
Binding rank-change algebra: if pressure favors a over b but C_b - C_a > P_a - P_b, finite-storage dual scoring can rank b over a.
```

Required Phase 7 checker helper contract to create in `scripts/check_theory_separation.py`:
```python
def build_and_check_phase7_payload() -> dict[str, Any]: ...
def main() -> None: ...
```
</interfaces>
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Required Coverage | Plan Coverage |
|---|---|---|---|
| GOAL | Phase 7 goal: formal finite-storage primal-dual separation package explaining pressure recovery and finite-storage difference conditions | Must be delivered as executable theory/checker/test artifacts | Task 1, Task 2, Task 3 |
| REQ | THRY-01 slack max-pressure/backpressure recovery theorem | Memo theorem plus slack test/checker example | Task 1 tests, Task 2 JSON/checker, Task 3 memo |
| REQ | THRY-02 finite-storage/spillback/incident/switching separation theorem/counterexample | Deterministic explicit binding two-phase example | Task 1 tests, Task 2 checker/JSON, Task 3 memo |
| REQ | THRY-03 strict improvement under predeclared one-step objective | Predeclared objective components and margin > 0 | Task 1 tests, Task 2 checker/JSON, Task 3 memo |
| REQ | THRY-04 one additional guarantee candidate | Exactly one candidate: constrained LP oracle regret | Task 1 tests, Task 2 metadata, Task 3 memo |
| RESEARCH | Use stdlib algebraic checker unless LP dual extraction becomes necessary | No new dependency; deterministic static checker | Task 2 |
| RESEARCH | Reuse Phase 6 validators; do not hand-roll schema checks | Import `validate_state_objective_sample` and constants | Task 1, Task 2 |
| RESEARCH | Keep Phase 7 out of live controller/SUMO scope | Explicit forbidden scope guards and import restrictions | All tasks |
| CONTEXT | D-07-01 Special-Case Recovery | Implement exact sign convention and slack recovery assumptions | Task 1, Task 2, Task 3 |
| CONTEXT | D-07-02 Binding Separation | Use explicit finite-storage fields, not proxy-only labels | Task 1, Task 2, Task 3 |
| CONTEXT | D-07-03 Strict Improvement Criterion | Predeclare one-step objective using Phase 6 components | Task 1, Task 2, Task 3 |
| CONTEXT | D-07-04 Additional Guarantee Candidate | Select exactly constrained LP oracle regret by default | Task 1, Task 2, Task 3 |
| CONTEXT Deferred/Out of Scope | live controller, closed-loop SUMO, new baselines, paper drafting, v1.0 superiority reinterpretation | Must not appear as implementation scope | Scope guards in every task and verification |

No unplanned source item remains. Deferred ideas and other-phase requirements are excluded by Phase 7 context.
</source_audit>

<dependency_graph>
Task 1 creates compile-safe executable behavioral tests without requiring the future checker to exist yet. Task 2 implements the checker and generated JSON that makes checker-dependent tests pass. Task 3 writes the memo, tightens memo tests to require the memo, and verifies claim audit fail-closed without missing-path allowances. All files are Phase 7-only and do not overlap with controller/SUMO implementation paths.

- Task 1 needs: Phase 6 validators/policy patterns; creates: `tests/test_theory_separation.py` with deferred imports inside test functions and verifies syntax only.
- Task 2 needs: Task 1 behavior contract and Phase 6 schema validators; creates: `scripts/check_theory_separation.py`, `experiments/dual_sensitivity/phase7_theory_separation.json`, and passes checker-dependent tests.
- Task 3 needs: Task 2 artifact shape and Phase 1/Phase 7 theory context; creates: `refine-logs/THEORY_AND_SEPARATION.md`, removes any optional memo skip behavior, and passes full relevant tests plus claim audit.
</dependency_graph>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create red/green-compatible Phase 7 theory separation tests</name>
  <files>tests/test_theory_separation.py</files>
  <read_first>
    - `/home/samuel/projects/pi_light_OR/tests/test_finite_storage_schema.py` for `ROOT`, `SCRIPTS`, direct imports, schema validation assertions, and subprocess style.
    - `/home/samuel/projects/pi_light_OR/tests/test_claim_discipline.py` for claim audit subprocess helpers and bounded-claim assertions.
    - `/home/samuel/projects/pi_light_OR/.planning/phases/07-theory-and-separation-package/07-PATTERNS.md` sections “tests/test_theory_separation.py” and “Recommended tests”.
  </read_first>
  <behavior>
    - Test 1: `test_slack_recovery_matches_pressure_or_tie` imports `build_and_check_phase7_payload()`, locates the `slack_recovery` example, and asserts finite-storage action equals pressure action or the pressure action belongs to a declared tie set, per D-07-01 and THRY-01.
    - Test 2: `test_binding_example_separates_actions_with_explicit_state` validates every example with `validate_state_objective_sample()`, locates `storage_binding_two_phase_separation`, and asserts `pressure_action != finite_storage_action`, `actions_separate is True`, and explicit `finite_storage_state` fields include downstream storage/residual/spillback data, per D-07-02 and THRY-02.
    - Test 3: `test_binding_example_strictly_improves_predeclared_objective` asserts the JSON payload declares the one-step objective before action comparison, uses exactly Phase 6 objective component keys, and reports `objective_margin > 0` plus `strict_objective_improvement is True`, per D-07-03 and THRY-03.
    - Test 4: `test_phase7_checker_cli_writes_parseable_artifact` runs the checker with a `tmp_path` output and asserts `status == PASSED`, `requirements_covered == ["THRY-01", "THRY-02", "THRY-03", "THRY-04"]`, `schema_version == "phase6_explicit_state_v1"`, and the expected examples exist.
    - Test 5: `test_memo_requirement_markers_and_claim_safe_language` uses a helper that skips only when the memo is not present before Task 3; Task 3 must tighten this helper or add a final required-memo test so `refine-logs/THEORY_AND_SEPARATION.md` is mandatory by phase end. When present, the memo must contain `THRY-01` through `THRY-04`, must not contain forbidden affirmative phrases from `FORBIDDEN_CLAIM_PATTERNS`, and must keep v1.0 evidence historical/insufficient rather than superiority evidence.
    - Test 6: `test_guarantee_candidate_is_constrained_lp_oracle_regret` asserts payload and memo select exactly `constrained_lp_oracle_regret` / “constrained LP oracle regret” as the Phase 7 guarantee candidate, finite-sample/oracle-relative only, per D-07-04 and THRY-04.
  </behavior>
  <action>Create `tests/test_theory_separation.py` as the executable Phase 7 contract. Use the same `ROOT = Path(__file__).resolve().parents[1]`, `SCRIPTS = ROOT / "scripts"`, and `sys.path.insert(0, str(SCRIPTS))` pattern as existing tests. Import Phase 6 validators and claim policy at module scope, but keep `from check_theory_separation import build_and_check_phase7_payload` inside checker-dependent test functions or helper functions so the test file can compile before Task 2 exists. Include subprocess helper `run_checker(*args)` for `scripts/check_theory_separation.py`. Make Task 1 verify syntax/structure only; full pytest is intentionally deferred to Task 2 after implementation exists. Do not modify existing tests. Do not add tests that require live controller, closed-loop SUMO, new baselines, paper drafting, or v1.0 evidence as superiority.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 -m py_compile tests/test_theory_separation.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_theory_separation.py` exists and contains tests for slack recovery, binding separation, strict one-step objective improvement, CLI artifact writing, memo markers/claim safety, and constrained LP oracle regret.
    - The test module references THRY-01, THRY-02, THRY-03, THRY-04 either in test names, assertions, or comments/docstrings.
    - Checker-dependent imports are deferred inside functions/helpers so `python3 -m py_compile tests/test_theory_separation.py` passes before Task 2 creates `scripts/check_theory_separation.py`.
    - No test invokes SUMO, TraCI, CityFlow, `pi_light_code`, live controller code, closed-loop experiment runners, or baseline generation.
  </acceptance_criteria>
  <done>The Phase 7 test contract is present and compile-safe; checker-dependent pytest execution is deferred to Task 2/3 after implementation artifacts exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic checker and generated Phase 7 separation JSON</name>
  <files>scripts/check_theory_separation.py, experiments/dual_sensitivity/phase7_theory_separation.json</files>
  <read_first>
    - `/home/samuel/projects/pi_light_OR/scripts/finite_storage_schema.py` for schema constants, `SCHEMA_VERSION`, and validators.
    - `/home/samuel/projects/pi_light_OR/scripts/run_dual_sanity.py` only if needed for naming patterns; do not copy LP/SciPy dependency unless necessary.
    - `/home/samuel/projects/pi_light_OR/.planning/phases/07-theory-and-separation-package/07-RESEARCH.md` sections “THRY-02 / THRY-03 Binding Separation Example Recommendation” and “Concrete File and Command Plan for Planner”.
    - `/home/samuel/projects/pi_light_OR/.planning/phases/07-theory-and-separation-package/07-PATTERNS.md` sections “scripts/check_theory_separation.py” and “JSON artifact field pattern for Phase 7”.
  </read_first>
  <action>Create `scripts/check_theory_separation.py` with shebang, top docstring, `from __future__ import annotations`, stdlib imports (`argparse`, `json`, `Path`, `Any`), Phase 6 schema imports, `build_and_check_phase7_payload()`, and `main() -> None`. The checker must build deterministic static examples, validate each example with `validate_state_objective_sample()`, recompute pressure scores as `queue_up - queue_down`, recompute finite-storage scores as pressure plus explicit declared correction terms, select actions deterministically, compute objective totals from declared nonnegative weights over Phase 6 objective components, and fail closed if criteria are false. Generate `experiments/dual_sensitivity/phase7_theory_separation.json` by running the checker, not by manual JSON transcription. Use D-07-01 sign convention `lambda_up - lambda_down` / `queue_up - queue_down`; use D-07-02 explicit storage/spillback binding fields; use D-07-03 predeclared one-step objective; use D-07-04 exactly one guarantee candidate `constrained_lp_oracle_regret`. Top-level JSON must include `experiment: phase7_theory_separation`, `status`, `scope: theory_static_one_step_only_no_closed_loop_claims`, `generated_by`, `requirements_covered`, `schema_version`, `one_step_objective_definition`, `criteria`, `claim_scope`, and `examples`. Include at least one `slack_recovery` example and one `storage_binding_two_phase_separation` example. The binding example should follow the research values unless a test-safe equivalent is cleaner: pressure selects phase A because pressure A > pressure B; finite-storage correction penalizes the blocked downstream action so finite-storage selects phase B; action objective totals produce a strictly positive margin in favor of phase B. Do not import or call `traci`, `sumolib`, `cityflow`, `torch`, `pi_light_code`, closed-loop SUMO runners, or baseline scripts. Do not add new packages.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 scripts/check_theory_separation.py --out experiments/dual_sensitivity/phase7_theory_separation.json</automated>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 -m pytest tests/test_theory_separation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check_theory_separation.py` exposes `build_and_check_phase7_payload()` and `main()` and writes JSON with `Path(args.out).parent.mkdir(parents=True, exist_ok=True)` and `json.dumps(..., indent=2)`.
    - `experiments/dual_sensitivity/phase7_theory_separation.json` exists, parses as JSON, has `status: PASSED`, covers exactly THRY-01~THRY-04, and uses `schema_version: phase6_explicit_state_v1`.
    - Every example validates via `validate_state_objective_sample()` and contains Phase 6 `finite_storage_state` plus `objective_components` fields.
    - Binding example has `pressure_action != finite_storage_action`, `actions_separate: true`, `objective_margin > 0`, and `strict_objective_improvement: true`.
    - JSON claim/scope language is bounded: static one-step/theory only, no closed-loop or deployable performance claim.
  </acceptance_criteria>
  <done>The checker generates a deterministic passed Phase 7 JSON artifact and the Phase 7 tests pass except any memo-specific assertions that are intentionally completed in Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write bounded technical memo and verify tests plus claim audit</name>
  <files>refine-logs/THEORY_AND_SEPARATION.md</files>
  <read_first>
    - `/home/samuel/projects/pi_light_OR/refine-logs/THEORY_AND_CLAIMS.md` for theorem/proof-section style and sign convention wording.
    - `/home/samuel/projects/pi_light_OR/refine-logs/THEORY_AND_ATOMS.md` for movement marginal value, pressure special case, and oracle-regret candidate framing.
    - `/home/samuel/projects/pi_light_OR/experiments/dual_sensitivity/phase7_theory_separation.json` after Task 2 generates it.
    - `/home/samuel/projects/pi_light_OR/.planning/phases/07-theory-and-separation-package/07-CONTEXT.md` for D-07-01 through D-07-04 and out-of-scope boundaries.
  </read_first>
  <action>Create `refine-logs/THEORY_AND_SEPARATION.md` as a technical memo, not a paper draft. Use a clear header with date, Phase 07, requirements covered THRY-01~THRY-04, and primary references. Include “Claim-Discipline Guardrails” with Allowed and Not allowed lists. Include explicit sections: `THRY-01 — Slack finite-storage recovery of max-pressure/backpressure`, `THRY-02 — Explicit binding finite-storage separation`, `THRY-03 — Strict one-step constrained objective improvement`, and `THRY-04 — Constrained LP oracle regret guarantee candidate`. For THRY-01, state assumptions per D-07-01: infinite/nonbinding storage, residual receiving constraints slack, no switching loss, fixed turning ratios, incident/service terms absent or ranking-neutral, and movement value `lambda_up - lambda_down`; conclude recover/tie, not superiority. For THRY-02, describe the generated two-phase storage/spillback example from the JSON and the rank-change algebra per D-07-02. For THRY-03, predeclare `J(action) = delay + unfinished_vehicle_penalty + spillback_blocking_time + switching_lost_time` using Phase 6 component names and cite the JSON objective margin per D-07-03. For THRY-04, select exactly “constrained LP oracle regret” as a finite-sample, oracle-relative candidate per D-07-04; state it does not establish closed-loop network performance. Tighten `tests/test_theory_separation.py` if needed so phase-end tests require the memo to exist rather than skipping missing memo. Explicitly prohibit and avoid: live controller implementation, closed-loop SUMO evidence, new baselines, final paper/manuscript drafting, related-work writing, submission prep, and v1.0 pressure-equivalent evidence as superiority. Keep wording claim-audit-safe: do not use affirmative forbidden phrases such as `proves superiority`, `universal dominance`, or `static evidence proves closed-loop` except in negated/bounded “not allowed” contexts that the existing policy handles.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && test -f refine-logs/THEORY_AND_SEPARATION.md && test -f experiments/dual_sensitivity/phase7_theory_separation.json</automated>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 -m pytest tests/test_theory_separation.py -q</automated>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 -m pytest tests/test_finite_storage_schema.py tests/test_claim_discipline.py tests/test_theory_separation.py -q</automated>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 scripts/audit_claim_discipline.py --paths refine-logs/THEORY_AND_SEPARATION.md experiments/dual_sensitivity/phase7_theory_separation.json --policy-out /tmp/phase7_claim_policy.json --audit-out /tmp/phase7_claim_audit.json</automated>
  </verify>
  <acceptance_criteria>
    - Memo contains THRY-01, THRY-02, THRY-03, THRY-04 sections and references `experiments/dual_sensitivity/phase7_theory_separation.json`.
    - Memo states the movement-value sign convention as `lambda_up - lambda_down` and does not invert signs.
    - Memo states constrained LP oracle regret as the only Phase 7 guarantee candidate and labels stronger drift/closed-loop claims as later-phase work.
    - Memo and JSON both exist and pass `scripts/audit_claim_discipline.py` with `status: PASSED` without `--allow-missing-paths`.
    - Full relevant regression command passes for finite-storage schema, claim discipline, and theory separation tests.
  </acceptance_criteria>
  <done>Phase 7 has a bounded, auditable theory/separation memo consistent with the generated JSON and all required automated checks pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local checker → JSON artifact | Deterministic Python code emits a machine-readable artifact that may be used as future evidence; malformed or unchecked fields could create false confidence. |
| JSON/memo → claim audit/report surface | Prose and JSON strings cross into claim-facing surfaces; unsupported claim language could leak into future paper inputs. |
| Phase 7 static artifact → later controller/gate phases | Static one-step structural evidence could be misread as live-controller or closed-loop evidence if scope metadata is unclear. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `experiments/dual_sensitivity/phase7_theory_separation.json` | mitigate | Generate via `scripts/check_theory_separation.py`; validate every example with `validate_state_objective_sample`; tests assert status, requirements, schema version, actions, objective margin. |
| T-07-02 | Repudiation | `refine-logs/THEORY_AND_SEPARATION.md` | mitigate | Include THRY markers, objective definition, references to generated JSON, and claim audit verification so claims trace to artifacts. |
| T-07-03 | Information Disclosure | checker/test scripts | accept | Phase 7 uses no secrets, no external services, no `.env`, no account credentials; local JSON/prose only. |
| T-07-04 | Elevation of Privilege | accidental import/use of live controller/SUMO paths | mitigate | Task actions forbid `traci`, `sumolib`, `cityflow`, `torch`, `pi_light_code`, controller classes, closed-loop runners, and new baselines. |
| T-07-05 | Integrity | v1.0 pressure-equivalent evidence reinterpreted as superiority | mitigate | Reuse Phase 6 `audit_claim_discipline.py`; memo/JSON scope says static one-step/theory only; tests assert bounded language. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks; Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
Run these from the repository root after all tasks:

```bash
cd /home/samuel/projects/pi_light_OR && python3 scripts/check_theory_separation.py --out experiments/dual_sensitivity/phase7_theory_separation.json
cd /home/samuel/projects/pi_light_OR && python3 -m pytest tests/test_theory_separation.py -q
cd /home/samuel/projects/pi_light_OR && python3 -m pytest tests/test_finite_storage_schema.py tests/test_claim_discipline.py tests/test_theory_separation.py -q
cd /home/samuel/projects/pi_light_OR && python3 scripts/audit_claim_discipline.py --paths refine-logs/THEORY_AND_SEPARATION.md experiments/dual_sensitivity/phase7_theory_separation.json --policy-out /tmp/phase7_claim_policy.json --audit-out /tmp/phase7_claim_audit.json
```

Optional scope hygiene check if tests do not already cover it:

```bash
cd /home/samuel/projects/pi_light_OR && grep -R "traci\|sumolib\|cityflow\|torch\|pi_light_code\|run_closed_loop\|baseline" scripts/check_theory_separation.py tests/test_theory_separation.py refine-logs/THEORY_AND_SEPARATION.md | grep -v "Do not" | grep -v "Not allowed" || true
```
</verification>

<success_criteria>
- THRY-01 covered: slack example and memo theorem show finite-storage primal-dual score recovers/ties pressure under D-07-01 assumptions.
- THRY-02 covered: binding example and memo show pressure and finite-storage actions differ because explicit storage/spillback fields bind, not because of proxy-only labels.
- THRY-03 covered: binding example and memo show strict improvement under the predeclared one-step objective using Phase 6 objective component keys.
- THRY-04 covered: memo and JSON identify exactly constrained LP oracle regret as the additional finite-sample/oracle-relative guarantee candidate.
- `tests/test_theory_separation.py`, `scripts/check_theory_separation.py`, `experiments/dual_sensitivity/phase7_theory_separation.json`, and `refine-logs/THEORY_AND_SEPARATION.md` all exist and are internally consistent.
- Claim audit passes over the new memo and JSON artifact.
- No source/controller code is modified; no live controller, closed-loop SUMO, new baselines, paper drafting, or v1.0-superiority reinterpretation enters Phase 7.
</success_criteria>

<output>
Create `/home/samuel/projects/pi_light_OR/.planning/phases/07-theory-and-separation-package/07-01-SUMMARY.md` when done.
</output>
