---
phase: 10-strong-baselines-and-stress-scenario-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run_closed_loop_sumo.py
  - scripts/run_closed_loop_suite.py
  - tests/test_closed_loop_sumo.py
  - experiments/dual_sensitivity/phase10_baselines_stress_suite.json
autonomous: true
requirements:
  - EXP-01
  - EXP-02
  - EXP-04
user_setup: []
must_haves:
  truths:
    - "Suite includes strong feasible baselines/controllers and preserves old not-feasible guard honesty."
    - "Stress scenario coverage explicitly includes downstream blockage, spillback, incident/capacity drop, oversaturation, turning shock, and switching-loss-sensitive categories."
    - "Grid fixed-time counterexample is represented as an explicit check/metadata before broad performance language."
    - "Phase 10 artifact is smoke/scenario-capability evidence only and excludes Gate C, paired-seed dominance, long-horizon stats, and manuscript claims."
---

<objective>
完成 Phase 10 的 EXP-01、EXP-02、EXP-04：扩展 closed-loop suite 的强基线和显式 stress scenario 覆盖，生成 Phase 10 smoke/spec artifact，证明 suite capability 与 coverage，而不是证明 closed-loop dominance。
</objective>

<context>
@.planning/phases/10-strong-baselines-and-stress-scenario-suite/10-CONTEXT.md
@.planning/phases/10-strong-baselines-and-stress-scenario-suite/10-RESEARCH.md
@.planning/phases/10-strong-baselines-and-stress-scenario-suite/10-PATTERNS.md
@.planning/phases/10-strong-baselines-and-stress-scenario-suite/10-VALIDATION.md
@.planning/phases/09-slack-and-binding-kill-gates/09-VERIFICATION.md
@scripts/run_closed_loop_sumo.py
@scripts/run_closed_loop_suite.py
@tests/test_closed_loop_sumo.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 写入 Phase 10 suite/baseline/stress 合同测试</name>
  <files>tests/test_closed_loop_sumo.py</files>
  <action>扩展 `tests/test_closed_loop_sumo.py`，先定义 Phase 10 合同。测试必须覆盖：`CONTROLLER_REGISTRY`/suite default controllers 包含 fixed_time、actuated_local_pressure、max_pressure、capacity_aware_pressure、finite_storage_primal_dual，以及本计划必须实现的新增可行 baselines `cycle_pressure`、`finite_storage_double_pressure`；`local_pilight` 与 `full_dual_symbolic` 仍在 not-feasible guard；`optimized_fixed_time` 必须以 suite metadata 明确报告为当前 fixed-time 基线的可调/网格搜索占位或非优化限制说明，不能被默认为已完成性能优化；`build_suite_spec(profile="smoke")` 或新增 coverage helper 包含 stress categories：downstream_blockage、spillback、incident_capacity_drop、oversaturation、turning_shock、switching_loss_sensitive；Phase 10 payload/helper 包含 `strong_baseline_coverage`、`stress_scenario_coverage`、`grid_fixed_time_counterexample_check` 和 scope caveats；payload 不含 Gate C、paired-seed CI、dominance claim、long-horizon statistics、manuscript claim。测试可使用 synthetic rows，不要运行长时域 SUMO。</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 -m py_compile tests/test_closed_loop_sumo.py</automated>
  </verify>
  <acceptance_criteria>
    - 合同测试覆盖 EXP-01、EXP-02、EXP-04 和 scope guards。
    - 不调用 long-horizon SUMO 或 paired-seed stats。
  </acceptance_criteria>
  <done>Phase 10 合同测试定义强基线覆盖、显式 stress scenario 覆盖、grid fixed-time counterexample check 和 no-dominance/no-Gate-C scope guards。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 runner/suite 的 baseline 与 stress scenario coverage</name>
  <files>scripts/run_closed_loop_sumo.py, scripts/run_closed_loop_suite.py, tests/test_closed_loop_sumo.py</files>
  <action>在 `scripts/run_closed_loop_sumo.py` 中必须添加两个轻量可行 controller variants：`cycle_pressure`（cycle/phase-hold pressure variant，复用现有 pressure phase scoring 并加入 deterministic cycle/hold bias）与 `finite_storage_double_pressure`（finite-storage/capacity-aware double-pressure variant，复用 Phase 8 finite-storage helpers 或 capacity-aware pressure terms，不引入新依赖），并保持 `local_pilight`、`full_dual_symbolic` not-feasible。扩展 scenario mechanics/metadata，使 stress scenario tags 能显式标注 downstream blockage、spillback、incident capacity drop、oversaturation、turning shock、switching-loss-sensitive；可复用现有 demand insertion 和 edge speed reduction mechanics，短 smoke 只验证机制与 schema。在 `scripts/run_closed_loop_suite.py` 中扩展 `DEFAULT_CONTROLLERS`、scenario spec/coverage helper、`build_payload()`，加入 `strong_baseline_coverage`、`stress_scenario_coverage`、`grid_fixed_time_counterexample_check`、`optimized_fixed_time_metadata`、scope caveats。`optimized_fixed_time_metadata` 必须明确当前 Phase 10 是否实现了可调固定配时搜索；如果未实现搜索，必须报告 `status: documented_limit` 或等价限制说明，防止把普通 `fixed_time` 冒充 optimized fixed-time。不要添加 Gate C、paired-seed CI、long-horizon stats、manuscript claims 或 broad dominance wording。</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 -m py_compile scripts/run_closed_loop_sumo.py scripts/run_closed_loop_suite.py tests/test_closed_loop_sumo.py</automated>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 -m pytest tests/test_closed_loop_sumo.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Required baseline/controller set is registered, including `cycle_pressure` and `finite_storage_double_pressure`, or explicitly fails tests.
    - Smoke/spec validation includes `cycle_pressure` and `finite_storage_double_pressure` in controllers or artifact coverage assertions.
    - Stress coverage helper reports all six required stress categories.
    - Payload records grid fixed-time counterexample check metadata and `optimized_fixed_time_metadata` with honest implemented/documented-limit status.
    - Existing Phase 8/9 schema/action-audit tests remain green.
  </acceptance_criteria>
  <done>Runner and suite expose Phase 10 controller/stress coverage metadata and tests pass without adding Phase 11 evidence machinery。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 生成 Phase 10 smoke/spec artifact 并运行回归</name>
  <files>scripts/run_closed_loop_sumo.py, scripts/run_closed_loop_suite.py, tests/test_closed_loop_sumo.py, experiments/dual_sensitivity/phase10_baselines_stress_suite.json</files>
  <action>运行 Phase 10 smoke/spec command 生成 `experiments/dual_sensitivity/phase10_baselines_stress_suite.json`。该 artifact 必须展示 required controller/baseline coverage、stress scenario coverage、grid fixed-time counterexample check、finite-storage state/objective schema metadata、scope caveats；可包含短 smoke rows，但不得被描述为 performance evidence。运行 Phase 10 相关回归。完成后写 `10-01-SUMMARY.md`、`10-VERIFICATION.md`，并更新 ROADMAP/REQUIREMENTS/STATE/PROJECT 标记 Phase 10 complete。</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 scripts/run_closed_loop_suite.py --profile smoke --controllers fixed_time actuated_local_pressure max_pressure capacity_aware_pressure cycle_pressure finite_storage_double_pressure finite_storage_primal_dual --steps 80 --warmup 20 --action-interval 10 --out experiments/dual_sensitivity/phase10_baselines_stress_suite.json</automated>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 -m pytest tests/test_closed_loop_sumo.py tests/test_slack_binding_gates.py tests/test_theory_separation.py tests/test_finite_storage_schema.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Phase 10 artifact exists and parses.
    - Artifact contains baseline and stress coverage plus scope caveats.
    - Relevant regression suite passes.
  </acceptance_criteria>
  <done>Phase 10 smoke/spec artifact and docs are generated, regressions pass, and planning state is updated without Gate C/dominance/long-horizon scope creep。</done>
</task>

</tasks>

<verification>
必须运行：

1. `cd /home/samuel/projects/pi_light_OR && python3 -m py_compile scripts/run_closed_loop_sumo.py scripts/run_closed_loop_suite.py tests/test_closed_loop_sumo.py`
2. `cd /home/samuel/projects/pi_light_OR && python3 -m pytest tests/test_closed_loop_sumo.py -q`
3. `cd /home/samuel/projects/pi_light_OR && python3 scripts/run_closed_loop_suite.py --profile smoke --controllers fixed_time actuated_local_pressure max_pressure capacity_aware_pressure cycle_pressure finite_storage_double_pressure finite_storage_primal_dual --steps 80 --warmup 20 --action-interval 10 --out experiments/dual_sensitivity/phase10_baselines_stress_suite.json`
4. `cd /home/samuel/projects/pi_light_OR && python3 -m pytest tests/test_closed_loop_sumo.py tests/test_slack_binding_gates.py tests/test_theory_separation.py tests/test_finite_storage_schema.py -q`
</verification>

<success_criteria>
- EXP-01: strong feasible baseline/controller coverage is explicit and honest, including `cycle_pressure`, `finite_storage_double_pressure`, and honest optimized fixed-time metadata/limitation status.
- EXP-02: grid fixed-time counterexample check/metadata exists before broad performance language.
- EXP-04: all six required stress scenario categories are covered in suite metadata/spec.
- No Gate C, paired-seed dominance, long-horizon statistics, manuscript text, or broad dominance claims are introduced.
</success_criteria>
