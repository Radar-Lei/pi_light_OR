# Phase 12.1: Close v1.1 gap: execute or downgrade Gate C long-horizon evidence - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-25
**Phase:** 12.1-close-v1-1-gap-execute-or-downgrade-gate-c-long-horizon-evidence
**Areas discussed:** Evidence route

---

## Gray Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| Evidence route | Choose whether to execute long-horizon Gate C, execute a partial subset, or formally downgrade as future work. | ✓ |
| Downgrade wording | If not executing expensive experiments, decide how ROADMAP/requirements/artifacts should mark GATE-03, EXP-03, EXP-05. | |
| Compute budget | If executing experiments, decide acceptable network/scenario/seed/demand multiplier scope and runtime ceiling. | |
| Claude decides | Use the conservative current evidence discipline by default. | |

**User's choice:** Evidence route.
**Notes:** User chose to discuss only the route for closing the Gate C evidence gap.

---

## Evidence Route

| Option | Description | Selected |
|--------|-------------|----------|
| Formally downgrade | Do not execute 2160 long-horizon SUMO rows; mark Gate C/EXP-03/EXP-05 as future evidence input / pending. | |
| Small execution | Execute a constrained real subset as pilot/partial evidence only. | |
| Complete execution | Execute the Phase 11 main profile so Gate C can become PASSED/FAILED from real evidence. | ✓ |
| Claude decides | Let Claude choose the most conservative and least misleading route. | |

**User's choice:** Complete execution.
**Notes:** The selected route is to actually run the long-horizon paired-seed evidence rather than downgrade the claim without execution.

---

## Failure Handling

| Option | Description | Selected |
|--------|-------------|----------|
| Fail closed | Any missing row, unpaired seed, or run failure remains FAILED/INCONCLUSIVE; completed rows can be retained for audit only. | ✓ |
| Allow partial | Keep partial evidence as pilot/partial, but strict Gate C cannot pass. | |
| Retry to complete | Automatically retry failed rows until the full main profile is complete before strict Gate C. | |
| Claude decides | Follow Phase 11 fail-closed rules. | |

**User's choice:** Fail closed.
**Notes:** Partial or failed execution must not be promoted into dominance evidence.

---

## Execution Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Keep original definition | 6 binding scenarios × 6 controllers × 20 seeds × 3 demand multipliers = 2160 rows. | ✓ |
| Minimal complete gate | Run only Gate C required comparators and scenarios, with an explicit deviation from original profile. | |
| Extend to 7200s | Use a heavier 7200s main evidence profile. | |
| Claude decides | Prefer the predeclared Phase 11 profile unless code proves infeasible. | |

**User's choice:** Keep original definition.
**Notes:** Downstream planning should preserve the Phase 11 main profile rather than shrinking or expanding it.

---

## Claude's Discretion

- Choose safe execution mechanics: batching, resumability, checkpointing, logging, timeout management, and verification commands.

## Deferred Ideas

- Downgrade-only route was considered but not selected.
- Partial/pilot evidence route was considered but not selected as the target; partial rows may only remain as audit/provenance if complete execution fails.
