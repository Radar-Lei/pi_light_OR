---
phase: 11-long-horizon-paired-seed-evidence
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - scripts/run_closed_loop_sumo.py
  - scripts/run_phase11_paired_evidence.py
  - experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json
autonomous: true
requirements:
  - EXP-03
  - EXP-05
  - GATE-03
must_haves:
  truths:
    - "User can run a dedicated Phase 11 pilot or main profile that produces `phase11_long_horizon_paired_seed_evidence.json` without overloading Phase 10 semantics."
    - "The main profile generates executed 3600s+ rows with 900s warmup, paired seeds, actual demand multiplier behavior, and strongest feasible comparators, or records explicit main-profile FAILED/INCONCLUSIVE reasons."
    - "The artifact records raw rows, paired statistics for every applicable D-11-04 primary metric, Gate C classifications, profile, actual seed count, caveats, metric schema, demand multiplier provenance, and finite-storage audit schema."
  artifacts:
    - path: scripts/run_closed_loop_sumo.py
      provides: "Optional `sumocfg_override` execution hook so generated Phase 11 scaled configs are actually loaded by SUMO while preserving base network topology"
    - path: scripts/run_phase11_paired_evidence.py
      provides: "Executable Phase 11 CLI runner over existing SUMO `run_experiment`, demand multiplier adapter, payload builder, and artifact writer"
    - path: experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json
      provides: "Generated Phase 11 main paired-seed evidence artifact, or explicit main FAILED/INCONCLUSIVE artifact if execution cannot complete"
  key_links:
    - from: scripts/run_phase11_paired_evidence.py
      to: scripts/run_closed_loop_sumo.py
      via: "calls `run_experiment(..., sumocfg_override=generated_sumocfg)` for each Phase 11 spec row after generating scaled route/config inputs"
      pattern: "sumocfg_override"
    - from: scripts/run_phase11_paired_evidence.py
      to: experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json
      via: "writes JSON payload with `scenario_results`, `paired_statistics`, `gate_c`, and demand multiplier provenance"
      pattern: "phase11_long_horizon_paired_seed_evidence"
---

<objective>
Turn the Phase 11 helper layer into an executable experiment runner and artifact writer. The runner must support a fast `pilot` profile and the required journal-grade `main` profile using long horizons, paired seeds, actual demand multiplier behavior, and the strongest feasible Phase 10 baselines.

Purpose: Satisfy EXP-03 by producing a dedicated Phase 11 long-horizon paired-seed artifact, while carrying EXP-05 paired statistics for all D-11-04 primary metrics and GATE-03 preliminary classifications forward from Plan 01. Pilot tooling is not phase completion; the main profile must run/generate 3600s+ rows or fail/inconclusive explicitly.

Output: Updated `scripts/run_phase11_paired_evidence.py` CLI plus generated `experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json`.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-long-horizon-paired-seed-evidence/11-CONTEXT.md
@.planning/phases/11-long-horizon-paired-seed-evidence/11-RESEARCH.md
@.planning/phases/11-long-horizon-paired-seed-evidence/11-PATTERNS.md
@.planning/phases/11-long-horizon-paired-seed-evidence/11-VALIDATION.md
@.planning/phases/11-long-horizon-paired-seed-evidence/11-01-SUMMARY.md
@scripts/run_phase11_paired_evidence.py
@scripts/run_closed_loop_sumo.py
@scripts/run_closed_loop_suite.py
@experiments/dual_sensitivity/phase10_baselines_stress_suite.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add executable Phase 11 CLI and actual demand multiplier behavior</name>
  <files>scripts/run_closed_loop_sumo.py, scripts/run_phase11_paired_evidence.py</files>
  <read_first>
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-CONTEXT.md` decisions D-11-01, D-11-02, D-11-03.
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-RESEARCH.md` `## Open Questions (RESOLVED)` item 3 and demand multiplier anti-patterns.
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-PATTERNS.md` SUMO primitive pattern and CLI validation pattern.
    - `scripts/run_closed_loop_sumo.py` for `run_experiment`, `load_route_metadata`, `CONTROLLER_REGISTRY`, `METRIC_FIELDS`, route/config arguments, and row schema.
    - `scripts/run_phase11_paired_evidence.py` from Plan 01.
  </read_first>
  <action>Extend `scripts/run_closed_loop_sumo.py::run_experiment` with a concrete optional argument `sumocfg_override: str | Path | None = None`. Inside `run_experiment`, keep `paths = resolve_network(network)` for the base `net_file` and topology, then set `sumocfg_path = Path(sumocfg_override) if sumocfg_override is not None else paths["sumocfg"]`, validate the override exists, use `sumocfg_path` in the TraCI command, and record `sumocfg: str(sumocfg_path)` plus `base_sumocfg: str(paths["sumocfg"])` on completed rows. Extend `scripts/run_phase11_paired_evidence.py` with `parse_args`, `validate_args`, and `main() -> None`. CLI must support `--profile pilot|main`, `--controllers`, `--seeds`, `--steps`, `--warmup`, `--action-interval`, `--demand-multipliers`, `--route-json`, `--out`, and a safe `--dry-run` spec-only mode. Defaults must be Phase 11-specific: output path `experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json`, main profile target at least 3600 steps and 900 warmup per D-11-02, paired seeds shared across proposed and comparator rows, and required comparators from D-11-03. Implement actual demand multiplier behavior before execution per EXP-03 and resolved research: for each multiplier, generate scaled `.rou.xml` and `.sumocfg` files under `experiments/dual_sensitivity/phase11_scaled_inputs/`, pass the generated `.sumocfg` path into `run_experiment(..., sumocfg_override=generated_sumocfg)`, record `demand_multiplier`, `demand_scaling_method="scaled_route_sumocfg_override"`, `base_demand_total`, `scaled_demand_total`, `generated_route_file`, `generated_sumocfg`, and `base_sumocfg` on every row, and validate that distinct multipliers such as 0.8, 1.0, and 1.2 yield distinct generated config/route paths and distinct demand totals. Do not implement a second TraCI loop. Validate unknown controllers, nonpositive horizon/action interval, negative warmup, empty seeds, invalid demand multipliers, metadata-only multiplier configurations, missing generated override files, and path extension before execution. Keep `fixed_time` as context-only if present and keep `local_pilight`/old `full_dual_symbolic` excluded unless existing registry marks them not feasible; do not change prior safety decisions.</action>
  <verify>
    <automated>python tests/test_phase11_paired_evidence.py</automated>
  </verify>
  <acceptance_criteria>`scripts/run_closed_loop_sumo.py` contains `sumocfg_override: str | Path | None = None`; `run_experiment` uses the override path in the `traci.start` command and records both `sumocfg` and `base_sumocfg`; `scripts/run_phase11_paired_evidence.py` generates multiplier-specific `.rou.xml` and `.sumocfg` files under `experiments/dual_sensitivity/phase11_scaled_inputs/`; Phase 11 spec rows pass `sumocfg_override=generated_sumocfg` into `run_experiment`; tests assert distinct multipliers produce distinct generated config/route paths and distinct `scaled_demand_total` values; runner refuses metadata-only multiplier configurations before simulation starts.</acceptance_criteria>
  <done>EXP-03 executable runner path exists with real demand multiplier behavior and without Phase 10 dominance reuse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Phase 11 payload schema with all-primary-metric statistics and main evidence eligibility</name>
  <files>scripts/run_phase11_paired_evidence.py, experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json</files>
  <read_first>
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-PATTERNS.md` artifact shape, payload construction pattern, schema metadata pattern, and coverage metadata pattern.
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-RESEARCH.md` resolved Gate C primary-metric family rule and main-profile requirement.
    - `experiments/dual_sensitivity/phase10_baselines_stress_suite.json` only for artifact-shape analogs and caveat style, not evidence rows.
  </read_first>
  <action>Implement `build_payload(...)` and artifact writing for Phase 11. The payload must include `experiment="phase11_long_horizon_paired_seed_evidence"`, `status` among `PASSED`, `FAILED`, `INCONCLUSIVE`, or `PILOT_ONLY`, profile, steps, warmup, action interval, demand multipliers, demand scaling provenance, actual seed count, paired seed design statement, required binding scenarios, required Gate C comparators, route metadata, `scenario_results`, `paired_statistics` covering every applicable D-11-04 primary metric from Plan 01, `gate_c` generated by the reusable primary-metric family rule, `paired_seed_alignment`, `statistical_family`, metric schema, objective component schema, finite-storage state schema, and caveats that Phase 12 still regenerates final manuscript inputs. Pilot artifacts must clearly state they validate pipeline only and are not journal-grade dominance evidence. Main artifacts must record the 3600+/900 target, execution mode, actual seed count, actual row count, missing-row reasons if any, and whether all rows are executed rather than dry-run placeholders. A main artifact with missing executions may be `FAILED` or `INCONCLUSIVE`; a pilot-only or dry-run artifact must not be treated as phase-complete evidence. Write JSON with `Path(...).write_text(...)`, create the parent directory, and print a compact JSON summary.</action>
  <verify>
    <automated>python tests/test_phase11_paired_evidence.py && python scripts/run_phase11_paired_evidence.py --profile pilot --steps 120 --warmup 30 --seeds 20260523 --demand-multipliers 1.0 --dry-run --out experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json</automated>
  </verify>
  <acceptance_criteria>The generated artifact is a Phase 11 artifact with raw row placeholders or executed rows, all-primary-metric paired-statistics/gate sections, profile metadata, demand provenance, schema metadata, and claim caveats; it never presents Phase 10 smoke rows, pilot-only rows, or metadata-only demand sweeps as paired-seed dominance evidence.</acceptance_criteria>
  <done>Dedicated Phase 11 artifact exists at the requested path and records pilot/main distinction, demand behavior, and all-primary-metric Gate C status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Execute or fail-closed the main 3600s+ profile artifact</name>
  <files>scripts/run_phase11_paired_evidence.py, experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json</files>
  <read_first>
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-CONTEXT.md` decisions D-11-02 and D-11-05.
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-RESEARCH.md` `## Open Questions (RESOLVED)` item 2 and Validation Architecture phase gate.
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-VALIDATION.md` full suite command and manual-only verification note.
    - `scripts/run_closed_loop_sumo.py` for runtime row fields and failure statuses.
  </read_first>
  <action>Ensure the CLI can run a short pilot profile for plumbing, but make the plan output a main-profile artifact rather than stopping at pilot. Add or use a `--profile main` command path that generates/executes rows with at least 3600 steps, 900 warmup, paired seeds, required strong comparators, six binding scenarios, and multiple predeclared demand multipliers `0.8`, `1.0`, and `1.2`. The generated `experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json` must have `profile="main"` for phase completion. Main evidence should use the predeclared 20-seed target where computationally feasible; if execution uses fewer than 20 seeds, the artifact must set `status="INCONCLUSIVE"` unless a later user-approved run completes the 20-seed target. If local runtime or SUMO execution prevents completing all rows, write a main `FAILED` or `INCONCLUSIVE` artifact with attempted row counts, missing row keys, exception/status reasons, seed-count limitation, demand-multiplier coverage limitation if any, and no `PASSED` dominance claim. Do not treat the pilot/dry-run artifact from Task 2 as the completed Phase 11 evidence artifact. Add tests or artifact assertions that `profile="main"` requires steps `>= 3600`, warmup `>= 900`, at least two paired seeds for CI construction, multiple demand multipliers including `0.8`, `1.0`, and `1.2`, executed row status rather than dry-run placeholders, nonempty demand scaling provenance, and all required scenario/controller/seed/demand-multiplier combinations or explicit fail-closed reasons.</action>
  <verify>
    <automated>python tests/test_phase11_paired_evidence.py && python scripts/run_phase11_paired_evidence.py --profile main --steps 3600 --warmup 900 --seeds 20260523 20260524 20260525 20260526 20260527 20260528 20260529 20260530 20260531 20260601 20260602 20260603 20260604 20260605 20260606 20260607 20260608 20260609 20260610 20260611 --demand-multipliers 0.8 1.0 1.2 --out experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json</automated>
  </verify>
  <acceptance_criteria>The final Phase 11 evidence artifact is a main-profile artifact with executed 3600s+ rows, 900s warmup, multiple predeclared demand multipliers `0.8`, `1.0`, and `1.2`, and a 20-seed paired main target, or a machine-readable main `FAILED`/`INCONCLUSIVE` artifact explaining why the required rows, seed count, or multiplier sweep could not be completed. A one-seed or single-multiplier main artifact cannot have `status="PASSED"`. Pilot-only/dry-run artifacts cannot satisfy this task.</acceptance_criteria>
  <done>EXP-03 long-horizon paired-seed evidence target is executed or fail-closed as main-profile evidence, not merely predeclared.</done>
</task>

</tasks>

<verification>
Run `python tests/test_phase11_paired_evidence.py`, the dry-run artifact command from Task 2, and the main-profile command from Task 3 using 20 paired seeds and demand multipliers `0.8 1.0 1.2`. The pilot command may produce `PILOT_ONLY` or `INCONCLUSIVE`; the final phase artifact must be `profile="main"` with executed 3600s+ rows and the predeclared multiplier sweep, or explicit main `FAILED`/`INCONCLUSIVE` reasons. If runtime is too high, do not replace the main artifact with pilot evidence, one-seed evidence, or single-multiplier evidence.
</verification>

<success_criteria>
- `scripts/run_phase11_paired_evidence.py` has a CLI with pilot/main profiles, validation, dry-run/spec-only support, actual demand multiplier behavior, and existing `run_experiment` integration.
- `experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json` exists as a main-profile artifact and contains Phase 11 profile, paired seed design, required scenarios/comparators, demand scaling provenance, all-primary-metric paired statistics, Gate C sections, schema metadata, caveats, and executed-row or fail-closed main status.
- Phase 10 artifact may be cited as capability/scope context only and is not consumed as dominance evidence.
- Pilot-only and metadata-only demand multiplier artifacts cannot be marked as phase-complete evidence.
</success_criteria>

<must_haves>
<truths>
- User can invoke a Phase 11 runner that writes the dedicated Phase 11 artifact.
- User can distinguish pilot pipeline validation from main 3600+/900 paired-seed evidence, and pilot-only artifacts cannot complete the phase.
- User can inspect the artifact and see seed pairing, actual demand multiplier behavior, required comparators, all D-11-04 primary metrics, and Gate C sections.
</truths>
<artifacts>
- `scripts/run_phase11_paired_evidence.py` provides the CLI runner, demand multiplier adapter, and payload writer.
- `experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json` provides the generated main Phase 11 artifact or main fail-closed artifact.
</artifacts>
<key_links>
- CLI spec rows generate scaled route/config inputs and pass `sumocfg_override=generated_sumocfg` into `run_closed_loop_sumo.run_experiment(...)`.
- Raw rows feed Plan 01 paired statistics and Gate C primary-metric primitives.
- Payload writer emits the artifact consumed by Plan 03 checker.
</key_links>
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> SUMO run spec | User-provided horizons, seeds, paths, controllers, and demand multipliers determine expensive local execution. |
| Demand multiplier adapter -> SUMO route/config inputs | Generated demand inputs must reflect requested multipliers and not silently leave demand unchanged. |
| SUMO rows -> JSON artifact | Runtime outputs must remain auditable and not be silently dropped. |
| Artifact paths -> filesystem | Output path handling could overwrite unintended files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | CLI/profile validation | mitigate | Reject unknown controllers, empty seeds, invalid horizons/warmups/action intervals, invalid demand multipliers, metadata-only demand sweep configs, and non-JSON output paths before execution. |
| T-11-06 | Tampering | demand multiplier adapter | mitigate | Record base/scaled demand totals and route/config or insertion-intensity provenance for each multiplier; fail/inconclusive if distinct multipliers do not alter actual demand. |
| T-11-07 | Tampering | artifact writer | mitigate | Use explicit `Path` writes under caller-provided JSON path, create parent directories, and include route/profile/source metadata in payload. |
| T-11-08 | Denial of Service | main profile execution | mitigate | Provide dry-run and pilot profile for plumbing, but require main artifact execution or explicit fail-closed status for phase completion. |
| T-11-09 | Information Integrity | payload caveats/status | mitigate | Pilot artifacts cannot claim Gate C dominance; Phase 10 rows cannot be imported as dominance evidence; all D-11-04 primary metrics must be present in statistics. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Run long-horizon closed-loop paired-seed experiments and statistical uncertainty | 11-02 | COVERED | Dedicated runner/artifact.
REQ | EXP-03 | 3600–7200s horizons, warmup, demand multiplier sweeps, paired seeds | 11-02 | COVERED | CLI/profile, real demand multiplier behavior, and main artifact execution/fail-closed status.
REQ | EXP-05 | Statistical reports in artifact | 11-01, 11-02 | COVERED | Helper stats from Plan 01 included in payload for all applicable D-11-04 metrics.
REQ | GATE-03 | Gate C evidence sections in artifact | 11-02, 11-03 | COVERED | Payload sections here; strict checker in Plan 03.
RESEARCH | RESOLVED-Q2 | Main profile must generate/run 3600s+ rows or fail/inconclusive; pilot-only cannot complete phase | 11-02 | COVERED | Task 3 requires main artifact execution or fail-closed main artifact.
RESEARCH | RESOLVED-Q3 | Demand multipliers must alter actual SUMO behavior | 11-02 | COVERED | Task 1 implements route/insertion scaling and provenance.
RESEARCH | — | Reuse existing SUMO primitive, not second TraCI loop | 11-02 | COVERED | Runner calls `run_experiment`.
CONTEXT | D-11-02 | Dedicated runner/artifact, pilot/main distinction, 3600/900, paired seeds | 11-02 | COVERED | Primary plan output.
CONTEXT | D-11-03 | Strong feasible baselines | 11-02 | COVERED | CLI defaults and validation.
CONTEXT | D-11-04 | Multiple primary metrics and paired statistical reporting | 11-01, 11-02 | COVERED | Payload includes all-primary-metric statistics.
CONTEXT | D-11-06 | Phase 12 owns manuscript inputs | 11-02 | COVERED | Caveats in payload.
</source_audit>

<output>
Create `.planning/phases/11-long-horizon-paired-seed-evidence/11-02-SUMMARY.md` when done.
</output>
