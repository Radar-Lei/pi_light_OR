---
phase: 11-long-horizon-paired-seed-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run_phase11_paired_evidence.py
  - tests/test_phase11_paired_evidence.py
autonomous: true
requirements:
  - GATE-03
  - EXP-03
  - EXP-05
must_haves:
  truths:
    - "Phase 11 has pure helpers that construct paired-seed evidence profiles without reusing Phase 10 dominance semantics."
    - "Paired statistics are computed from seed-aligned proposed/comparator differences for every applicable D-11-04 primary metric with confidence intervals, effect sizes, and family metadata."
    - "Gate C has one reusable bounded-improvement rule that both the runner and strict checker consume, and it rejects missing scenarios, comparators, metrics, demand behavior, metadata, unpaired seeds, and forbidden claim language."
  artifacts:
    - path: scripts/run_phase11_paired_evidence.py
      provides: "Phase 11 constants, spec builder, demand multiplier contract, paired-statistics helpers, Gate C core evaluator, and payload-scope validation helpers"
    - path: tests/test_phase11_paired_evidence.py
      provides: "Synthetic direct-assertion tests for profile construction, demand multiplier behavior contracts, paired statistics, fail-closed Gate C inputs, and claim discipline"
  key_links:
    - from: tests/test_phase11_paired_evidence.py
      to: scripts/run_phase11_paired_evidence.py
      via: "imports pure helper functions from scripts path"
      pattern: "from run_phase11_paired_evidence import"
    - from: scripts/run_phase11_paired_evidence.py
      to: scripts/run_closed_loop_sumo.py
      via: "imports METRIC_FIELDS and row validation contracts without creating a second TraCI loop"
      pattern: "from run_closed_loop_sumo import"
---

<objective>
Create the Phase 11 pure contract layer: predeclared binding scenarios, paired seed profile helpers, actual demand multiplier contracts, paired statistical reporting helpers for every D-11-04 primary metric, one reusable Gate C bounded-improvement rule, and synthetic tests. This plan implements the evidence-governance core before any SUMO long-horizon orchestration.

Purpose: Satisfy GATE-03, EXP-03, and EXP-05 foundations while honoring D-11-01 through D-11-06: Phase 11 evidence is binding-regime-only, paired-seed, simulator/network/horizon/seed/demand-relative, and not a reinterpretation of Phase 10 smoke rows.

Output: `scripts/run_phase11_paired_evidence.py` helper layer and `tests/test_phase11_paired_evidence.py` synthetic tests.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-long-horizon-paired-seed-evidence/11-CONTEXT.md
@.planning/phases/11-long-horizon-paired-seed-evidence/11-RESEARCH.md
@.planning/phases/11-long-horizon-paired-seed-evidence/11-PATTERNS.md
@.planning/phases/11-long-horizon-paired-seed-evidence/11-VALIDATION.md
@scripts/run_closed_loop_sumo.py
@scripts/run_closed_loop_suite.py
@scripts/run_slack_binding_gates.py
@tests/test_closed_loop_sumo.py
@tests/test_slack_binding_gates.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Phase 11 evidence profile contracts and demand multiplier contract tests</name>
  <files>scripts/run_phase11_paired_evidence.py, tests/test_phase11_paired_evidence.py</files>
  <read_first>
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-CONTEXT.md` decisions D-11-01, D-11-02, D-11-03, D-11-06.
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-RESEARCH.md` `## Open Questions (RESOLVED)` and Pattern 1 / Pattern 3 demand multiplier requirements.
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-PATTERNS.md` sections for `BINDING_EVIDENCE_SCENARIOS`, `REQUIRED_GATE_C_COMPARATORS`, CLI/spec builder, and direct executable test pattern.
    - `scripts/run_closed_loop_suite.py` for Phase 10 scenario names only; do not reuse Phase 10 smoke rows as dominance evidence per D-11-01.
  </read_first>
  <action>Create `scripts/run_phase11_paired_evidence.py` with Phase 11-only constants: `PROPOSED_CONTROLLER = "finite_storage_primal_dual"`, `BINDING_EVIDENCE_SCENARIOS` exactly equal to the six D-11-01 scenario tags, `SLACK_CONTEXT_SCENARIOS` for sanity/recovery context, `REQUIRED_GATE_C_COMPARATORS` exactly `max_pressure`, `capacity_aware_pressure`, and `finite_storage_double_pressure`, optional context controllers `cycle_pressure` and `actuated_local_pressure`, and demand multiplier provenance keys. Add a pure `build_phase11_spec(profile, controllers, seeds, steps, warmup, action_interval, demand_multipliers)` helper that returns rows for binding scenarios, controllers, paired seeds, and demand multipliers; `profile="main"` defaults must represent the D-11-02 journal-grade target of at least 3600 steps, 900 warmup, and paired seeds, while `profile="pilot"` must be explicitly marked as pipeline-validation evidence, not Gate C dominance evidence. Per EXP-03 and resolved research, include a demand multiplier contract in each spec row requiring actual SUMO behavior change via route demand scaling or insertion-intensity scaling; metadata-only multipliers are invalid. In `tests/test_phase11_paired_evidence.py`, first add tests that assert binding scenario coverage, paired seed equality across proposed and required comparators, main 3600/900 defaults, pilot/main evidence labels, and demand multiplier sweep contract fields. Then implement the helper until these tests pass.</action>
  <verify>
    <automated>python tests/test_phase11_paired_evidence.py</automated>
  </verify>
  <acceptance_criteria>`build_phase11_spec` produces Phase 11-only specs with no Phase 10 dominance reuse, all D-11-01 binding scenarios, required strong comparators, shared seed sets per scenario/comparator/proposed controller, profile metadata, and demand multiplier entries that require actual SUMO demand behavior changes.</acceptance_criteria>
  <done>EXP-03 profile construction and demand multiplier behavior contracts are test-covered and deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement paired statistics and reusable Gate C primary-metric family rule</name>
  <files>scripts/run_phase11_paired_evidence.py, tests/test_phase11_paired_evidence.py</files>
  <read_first>
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-CONTEXT.md` decisions D-11-04 and D-11-05.
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-RESEARCH.md` `## Open Questions (RESOLVED)`, “Pattern 2: Paired Difference as the Atomic Statistical Unit”, and “Pattern 4: Reusable Gate C Primary-Metric Family Rule”.
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-PATTERNS.md` paired statistics helper shape.
  </read_first>
  <action>Add direction-aware metric helpers per D-11-04 and resolved research. Export `GATE_C_PRIMARY_METRICS = ("penalized_avg_travel_time", "total_delay", "spillback_count", "blocking_count", "unfinished_vehicle_count")`, `GATE_C_CONDITIONAL_PRIMARY_METRICS` requiring `switching_count` for `arterial_switching_loss_sensitive` and any row/scenario with switching-loss metadata, and `GATE_C_STATISTICAL_FAMILY = "gate_c_primary_metrics_v1"`. Lower-is-better metrics report improvement as comparator minus proposed; higher-is-better metrics report proposed minus comparator. Implement pure helpers for grouping completed rows by `(scenario_tag, demand_multiplier, comparator, metric, seed)`, rejecting unpaired proposed/comparator seed sets, computing mean paired difference, paired bootstrap 95% confidence interval using SciPy, paired t/Wilcoxon diagnostics when available, standardized paired effect size, sample size, metric direction, `positive_means`, and `difference_definition`. Implement one reusable `evaluate_gate_c_primary_metric_rule` or equivalent that classifies every applicable scenario/comparator/demand-multiplier/metric as non-worsening when `ci_low >= 0`, bounded harm when `ci_high < 0`, or inconclusive when the CI crosses zero; applies Holm-Bonferroni or equivalent family metadata over all `gate_c_primary_metrics_v1` diagnostics; returns overall `PASSED` only when every applicable primary metric is non-worsening and each scenario/comparator/demand-multiplier family has at least one strict positive signal, `FAILED` for any bounded harm or missing precondition, otherwise `INCONCLUSIVE`. In tests, create synthetic paired rows where the proposed controller is better, worse, tied, unpaired, missing one D-11-04 metric, and missing switching metric for switching-loss scenarios; assert signs, CI fields, effect-size fields, family metadata, all-primary-metric coverage, exact pass/fail/inconclusive status, and fail-closed unpaired behavior per EXP-05.</action>
  <verify>
    <automated>python tests/test_phase11_paired_evidence.py</automated>
  </verify>
  <acceptance_criteria>Paired statistics never use independent controller means as Gate C evidence; every statistic records seed count, paired differences, CI bounds, effect size, metric direction, comparison family metadata, and explicit failure/inconclusive reasons. The reusable Gate C rule covers all D-11-04 primary metrics and is not reducible to `penalized_avg_travel_time` alone.</acceptance_criteria>
  <done>EXP-05 paired statistical reporting and the exact D-11-04/D-11-05 Gate C rule are test-covered with synthetic pass/fail/inconclusive cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Gate C core evaluator, demand provenance validation, and claim-discipline primitives</name>
  <files>scripts/run_phase11_paired_evidence.py, tests/test_phase11_paired_evidence.py</files>
  <read_first>
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-CONTEXT.md` decisions D-11-01, D-11-03, D-11-05, D-11-06.
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-RESEARCH.md` resolved requirements for pilot-only rejection and actual demand multiplier behavior.
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-PATTERNS.md` fail-closed gate validation pattern and payload-scope forbidden-language pattern.
    - `scripts/run_slack_binding_gates.py` for machine-readable fail-closed gate style.
  </read_first>
  <action>Implement pure Gate C primitives in `scripts/run_phase11_paired_evidence.py`: validate required binding scenarios, required strong comparator rows, completed `finite_storage_primal_dual` rows, paired seed alignment per demand multiplier, numeric applicable D-11-04 primary metrics, actual demand multiplier provenance, stress metadata, `finite_storage_state`, `objective_components`, proposed-controller `action_decomposition`, and main-profile evidence eligibility. Pilot, dry-run, spec-only, sub-3600-step, or metadata-only demand multiplier artifacts must be classified as `INCONCLUSIVE` or `FAILED` for phase-complete Gate C evidence and never `PASSED`. Classify rows into `binding_regime_dominance`, `slack_regime_recovery_or_context`, `inconclusive`, and `not_evidence` per D-11-05 using the reusable Gate C primary-metric rule from Task 2. Implement `validate_payload_scope` that allows only bounded Phase 11 language and rejects universal dominance, deployment readiness, final manuscript claims, broad max-pressure superiority outside binding regimes, and Phase 10 superiority reuse per D-11-06. Tests must cover synthetic pass, missing required scenario, missing each required comparator, unpaired seeds, missing actual demand multiplier behavior, missing stress metadata, missing finite-storage state/objective components, missing action decomposition, pilot-only rejection, slack rows classified as context only, and forbidden claim text.</action>
  <verify>
    <automated>python tests/test_phase11_paired_evidence.py</automated>
  </verify>
  <acceptance_criteria>Gate C primitives fail closed before interpreting metrics, consume the exact reusable Gate C rule, reject pilot-only and metadata-only demand multiplier artifacts as complete evidence, and never classify slack/control scenarios or Phase 10 smoke evidence as dominance evidence.</acceptance_criteria>
  <done>GATE-03 core validation, EXP-03 demand provenance, and D-11-06 claim discipline are test-covered.</done>
</task>

</tasks>

<verification>
Run `python tests/test_phase11_paired_evidence.py`. The test file must be directly executable, use synthetic rows only, and complete quickly. If any test imports Phase 10 artifacts as evidence, revise the test and helper code to treat Phase 10 only as scenario/baseline capability context per D-11-01. Confirm with a grep gate that non-comment plan/test text does not encode a single-primary Gate C rule: `grep -v '^#' scripts/run_phase11_paired_evidence.py | grep -c 'GATE_C_PRIMARY_METRICS'` must be greater than 0 and paired tests must assert `total_delay`, `spillback_count`, `blocking_count`, `unfinished_vehicle_count`, and switching-loss `switching_count` behavior.
</verification>

<success_criteria>
- `scripts/run_phase11_paired_evidence.py` exists with Phase 11 constants, profile/spec helpers, demand multiplier behavior contracts, paired-statistics helpers, a reusable D-11-04/D-11-05 Gate C primary-metric family rule, Gate C core validation primitives, and claim-scope validation.
- `tests/test_phase11_paired_evidence.py` exists and covers synthetic pass/fail/inconclusive cases for GATE-03, EXP-03, EXP-05, actual demand multiplier behavior, pilot-only rejection, and D-11-06 claim discipline.
- All locked decisions D-11-01 through D-11-06 referenced in this plan are represented in helper behavior or tests, and D-11-04 is implemented with all applicable primary metrics rather than a reduced single metric.
</success_criteria>

<must_haves>
<truths>
- Researchers can inspect the Phase 11 helper layer and see that binding-dominance evidence is restricted to the six D-11-01 stress regimes.
- Researchers can see paired seed differences for all applicable D-11-04 primary metrics with confidence intervals, effect sizes, and multiple-comparison family metadata.
- Researchers can observe tests fail closed on malformed evidence, metadata-only demand multipliers, pilot-only completion attempts, and forbidden overclaiming.
</truths>
<artifacts>
- `scripts/run_phase11_paired_evidence.py` provides Phase 11 contracts, demand multiplier contracts, paired statistics, Gate C primitives, and claim validation.
- `tests/test_phase11_paired_evidence.py` provides fast synthetic tests.
</artifacts>
<key_links>
- `tests/test_phase11_paired_evidence.py` imports and exercises `build_phase11_spec`, paired-statistics helpers, the reusable Gate C primary-metric rule, Gate C evaluator, and `validate_payload_scope`.
- `scripts/run_phase11_paired_evidence.py` imports existing row schema contracts from `scripts/run_closed_loop_sumo.py` rather than creating a second simulator loop.
</key_links>
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local JSON rows -> paired statistics | Untrusted or malformed experiment rows could be interpreted as evidence. |
| CLI/profile inputs -> spec builder | Invalid seeds, horizons, paths, controllers, or demand multipliers could corrupt the artifact. |
| Artifact text -> future claim surfaces | Overclaiming language could leak into downstream reports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | `evaluate_gate_c` row validation | mitigate | Validate scenario tags, controllers, paired seeds, demand multiplier provenance, D-11-04 metric numeric types, stress metadata, `finite_storage_state`, `objective_components`, and action decomposition before computing evidence. |
| T-11-02 | Repudiation | `paired_metric_summary` statistical report | mitigate | Store metric direction, difference definition, seed count, CI method, effect size, all-primary-metric coverage, and multiple-comparison family metadata in every statistic. |
| T-11-03 | Information Integrity | `validate_payload_scope` | mitigate | Reject universal dominance, deployment readiness, final manuscript claims, broad max-pressure superiority, and Phase 10 superiority reuse outside caveats. |
| T-11-04 | Denial of Service | profile/spec construction | mitigate | Keep this plan synthetic-only and validate seeds/horizon/demand multiplier counts before any later SUMO run. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Long-horizon paired-seed evidence restricted to binding stress regimes with statistical uncertainty | 11-01, 11-02, 11-03 | COVERED | Helper layer, runner, and Gate C split across the plan set.
REQ | GATE-03 | Gate C closed-loop dominance in predeclared binding stress regimes against strongest feasible baselines | 11-01, 11-03 | COVERED | Core validation and exact all-primary-metric rule here; standalone checker in Plan 03.
REQ | EXP-03 | 3600–7200s horizon, warmup, demand sweeps, paired seeds where feasible | 11-01, 11-02 | COVERED | Spec/demand contracts here; SUMO demand behavior and main execution in Plan 02.
REQ | EXP-05 | Paired CI/effect sizes/multiple-comparison handling | 11-01 | COVERED | Paired statistics helpers and tests over `gate_c_primary_metrics_v1`.
RESEARCH | RESOLVED-Q1 | Gate C pass rule uses all D-11-04 primary metrics with family handling | 11-01, 11-03 | COVERED | Exported constants/rule here; checker consumes same rule.
RESEARCH | RESOLVED-Q2 | Main profile must generate/run 3600s+ rows or fail/inconclusive; pilot-only cannot complete phase | 11-01, 11-02, 11-03 | COVERED | Eligibility primitives here; runner/checker enforce later.
RESEARCH | RESOLVED-Q3 | Demand multipliers must alter actual SUMO behavior | 11-01, 11-02 | COVERED | Contract here; implementation in Plan 02.
RESEARCH | — | Dedicated Phase 11 runner/artifact, not Phase 10 dominance reuse | 11-01, 11-02 | COVERED | Constants/tests enforce separation; runner writes Phase 11 artifact.
RESEARCH | — | Fail-closed validation before interpretation | 11-01, 11-03 | COVERED | Pure evaluator here; strict CLI in Plan 03.
CONTEXT | D-11-01 | Binding-only Gate C scenario split | 11-01, 11-03 | COVERED | Exact scenario list and classification.
CONTEXT | D-11-02 | Dedicated runner/artifact with pilot/main 3600/900 paired seeds | 11-01, 11-02 | COVERED | Contract here; orchestration in Plan 02.
CONTEXT | D-11-03 | Strong comparator set | 11-01, 11-03 | COVERED | Required comparator constants and tests.
CONTEXT | D-11-04 | Metric directions and paired statistical reporting | 11-01, 11-03 | COVERED | All primary metric statistics and shared rule.
CONTEXT | D-11-05 | Machine-readable fail-closed Gate C | 11-01, 11-03 | COVERED | Evaluator and strict checker.
CONTEXT | D-11-06 | Bounded claim language only | 11-01, 11-03 | COVERED | Scope validation and claim tests.
</source_audit>

<output>
Create `.planning/phases/11-long-horizon-paired-seed-evidence/11-01-SUMMARY.md` when done.
</output>
