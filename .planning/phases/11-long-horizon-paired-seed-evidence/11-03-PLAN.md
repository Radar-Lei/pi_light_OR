---
phase: 11-long-horizon-paired-seed-evidence
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - scripts/run_gate_c_paired_evidence.py
  - tests/test_phase11_paired_evidence.py
  - experiments/dual_sensitivity/phase11_gate_c_paired_evidence.json
autonomous: true
requirements:
  - GATE-03
  - EXP-05
must_haves:
  truths:
    - "User can run a strict Gate C checker against the Phase 11 artifact."
    - "Gate C passes only for main-profile predeclared binding stress regimes with paired seeds, actual demand multiplier behavior, strongest feasible comparators, and all applicable D-11-04 primary metrics satisfying the shared rule."
    - "Gate C fails closed or reports inconclusive for missing rows, unpaired seeds, pilot-only artifacts, metadata-only demand multipliers, missing stress metadata, missing finite-storage fields, missing action decomposition, missing required metrics, and forbidden claim language."
  artifacts:
    - path: scripts/run_gate_c_paired_evidence.py
      provides: "Standalone strict Gate C checker for Phase 11 paired evidence artifacts"
    - path: tests/test_phase11_paired_evidence.py
      provides: "Synthetic strict-checker tests and claim-discipline failure tests"
    - path: experiments/dual_sensitivity/phase11_gate_c_paired_evidence.json
      provides: "Generated machine-readable Gate C output artifact"
  key_links:
    - from: scripts/run_gate_c_paired_evidence.py
      to: scripts/run_phase11_paired_evidence.py
      via: "imports Gate C evaluator, reusable primary-metric rule, payload validation, constants, and paired statistics contracts"
      pattern: "from run_phase11_paired_evidence import"
    - from: scripts/run_gate_c_paired_evidence.py
      to: experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json
      via: "reads Phase 11 evidence JSON input"
      pattern: "--input"
    - from: scripts/run_gate_c_paired_evidence.py
      to: experiments/dual_sensitivity/phase11_gate_c_paired_evidence.json
      via: "writes strict Gate C JSON output"
      pattern: "--out"
---

<objective>
Create the standalone fail-closed Gate C checker that consumes the dedicated Phase 11 paired-seed evidence artifact and emits a separate machine-readable Gate C result.

Purpose: Satisfy GATE-03 with a strict checker limited to predeclared binding stress regimes and strongest feasible baselines, while preserving EXP-05 statistical requirements, the full D-11-04 primary-metric family, main-profile evidence eligibility, actual demand multiplier behavior, and D-11-06 claim discipline.

Output: `scripts/run_gate_c_paired_evidence.py`, updated tests, and generated `experiments/dual_sensitivity/phase11_gate_c_paired_evidence.json`.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-long-horizon-paired-seed-evidence/11-CONTEXT.md
@.planning/phases/11-long-horizon-paired-seed-evidence/11-RESEARCH.md
@.planning/phases/11-long-horizon-paired-seed-evidence/11-PATTERNS.md
@.planning/phases/11-long-horizon-paired-seed-evidence/11-VALIDATION.md
@.planning/phases/11-long-horizon-paired-seed-evidence/11-01-SUMMARY.md
@.planning/phases/11-long-horizon-paired-seed-evidence/11-02-SUMMARY.md
@scripts/run_phase11_paired_evidence.py
@scripts/run_slack_binding_gates.py
@tests/test_phase11_paired_evidence.py
@experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create strict Gate C checker CLI and JSON output</name>
  <files>scripts/run_gate_c_paired_evidence.py, tests/test_phase11_paired_evidence.py</files>
  <read_first>
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-CONTEXT.md` decision D-11-05.
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-RESEARCH.md` resolved Open Questions and Pattern 4 shared Gate C rule.
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-PATTERNS.md` artifact writing pattern and fail-closed gate validation pattern.
    - `scripts/run_slack_binding_gates.py` for CLI/gate output conventions.
    - `scripts/run_phase11_paired_evidence.py` from Plans 01–02.
  </read_first>
  <action>Create `scripts/run_gate_c_paired_evidence.py` with `parse_args`, `load_input_payload`, `build_gate_payload`, `write_gate_artifact`, and `main() -> None`. CLI must support `--input`, `--out`, and `--strict`, defaulting to `experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json` and `experiments/dual_sensitivity/phase11_gate_c_paired_evidence.json`. It must import Gate C constants, `GATE_C_PRIMARY_METRICS`, `GATE_C_CONDITIONAL_PRIMARY_METRICS`, `GATE_C_STATISTICAL_FAMILY`, the reusable primary-metric rule, payload validation, and evaluator helpers from `run_phase11_paired_evidence.py`; do not duplicate or redefine pass/fail/inconclusive semantics. Validate the input artifact scope, reject pilot/dry-run/spec-only artifacts as phase-complete evidence, re-evaluate Gate C from raw `scenario_results`, and write a payload with `experiment="phase11_gate_c_paired_evidence"`, `status`, `requirements_covered`, `input_artifact`, `binding_regime_dominance`, `slack_regime_recovery_or_context`, `inconclusive`, `not_evidence`, `required_binding_scenarios`, `required_gate_c_comparators`, `gate_c_primary_metrics`, `statistical_family`, demand multiplier provenance summary, and caveats. In `--strict` mode, exit nonzero when status is `FAILED` and return zero for `PASSED` or documented `INCONCLUSIVE`; do not silently skip missing evidence.</action>
  <verify>
    <automated>python tests/test_phase11_paired_evidence.py</automated>
  </verify>
  <acceptance_criteria>Standalone Gate C checker exists, reads Phase 11 artifact input, revalidates raw evidence through the shared helper, writes a separate Gate C artifact, and follows fail-closed exit behavior without duplicated semantics.</acceptance_criteria>
  <done>GATE-03 has an executable checker surface using the same rule as Plan 01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce shared all-primary-metric, strongest-comparator, and binding-only dominance rules</name>
  <files>scripts/run_gate_c_paired_evidence.py, tests/test_phase11_paired_evidence.py</files>
  <read_first>
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-CONTEXT.md` decisions D-11-01, D-11-03, D-11-04, D-11-05.
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-RESEARCH.md` resolved Gate C pass rule and common pitfalls.
    - `scripts/run_phase11_paired_evidence.py` paired statistics, primary metric constants, and evaluator helpers.
  </read_first>
  <action>Make the checker judge dominance only for the six D-11-01 binding scenarios, only against D-11-03 required comparators, and only from main-profile executed evidence with actual demand multiplier behavior. Consume the Plan 01 `gate_c_primary_metrics_v1` rule exactly: require valid paired statistics for every required binding scenario/comparator/demand-multiplier on all applicable D-11-04 primary metrics (`penalized_avg_travel_time`, `total_delay`, `spillback_count`, `blocking_count`, `unfinished_vehicle_count`, plus `switching_count` where switching-loss scenarios are evaluated). Do not demote spillback, blocking, unfinished, delay, or scenario-relevant switching to supporting diagnostics. For strict pass, require the shared rule’s overall `PASSED`; if any required comparison lacks rows, paired seeds, actual demand provenance, metadata, a valid CI/statistic, or any applicable primary metric, output `FAILED` or `INCONCLUSIVE` with reasons from the shared helper. Keep `fixed_time`, `cycle_pressure`, and `actuated_local_pressure` as context/exploratory rows unless explicitly included as feasible, but never allow weak comparator wins to satisfy required Gate C. Add tests asserting slack scenarios cannot appear in `binding_regime_dominance`, missing `max_pressure`/`capacity_aware_pressure`/`finite_storage_double_pressure` fails or is inconclusive, missing each D-11-04 primary metric fails or is inconclusive, optional weak comparator wins do not mask strong comparator failures, metadata-only demand multipliers are rejected, and pilot-only artifacts are rejected as phase-complete evidence.</action>
  <verify>
    <automated>python tests/test_phase11_paired_evidence.py</automated>
  </verify>
  <acceptance_criteria>Gate C output is limited to predeclared binding stress regimes and strongest feasible pressure/storage-aware baselines; weak/context rows, pilot-only artifacts, metadata-only demand multipliers, and single-metric wins cannot satisfy dominance.</acceptance_criteria>
  <done>GATE-03 comparator, scenario, demand, profile, and all-primary-metric scope is enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generate Gate C artifact and full Phase 11 validation command</name>
  <files>scripts/run_gate_c_paired_evidence.py, tests/test_phase11_paired_evidence.py, experiments/dual_sensitivity/phase11_gate_c_paired_evidence.json</files>
  <read_first>
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-VALIDATION.md` full suite command.
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-CONTEXT.md` D-11-05 and D-11-06.
    - `.planning/phases/11-long-horizon-paired-seed-evidence/11-RESEARCH.md` resolved requirement that pilot-only cannot complete the phase.
    - `experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json` generated by Plan 02.
  </read_first>
  <action>Run the checker against the Phase 11 main artifact to produce `experiments/dual_sensitivity/phase11_gate_c_paired_evidence.json`. The generated checker output must include the machine-readable status and reasoned sections, not prose-only conclusions. Update or add tests that exercise checker behavior on a temporary synthetic artifact and verify output JSON status, requirements coverage, section names, shared `gate_c_primary_metrics_v1` fields, demand multiplier provenance validation, profile eligibility, and forbidden-language rejection. Ensure the documented validation command works: `python tests/test_phase11_paired_evidence.py && python scripts/run_gate_c_paired_evidence.py --input experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json --out experiments/dual_sensitivity/phase11_gate_c_paired_evidence.json --strict`. If the current Phase 11 artifact is pilot-only, dry-run, metadata-only, sub-3600, or missing required rows/metrics, the checker must output `INCONCLUSIVE` or `FAILED` with explicit fail-closed reasons; it must not output `PASSED` unless all Gate C conditions and the shared all-primary-metric rule are satisfied.</action>
  <verify>
    <automated>python tests/test_phase11_paired_evidence.py && python scripts/run_gate_c_paired_evidence.py --input experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json --out experiments/dual_sensitivity/phase11_gate_c_paired_evidence.json --strict</automated>
  </verify>
  <acceptance_criteria>Gate C artifact exists, is machine-readable, reports `PASSED`, `FAILED`, or `INCONCLUSIVE` honestly, includes all D-11-04 primary metrics/family metadata, and fails closed on claim-discipline, pilot-only, demand-integrity, and evidence-integrity problems.</acceptance_criteria>
  <done>Phase 11 has a strict Gate C executable and output artifact.</done>
</task>

</tasks>

<verification>
Run `python tests/test_phase11_paired_evidence.py && python scripts/run_gate_c_paired_evidence.py --input experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json --out experiments/dual_sensitivity/phase11_gate_c_paired_evidence.json --strict`. The command may produce `INCONCLUSIVE` for a main artifact with explicit missing evidence, but must never pass when D-11-05 preconditions, D-11-04 primary metrics, main-profile eligibility, or actual demand multiplier provenance are missing.
</verification>

<success_criteria>
- `scripts/run_gate_c_paired_evidence.py` exists and validates Phase 11 artifacts fail-closed using the shared Gate C rule from `scripts/run_phase11_paired_evidence.py`.
- `experiments/dual_sensitivity/phase11_gate_c_paired_evidence.json` exists and includes binding/context/inconclusive/not-evidence sections, `gate_c_primary_metrics`, `gate_c_primary_metrics_v1` family metadata, demand provenance summary, and profile eligibility reasons.
- Synthetic tests cover Gate C pass/fail, missing strong comparators, missing each D-11-04 primary metric, missing metadata, unpaired seeds, metadata-only demand multipliers, pilot-only rejection, missing action decomposition, and forbidden claims.
- Gate C dominance is limited to the six D-11-01 binding scenarios, required strong comparators, actual demand multiplier evidence, and the full D-11-04 primary metric family.
</success_criteria>

<must_haves>
<truths>
- Researchers can run Gate C as a machine-readable checker.
- Gate C refuses malformed, unpaired, weak-comparator-only, slack-only, pilot-only, metadata-only-demand, single-metric-only, or overclaiming artifacts.
- Gate C output distinguishes binding dominance, slack context, inconclusive evidence, and not-evidence rows.
</truths>
<artifacts>
- `scripts/run_gate_c_paired_evidence.py` provides strict checker CLI.
- `experiments/dual_sensitivity/phase11_gate_c_paired_evidence.json` provides checker output.
- `tests/test_phase11_paired_evidence.py` verifies strict checker behavior.
</artifacts>
<key_links>
- Checker reads `phase11_long_horizon_paired_seed_evidence.json` and writes `phase11_gate_c_paired_evidence.json`.
- Checker imports constants/evaluator/shared primary-metric rule from `run_phase11_paired_evidence.py` to avoid duplicated scenario/comparator/statistical rules.
</key_links>
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 11 artifact -> Gate C checker | The input JSON may be malformed, stale, pilot-only, metadata-only, or overclaiming. |
| Gate C output -> downstream evidence use | Checker output could be misread as universal or deployment evidence if caveats are missing. |
| File paths -> local filesystem | User-provided input/output paths must be handled predictably. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Tampering | `load_input_payload` and Gate C re-evaluation | mitigate | Recompute evidence from raw rows, validate required fields, all D-11-04 primary metrics, demand provenance, and reject malformed JSON or missing sections. |
| T-11-11 | Repudiation | Gate C status reasons | mitigate | Emit per-scenario/comparator/demand-multiplier/metric reasons for `FAILED`/`INCONCLUSIVE` rather than only a top-level status. |
| T-11-12 | Information Integrity | binding-only dominance section | mitigate | Only D-11-01 scenarios and D-11-03 comparators can populate `binding_regime_dominance`; slack/control rows go to context/not-evidence. |
| T-11-13 | Information Integrity | profile/demand eligibility | mitigate | Reject pilot/dry-run/sub-3600 and metadata-only demand multiplier artifacts as completed Gate C evidence. |
| T-11-14 | Tampering | path handling | mitigate | Use explicit `Path` reads/writes, require JSON input, create output parent, and avoid deleting or mutating the input artifact. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Closed-loop dominance checks only in binding stress regimes | 11-03 | COVERED | Strict Gate C checker.
REQ | GATE-03 | Fail-closed Gate C with paired CIs against strongest feasible baselines | 11-03 | COVERED | Main requirement for this plan.
REQ | EXP-05 | Paired statistical reports consumed by Gate C | 11-03 | COVERED | Checker validates paired statistics and family metadata from Plan 01.
RESEARCH | RESOLVED-Q1 | Gate C pass rule uses all D-11-04 primary metrics with family handling | 11-03 | COVERED | Checker imports and consumes shared rule; no single-primary reduction.
RESEARCH | RESOLVED-Q2 | Pilot-only cannot complete phase | 11-03 | COVERED | Checker rejects pilot/dry-run/sub-3600 artifacts as complete evidence.
RESEARCH | RESOLVED-Q3 | Demand multipliers must alter actual SUMO behavior | 11-03 | COVERED | Checker validates demand provenance.
RESEARCH | — | Gate C should emit binding/context/inconclusive/not_evidence | 11-03 | COVERED | Output sections required.
RESEARCH | — | Missing rows/metadata cannot be silently skipped | 11-03 | COVERED | Strict status and reasons.
CONTEXT | D-11-01 | Binding-only evidence scope | 11-03 | COVERED | Checker limits dominance section.
CONTEXT | D-11-03 | Strong comparator set | 11-03 | COVERED | Required comparators enforced.
CONTEXT | D-11-04 | Primary metrics/statistical rule | 11-03 | COVERED | Checker uses paired stats and all applicable primary metrics.
CONTEXT | D-11-05 | Machine-readable fail-closed Gate C | 11-03 | COVERED | Standalone CLI and JSON output.
CONTEXT | D-11-06 | Claim discipline | 11-03 | COVERED | Forbidden-language validation.
</source_audit>

<output>
Create `.planning/phases/11-long-horizon-paired-seed-evidence/11-03-SUMMARY.md` when done.
</output>
