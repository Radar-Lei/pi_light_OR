---
phase: 06-claim-discipline-and-explicit-state-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/finite_storage_schema.py
  - scripts/generate_static_regime_states.py
  - tests/test_finite_storage_schema.py
  - tests/test_generate_static_regime_states.py
  - experiments/dual_sensitivity/phase6_explicit_state_schema.json
  - experiments/dual_sensitivity/phase6_state_objective_fixtures.json
autonomous: true
requirements:
  - STATE-01
  - STATE-02
must_haves:
  truths:
    - "User can inspect generated state fixtures with explicit downstream storage, residual receiving capacity, spillback/blocking, switching-loss, service-urgency, and incident/capacity-drop fields."
    - "User can inspect objective_components with delay, unfinished_vehicle_penalty, spillback_blocking_time, and switching_lost_time terms."
    - "Proxy-only regime labels are insufficient for v1.1 binding-regime evidence."
  artifacts:
    - path: "scripts/finite_storage_schema.py"
      provides: "required-field validators and builders for finite_storage_state and objective_components"
      exports: ["build_finite_storage_state", "build_objective_components", "build_objective_components_from_metrics", "validate_finite_storage_state", "validate_objective_components", "validate_state_objective_sample", "write_schema_artifact"]
    - path: "scripts/generate_static_regime_states.py"
      provides: "static fixture generator emitting explicit Phase 6 nested state/objective fields"
    - path: "tests/test_finite_storage_schema.py"
      provides: "STATE-01 and STATE-02 schema tests"
    - path: "experiments/dual_sensitivity/phase6_explicit_state_schema.json"
      provides: "machine-readable schema documentation artifact"
    - path: "experiments/dual_sensitivity/phase6_state_objective_fixtures.json"
      provides: "deterministic explicit state/objective fixtures"
  key_links:
    - from: "scripts/generate_static_regime_states.py"
      to: "scripts/finite_storage_schema.py"
      via: "make_sample builds and validates nested fields before returning samples"
      pattern: "finite_storage_state"
    - from: "scripts/generate_static_regime_states.py"
      to: "scripts/finite_storage_schema.py"
      via: "static fixtures use the same objective-components helper contract as closed-loop metrics"
      pattern: "build_objective_components_from_metrics"
    - from: "tests/test_finite_storage_schema.py"
      to: "experiments/dual_sensitivity/phase6_state_objective_fixtures.json"
      via: "generator subprocess validates emitted sample fields and objective keys"
      pattern: "objective_components"
---

<objective>
Create explicit finite-storage state and objective-component schema helpers, then wire deterministic static fixtures to emit validated Phase 6 fields.

Purpose: satisfy STATE-01 and STATE-02 by replacing proxy-only labels with explicit state/objective data needed by downstream theory, controller, and gate phases.
Output: reusable schema helper, generator integration, schema tests, and generated Phase 6 schema/fixture JSON artifacts.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/samuel/projects/pi_light_OR/CLAUDE.md
@/home/samuel/projects/pi_light_OR/.planning/PROJECT.md
@/home/samuel/projects/pi_light_OR/.planning/ROADMAP.md
@/home/samuel/projects/pi_light_OR/.planning/REQUIREMENTS.md
@/home/samuel/projects/pi_light_OR/.planning/STATE.md
@/home/samuel/projects/pi_light_OR/.planning/phases/06-claim-discipline-and-explicit-state-foundation/06-CONTEXT.md
@/home/samuel/projects/pi_light_OR/.planning/phases/06-claim-discipline-and-explicit-state-foundation/06-RESEARCH.md
@/home/samuel/projects/pi_light_OR/.planning/phases/06-claim-discipline-and-explicit-state-foundation/06-PATTERNS.md
@/home/samuel/projects/pi_light_OR/.planning/phases/06-claim-discipline-and-explicit-state-foundation/06-VALIDATION.md
@/home/samuel/projects/pi_light_OR/scripts/generate_static_regime_states.py
@/home/samuel/projects/pi_light_OR/scripts/run_static_kill_gate.py
@/home/samuel/projects/pi_light_OR/tests/test_generate_static_regime_states.py

<interfaces>
Decision trace mapping for CONTEXT.md:
- D-01 Claim Guard Scope: not implemented in this plan; covered by Plan 01.
- D-02 Explicit State Schema: create explicit finite_storage_state fields for downstream_storage, residual_receiving_capacity, spillback_blocking, switching_loss_state, service_urgency, and incident_capacity_drop; proxy-only labels remain insufficient.
- D-03 Objective Decomposition: create objective_components with stable keys delay, unfinished_vehicle_penalty, spillback_blocking_time, and switching_lost_time.
- D-04 Repository Checks and Outputs: use scripts/, JSON payloads, explicit required-field validation, and deterministic artifacts.
- D-05 Claude's Discretion: choose formulas for Phase 6 fixture fields while keeping keys stable and JSON-serializable.

Create these public contracts in scripts/finite_storage_schema.py:
- FINITE_STORAGE_STATE_FIELDS = {"downstream_storage", "residual_receiving_capacity", "spillback_blocking", "switching_loss_state", "service_urgency", "incident_capacity_drop"}
- OBJECTIVE_COMPONENT_FIELDS = {"delay", "unfinished_vehicle_penalty", "spillback_blocking_time", "switching_lost_time"}
- build_finite_storage_state(queues: dict[str, float], capacities: dict[str, float], *, current_phase: int | None = None, time_since_switch: float | None = None, incident_edge: str | None = None, capacity_drop_factor: float | None = None) -> dict[str, Any]
- build_objective_components(queues: dict[str, float], *, unfinished_vehicle_count: int = 0, spillback_blocking_count: int = 0, switching_count: int = 0, horizon: float = 1.0, switching_lost_time_per_switch: float = 2.0) -> dict[str, float]
- build_objective_components_from_metrics(metrics: dict[str, Any], *, horizon: float = 1.0, switching_lost_time_per_switch: float = 2.0) -> dict[str, float]
- validate_finite_storage_state(state: dict[str, Any], *, path: Path | None = None, sample_idx: int | None = None) -> None
- validate_objective_components(components: dict[str, Any], *, path: Path | None = None, sample_idx: int | None = None) -> None
- validate_state_objective_sample(sample: dict[str, Any], *, path: Path | None = None, sample_idx: int | None = None) -> None
- schema_artifact_payload() -> dict[str, Any]
- write_schema_artifact(path: Path) -> dict[str, Any]

Shared objective contract:
- build_objective_components_from_metrics() is the single broad helper for both static fixture dictionaries and closed-loop metric rows. Static fixtures may call build_objective_components() directly only as a thin queue-oriented adapter around build_objective_components_from_metrics(); Plan 03 closed-loop code must import the same helper so D-03 component formulas and key names cannot drift.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing explicit-state and objective schema tests</name>
  <files>tests/test_finite_storage_schema.py, tests/test_generate_static_regime_states.py</files>
  <read_first>
    - /home/samuel/projects/pi_light_OR/tests/test_generate_static_regime_states.py
    - /home/samuel/projects/pi_light_OR/tests/test_run_static_kill_gate.py
    - /home/samuel/projects/pi_light_OR/.planning/phases/06-claim-discipline-and-explicit-state-foundation/06-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: validate_finite_storage_state rejects samples missing each required state key and rejects proxy-only samples without finite_storage_state.
    - Test 2: validate_objective_components rejects missing objective keys and non-finite numeric values.
    - Test 3: generate_static_regime_states.py emits finite_storage_state and objective_components for every sample when writing a Phase 6 fixture.
    - Test 4: schema artifact payload covers STATE-01 and STATE-02 and documents stable field names.
    - Test 5: build_objective_components_from_metrics accepts metric dictionaries used by both static fixtures and closed-loop rows and returns exactly the D-03 component keys.
  </behavior>
  <action>Create tests/test_finite_storage_schema.py using existing direct import and subprocess patterns. Extend tests/test_generate_static_regime_states.py only where needed to assert every generated sample includes finite_storage_state and objective_components. The tests must encode D-02 and D-03 exactly: downstream_storage, residual_receiving_capacity, spillback_blocking, switching_loss_state, service_urgency, incident_capacity_drop, delay, unfinished_vehicle_penalty, spillback_blocking_time, and switching_lost_time are all required. Add a contract test for build_objective_components_from_metrics() so Plan 02 static fixtures and Plan 03 closed-loop metrics share one objective helper. Do not accept proxy_reason or regime names as a substitute. This task intentionally creates the RED half of the TDD cycle only; the automated verify command must pass by asserting that the selected tests fail before scripts/finite_storage_schema.py and generator integration are implemented.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && ! python3 -m pytest tests/test_finite_storage_schema.py::test_explicit_finite_storage_state_required_fields tests/test_finite_storage_schema.py::test_objective_components_required_keys tests/test_finite_storage_schema.py::test_objective_components_from_metrics_contract -q</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before scripts/finite_storage_schema.py exists or before generate_static_regime_states.py emits nested fields; the verify command passes only because it explicitly asserts this expected red state.
    - Tests use real dictionaries and generator subprocess outputs, not mocks.
    - Tests are directly executable through a main() helper.
  </acceptance_criteria>
  <done>STATE-01 and STATE-02 required fields plus the shared objective helper contract are specified by executable tests before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement finite-storage schema helper and static generator integration</name>
  <files>scripts/finite_storage_schema.py, scripts/generate_static_regime_states.py, experiments/dual_sensitivity/phase6_explicit_state_schema.json, experiments/dual_sensitivity/phase6_state_objective_fixtures.json</files>
  <read_first>
    - /home/samuel/projects/pi_light_OR/scripts/generate_static_regime_states.py
    - /home/samuel/projects/pi_light_OR/scripts/run_static_kill_gate.py
    - /home/samuel/projects/pi_light_OR/.planning/phases/06-claim-discipline-and-explicit-state-foundation/06-RESEARCH.md
    - /home/samuel/projects/pi_light_OR/.planning/phases/06-claim-discipline-and-explicit-state-foundation/06-PATTERNS.md
  </read_first>
  <action>Implement scripts/finite_storage_schema.py per D-02/D-03 with stdlib-only explicit required-field and finite-number validation. Use Path, json, math, and typing.Any; no external JSON schema package. Build finite_storage_state as a nested JSON object: downstream_storage and residual_receiving_capacity are edge->float mappings, spillback_blocking is edge->object or edge->bool containing explicit spillback/blocking indicators, switching_loss_state contains current_phase and time_since_switch, service_urgency is edge->float, and incident_capacity_drop contains active, edge, and factor. Implement build_objective_components_from_metrics(metrics, horizon, switching_lost_time_per_switch) as the canonical D-03 helper accepting metric aliases from both static fixtures and closed-loop rows: delay or total_delay, unfinished_vehicle_count or unfinished_vehicles, spillback_blocking_count or spillback_count+blocking_count, and switching_count. Implement build_objective_components() as a queue-oriented adapter that prepares a metric dictionary and delegates to build_objective_components_from_metrics(). Update generate_static_regime_states.py make_sample() to import and call these helpers for every sample, including slack, storage_binding, proxy, incident, and demand-shift regimes. Add CLI options --phase6-fixture-out and --schema-out or equivalent default writes so execution can generate experiments/dual_sensitivity/phase6_explicit_state_schema.json and experiments/dual_sensitivity/phase6_state_objective_fixtures.json without mutating historical v1.0 artifacts. Preserve existing block3 output compatibility by keeping existing top-level sample keys.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 -m pytest tests/test_finite_storage_schema.py tests/test_generate_static_regime_states.py -q && python3 scripts/generate_static_regime_states.py --target-per-regime 3 --out experiments/dual_sensitivity/phase6_state_objective_fixtures.json --schema-out experiments/dual_sensitivity/phase6_explicit_state_schema.json</automated>
  </verify>
  <acceptance_criteria>
    - scripts/finite_storage_schema.py exports all contracts listed in the context interfaces block, including build_objective_components_from_metrics().
    - generate_static_regime_states.py emits finite_storage_state and objective_components for every sample and validates each sample before returning it.
    - experiments/dual_sensitivity/phase6_explicit_state_schema.json contains requirements_covered including STATE-01 and STATE-02 and lists all required fields.
    - experiments/dual_sensitivity/phase6_state_objective_fixtures.json contains samples with explicit nested fields and does not rely on proxy-only labels for v1.1 binding evidence.
  </acceptance_criteria>
  <done>Static Phase 6 fixtures and schema artifact are generated with explicit required fields, shared objective helper contract, and objective components.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden validation error messages and artifact metadata</name>
  <files>scripts/finite_storage_schema.py, tests/test_finite_storage_schema.py, experiments/dual_sensitivity/phase6_explicit_state_schema.json, experiments/dual_sensitivity/phase6_state_objective_fixtures.json</files>
  <read_first>
    - /home/samuel/projects/pi_light_OR/scripts/finite_storage_schema.py
    - /home/samuel/projects/pi_light_OR/tests/test_finite_storage_schema.py
    - /home/samuel/projects/pi_light_OR/.planning/REQUIREMENTS.md
  </read_first>
  <action>Add negative tests and implementation details so validation errors include the missing field name, path when provided, and sample index when provided. Ensure artifacts include experiment, status, generated_by, requirements_covered, schema_version, objective_formula_metadata naming build_objective_components_from_metrics, and note that old v1.0 proxy fields are historical/insufficient for binding-regime superiority per D-02. Do not add unrelated objective terms beyond D-03 stable keys.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 -m pytest tests/test_finite_storage_schema.py tests/test_generate_static_regime_states.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Missing finite_storage_state.downstream_storage errors mention downstream_storage.
    - Missing objective_components.switching_lost_time errors mention switching_lost_time.
    - Schema and fixture artifacts are parseable JSON and include STATE-01/STATE-02 in requirements_covered.
    - Objective metadata identifies build_objective_components_from_metrics as the shared static/closed-loop component builder.
  </acceptance_criteria>
  <done>Schema validation is fail-closed, diagnostic, and artifact metadata is sufficient for downstream audits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated fixture JSON -> schema validators | Synthetic states could omit explicit finite-storage/objective fields or use non-finite values. |
| legacy proxy labels -> v1.1 evidence consumers | Proxy-only regimes could be mistaken for explicit binding-regime evidence. |
| CLI path args -> artifact output | Relative output paths could write/read unintended locations if not handled consistently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | scripts/finite_storage_schema.py | mitigate | Reject missing required keys, wrong types, and non-finite numbers before emitting or consuming samples. |
| T-06-05 | Repudiation | scripts/generate_static_regime_states.py | mitigate | Persist generated_by, requirements_covered, schema_version, and explicit proxy-insufficiency notes in artifacts. |
| T-06-06 | Information Integrity | phase6_state_objective_fixtures.json | mitigate | Include nested finite_storage_state and objective_components on every sample; tests assert no proxy-only substitute is accepted. |
| T-06-06B | Information Integrity | objective helper contract | mitigate | Use build_objective_components_from_metrics as the canonical helper for both static fixtures and closed-loop metric rows so objective terms do not drift. |
| T-06-SC | Tampering | package installs | mitigate | No package installs are planned; task actions require stdlib-only schema validation. |
</threat_model>

<verification>
Run targeted schema tests and deterministic fixture generation:
- cd /home/samuel/projects/pi_light_OR && python3 -m pytest tests/test_finite_storage_schema.py tests/test_generate_static_regime_states.py -q
- cd /home/samuel/projects/pi_light_OR && python3 scripts/generate_static_regime_states.py --target-per-regime 3 --out experiments/dual_sensitivity/phase6_state_objective_fixtures.json --schema-out experiments/dual_sensitivity/phase6_explicit_state_schema.json
</verification>

<success_criteria>
- STATE-01: generated samples include explicit finite_storage_state fields and validators reject proxy-only samples.
- STATE-02: generated samples include objective_components with all four predeclared terms.
- The schema helper remains reusable by static kill gates and closed-loop runners in later plans.
</success_criteria>

<output>
Create `/home/samuel/projects/pi_light_OR/.planning/phases/06-claim-discipline-and-explicit-state-foundation/06-02-SUMMARY.md` when done.
</output>
