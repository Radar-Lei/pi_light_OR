---
phase: 05-reproducibility-and-repository-hardening
plan: "03"
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - scripts/render_paper_artifacts.py
  - experiments/dual_sensitivity/paper_tables.csv
  - experiments/dual_sensitivity/paper_figure_data.csv
  - experiments/dual_sensitivity/paper_artifacts_manifest.json
autonomous: true
requirements:
  - REPR-04
  - REPR-05
must_haves:
  truths:
    - "Paper-facing tables and figure data can be regenerated by a script from raw experiment artifacts, not copied by hand."
    - "Generated table/figure artifacts trace each row back to a source JSON/CSV file and script."
    - "Claim-discipline metadata in generated artifacts preserves the pressure-equivalent route and known limitations."
  artifacts:
    - path: "scripts/render_paper_artifacts.py"
      provides: "Deterministic generator for paper tables, figure data, and artifact manifest"
      exports: ["main", "load_inputs", "build_tables", "build_figure_data"]
    - path: "experiments/dual_sensitivity/paper_tables.csv"
      provides: "Paper-facing tabular summaries generated from raw artifacts"
      contains: "source_artifact"
    - path: "experiments/dual_sensitivity/paper_figure_data.csv"
      provides: "Plot-ready figure data generated from raw artifacts"
      contains: "figure_id"
    - path: "experiments/dual_sensitivity/paper_artifacts_manifest.json"
      provides: "Traceability manifest for generated table/figure outputs"
      contains: "source_artifacts"
  key_links:
    - from: "scripts/render_paper_artifacts.py"
      to: "experiments/dual_sensitivity/block3_static_kill_gate.json"
      via: "static route/table extraction"
      pattern: "block3_static_kill_gate.json"
    - from: "scripts/render_paper_artifacts.py"
      to: "experiments/dual_sensitivity/block4_closed_loop_suite.json"
      via: "closed-loop aggregate/table extraction"
      pattern: "block4_closed_loop_suite.json"
    - from: "experiments/dual_sensitivity/paper_tables.csv"
      to: "experiments/dual_sensitivity/paper_artifacts_manifest.json"
      via: "manifest records generated outputs and sources"
      pattern: "paper_tables.csv"
---

<objective>
Create deterministic paper table and figure-data generation from raw experiment artifacts.

Purpose: Phase 5 is not complete until paper-facing numbers are script-generated and traceable to JSON/CSV evidence rather than manually transcribed.
Output: a renderer script plus generated CSV/JSON artifacts for paper tables, figure data, and traceability.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-reproducibility-and-repository-hardening/05-02-SUMMARY.md
@README.md
@scripts/reproduce_blocks.py
@scripts/render_static_kill_gate_report.py
@scripts/render_closed_loop_report.py
@experiments/dual_sensitivity/reproducibility_manifest.json
@experiments/dual_sensitivity/block0_dual_sanity.json
@experiments/dual_sensitivity/block2_sparse_recovery.json
@experiments/dual_sensitivity/block3_static_kill_gate.json
@experiments/dual_sensitivity/block4_closed_loop_suite.json

<interfaces>
Use these raw artifact contracts directly.

Raw inputs:
- `block0_dual_sanity.json`: sanity-check rows under `results` with dual/finite-difference/pressure special-case fields.
- `block2_sparse_recovery.json`: sparse recovery `summary`, `best_by_library`, `runs`, and CSV/rules output paths.
- `block3_static_kill_gate.json`: static route decision and per-regime metrics. Treat route decision as source of truth.
- `block4_closed_loop_suite.json`: closed-loop `scenario_results`, `aggregates`, `baseline_coverage`, `completion_gates`, and `route_decision`.
- `reproducibility_manifest.json`: artifact audit status from Plan 05-02.

New renderer CLI contract:
- `python scripts/render_paper_artifacts.py --out-dir experiments/dual_sensitivity`
- Writes `paper_tables.csv`, `paper_figure_data.csv`, and `paper_artifacts_manifest.json`.
- Optional flags: `--fail-on-overclaim` defaults true; `--static-json`, `--closed-loop-json`, `--sparse-json`, `--block0-json`, and `--manifest-json` allow explicit input paths.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement raw-artifact-to-table renderer</name>
  <files>scripts/render_paper_artifacts.py</files>
  <behavior>
    - The renderer loads Block 0, sparse recovery, static kill-gate, closed-loop suite, and reproducibility manifest JSON inputs.
    - `build_tables(...)` returns rows with `table_id`, `panel`, `metric`, `value`, `source_artifact`, and `claim_note` fields.
    - Generated table rows include at least sanity checks, sparse recovery best-by-library summaries, static route/regime summaries, and closed-loop aggregate summaries.
    - The renderer refuses missing required raw artifacts and rejects forbidden overclaim phrases.
  </behavior>
  <action>Create `scripts/render_paper_artifacts.py` using stdlib only. Implement `load_inputs()`, `build_tables()`, `build_figure_data()`, `write_csv()`, and `main()`. Do not manually hardcode reported metric values; extract them from the JSON payloads at runtime. Keep table IDs minimal and auditable, for example `T0_dual_sanity`, `T1_sparse_recovery`, `T2_static_kill_gate`, `T3_closed_loop`. Include `source_artifact` and `source_key` columns for each row. For static and closed-loop rows, include `route_decision` and `claim_note` fields stating pressure-equivalent/generalized-pressure framing. For sparse recovery rows, include selected atoms, complexity, regret, agreement, and source rules path when present. For closed-loop aggregates, include network, scenario, controller, n_seeds, avg travel time mean/CI, completion rate, queue metrics, spillback/blocking, switching, runtime, and source JSON path. Do not introduce plotting libraries; produce plot-ready CSV data only.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 -m py_compile scripts/render_paper_artifacts.py && python3 -c 'import sys; sys.path.insert(0,"scripts"); import render_paper_artifacts as r; payload=r.load_inputs(r.default_args_for_tests() if hasattr(r,"default_args_for_tests") else None) if False else None; print("renderer imports ok")'</automated>
  </verify>
  <done>`scripts/render_paper_artifacts.py` imports cleanly and contains deterministic raw-artifact extraction functions for table and figure-data outputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate paper tables, figure data, and traceability manifest</name>
  <files>scripts/render_paper_artifacts.py, experiments/dual_sensitivity/paper_tables.csv, experiments/dual_sensitivity/paper_figure_data.csv, experiments/dual_sensitivity/paper_artifacts_manifest.json</files>
  <behavior>
    - Running the renderer writes table CSV, figure-data CSV, and manifest JSON under `experiments/dual_sensitivity/`.
    - `paper_tables.csv` contains rows from at least Block 0, sparse recovery, static kill-gate, and closed-loop suite sources.
    - `paper_figure_data.csv` contains plot-ready rows for at least static dual-vs-pressure metrics and closed-loop aggregate metrics.
    - `paper_artifacts_manifest.json` records source artifacts, generated artifacts, row counts, route decision, claim discipline, and requirements covered.
  </behavior>
  <action>Run `scripts/render_paper_artifacts.py --out-dir experiments/dual_sensitivity` to generate the Phase 5 paper artifacts. The script must write a manifest with top-level fields: `experiment: paper_artifacts`, `status`, `generated_by`, `source_artifacts`, `generated_artifacts`, `row_counts`, `route_decision`, `claim_discipline`, and `requirements_covered`. The manifest must preserve `route_decision=pressure-equivalent`, include `REPR-04` and `REPR-05`, and record that table/figure outputs are generated from raw artifacts. If input schemas differ slightly, adapt extraction defensively while keeping missing core inputs as hard failures. Do not edit source experiment JSON values by hand.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 scripts/render_paper_artifacts.py --out-dir experiments/dual_sensitivity && python3 -c 'import csv,json; tables=list(csv.DictReader(open("experiments/dual_sensitivity/paper_tables.csv"))); figs=list(csv.DictReader(open("experiments/dual_sensitivity/paper_figure_data.csv"))); manifest=json.load(open("experiments/dual_sensitivity/paper_artifacts_manifest.json")); assert tables; assert figs; assert manifest["experiment"]=="paper_artifacts"; assert manifest["status"]=="PASSED"; assert manifest["route_decision"]=="pressure-equivalent"; assert {"REPR-04","REPR-05"} <= set(manifest["requirements_covered"]); table_ids={r["table_id"] for r in tables}; assert {"T0_dual_sanity","T1_sparse_recovery","T2_static_kill_gate","T3_closed_loop"} <= table_ids; assert all(r.get("source_artifact") for r in tables); assert all(r.get("source_artifact") for r in figs); assert manifest["claim_discipline"]["forbidden_phrases_present"] == []; print("paper artifacts ok")'</automated>
  </verify>
  <done>Paper tables, figure-data CSV, and manifest are generated from raw artifacts with traceability and claim discipline checks passing.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run reproducibility audit including paper artifacts</name>
  <files>experiments/dual_sensitivity/reproducibility_manifest.json, experiments/dual_sensitivity/paper_artifacts_manifest.json</files>
  <action>After paper artifacts exist, rerun `scripts/reproduce_blocks.py --audit --manifest-out experiments/dual_sensitivity/reproducibility_manifest.json` so the Phase 5 manifest no longer marks the `paper_artifacts` block as missing. Ensure the updated manifest records `paper_tables.csv`, `paper_figure_data.csv`, and `paper_artifacts_manifest.json` as existing and parseable. Do not rerun long SUMO experiments in this task.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 scripts/reproduce_blocks.py --audit --manifest-out experiments/dual_sensitivity/reproducibility_manifest.json && python3 -c 'import json; p=json.load(open("experiments/dual_sensitivity/reproducibility_manifest.json")); checks={c["path"]:c for c in p["artifact_checks"]}; required=["experiments/dual_sensitivity/paper_tables.csv","experiments/dual_sensitivity/paper_figure_data.csv","experiments/dual_sensitivity/paper_artifacts_manifest.json"]; missing=[x for x in required if x not in checks or not checks[x]["exists"] or checks[x]["parse_status"]!="ok"]; assert not missing, missing; assert p["status"]=="PASSED"; print("final reproducibility audit ok")'</automated>
  </verify>
  <done>The final reproducibility manifest includes generated paper tables/figure data and all Phase 5 artifact audits pass.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 5 | Tables/figures can be rerun and audited from raw artifacts | 05-03 | COVERED | This plan creates the generator and traceability outputs.
REQ | REPR-01 | Root README entrypoint | 05-01 | COVERED | README documents the generator command.
REQ | REPR-02 | CPU/SUMO environment spec | 05-01 | COVERED | No new dependency is added here.
REQ | REPR-03 | Scripts organized by block | 05-02 | COVERED | This plan uses the block audit manifest.
REQ | REPR-04 | Structured JSON/CSV sufficient to regenerate tables/figures | 05-02,05-03 | COVERED | This plan consumes structured raw artifacts and generates CSV outputs.
REQ | REPR-05 | Paper tables and figures generated by scripts from raw artifacts | 05-03 | COVERED | `render_paper_artifacts.py` is the table/figure-data generator.
RESEARCH | None | External research skipped | N/A | COVERED | Uses raw artifacts only.
CONTEXT | Claim discipline | Pressure-equivalent route and limitations must remain visible | 05-03 | COVERED | Generated rows and manifest carry route/claim notes and forbidden phrase checks.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw JSON/CSV artifacts -> paper tables | Experimental outputs become paper-facing tables and figure data. |
| Renderer CLI flags -> file reads/writes | User-selected paths control source and generated artifact locations. |
| Generated CSV/JSON -> manuscript | Paper-facing values must remain traceable to raw sources and not manual edits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | `scripts/render_paper_artifacts.py` source extraction | mitigate | Extract values from parsed raw artifacts at runtime; include source artifact and source key per row. |
| T-05-10 | Repudiation | `paper_tables.csv` and `paper_figure_data.csv` | mitigate | Manifest records source artifacts, generated artifacts, row counts, route decision, and requirements covered. |
| T-05-11 | Information Disclosure | Paper artifact manifest | accept | Outputs contain local experiment metrics only and no secrets or external service data. |
| T-05-12 | Denial of Service | Renderer over large artifacts | mitigate | Use streaming `csv.DictWriter` and compact extraction from known JSON fields; no plotting/rendering dependencies. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install task is planned. |
</threat_model>

<verification>
Plan 05-03 is complete when the renderer compiles, generated paper table/figure artifacts exist, manifests pass assertions, and a final reproducibility audit confirms paper artifacts are parseable and traceable.
</verification>

<success_criteria>
- Paper-facing CSV tables and figure-data files are generated from raw JSON/CSV artifacts by a script.
- Every generated row includes source traceability.
- Final manifests pass claim-discipline and parseability checks for REPR-04 and REPR-05.
</success_criteria>

<output>
Create `.planning/phases/05-reproducibility-and-repository-hardening/05-03-SUMMARY.md` when done.
</output>
