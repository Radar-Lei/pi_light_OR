---
phase: 05-reproducibility-and-repository-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - environment.yml
autonomous: true
requirements:
  - REPR-01
  - REPR-02
must_haves:
  truths:
    - "A fresh reader can identify the research question, contribution claim, current evidence route, reproduction commands, known limitations, and next experiments from the repository root."
    - "The setup path is explicitly CPU/SUMO/optimization oriented and does not require GPU, CUDA, vllm, neural training, or external services."
    - "The README states pressure-equivalent generalized-pressure symbolic recovery rather than universal dual-over-pressure dominance."
  artifacts:
    - path: "README.md"
      provides: "Repository entrypoint with research framing, status, reproduction commands, limitations, and next experiments"
      contains: "pressure-equivalent"
    - path: "environment.yml"
      provides: "CPU/SUMO/SciPy optimization environment specification"
      contains: "sumo"
  key_links:
    - from: "README.md"
      to: "environment.yml"
      via: "setup instructions"
      pattern: "environment.yml"
    - from: "README.md"
      to: "scripts/reproduce_blocks.py"
      via: "reproduction command table"
      pattern: "reproduce_blocks.py"
    - from: "README.md"
      to: "scripts/render_paper_artifacts.py"
      via: "table/figure generation command"
      pattern: "render_paper_artifacts.py"
---

<objective>
Create the repository-level reproducibility entrypoint and CPU environment contract for Phase 5.

Purpose: Phase 5 needs a fresh-reader path before the repository can be treated as an auditable paper artifact.
Output: root README with claim discipline and an environment spec for CPU/SUMO/SciPy-style reproduction.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@.planning/phases/03-static-pressure-failure-kill-gate/03-03-SUMMARY.md
@.planning/phases/04-closed-loop-sumo-evaluation/04-03-SUMMARY.md

<interfaces>
Use these current artifact contracts directly.

Existing evidence route:
- Phase 3 route decision is `pressure-equivalent`; state this as generalized-pressure symbolic recovery and avoid dual-superiority claims.
- Phase 4 closed-loop report records SUMO evidence relative to short local horizons, seed gates, and first-class pressure/capacity-aware baselines.

Existing reproduction-related entrypoints to reference:
- `python scripts/run_dual_sanity.py --out experiments/dual_sensitivity/block0_dual_sanity.json`
- `python scripts/run_sparse_recovery.py --states experiments/dual_sensitivity/targeted_bottleneck_states.json --out experiments/dual_sensitivity/block2_sparse_recovery.json`
- `python scripts/run_static_kill_gate.py --states experiments/dual_sensitivity/block3_regime_states.json --out experiments/dual_sensitivity/block3_static_kill_gate.json --csv-out experiments/dual_sensitivity/block3_static_kill_gate.csv --rules-out experiments/dual_sensitivity/block3_static_kill_gate_rules.txt --report-out experiments/dual_sensitivity/block3_static_kill_gate_report.md`
- `python scripts/run_closed_loop_suite.py --profile smoke --out experiments/dual_sensitivity/block4_closed_loop_suite.json`
- Phase 5 will add `scripts/reproduce_blocks.py` and `scripts/render_paper_artifacts.py`; README may document them as the canonical reproduction commands for this phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CPU/SUMO optimization environment specification</name>
  <files>environment.yml</files>
  <action>Create `environment.yml` for a local CPU-oriented workflow. Include Python, NumPy, SciPy/HiGHS, SUMO/TraCI/sumolib, and standard tooling needed by the existing scripts. State in comments or dependency grouping that GPU, CUDA, vllm, MARL training, cloud services, and committed secrets are not required for the Phase 5 reproduction path. Keep AMPL optional if mentioned, with no license ID, activation command, or secret material. Do not add package-install commands to the plan output; the file is the environment contract.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 -c 'from pathlib import Path; text=Path("environment.yml").read_text(); low=text.lower(); assert "sumo" in low; assert "scipy" in low; assert "numpy" in low; assert "vllm" not in low; assert "cuda" not in low; assert "gpu" not in low or "not" in low; assert "626899fe" not in text; print("environment spec ok")'</automated>
  </verify>
  <done>`environment.yml` describes a reproducible CPU/SUMO/optimization setup and contains no GPU dependency, vllm dependency, or secret/license material.</done>
</task>

<task type="auto">
  <name>Task 2: Write the root README as the reproduction entrypoint</name>
  <files>README.md</files>
  <action>Create `README.md` at the repository root. It must include sections for: research question, contribution claim, current status, repository layout, environment setup using `environment.yml`, reproduction commands by block, artifact paths, known limitations, and next experiments. The reproduction command table must cover Block 0 dual sanity, static recovery/sparse recovery, static kill-gate, closed-loop SUMO evidence, artifact audit, and paper table/figure generation. Claim discipline is mandatory: phrase the current result as pressure-equivalent generalized-pressure symbolic recovery; state that max-pressure/backpressure and capacity-aware pressure are first-class baselines; state that outputs are simulator/network/horizon/seed relative. Do not claim universal dominance, deployable superiority, or that static evidence proves closed-loop performance.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 -c 'from pathlib import Path; text=Path("README.md").read_text(); low=text.lower(); required=["research question","contribution claim","current status","reproduction commands","known limitations","next experiments","environment.yml","block 0","static recovery","closed-loop","render_paper_artifacts.py","pressure-equivalent"]; missing=[r for r in required if r not in low]; assert not missing, missing; forbidden=["dual universally beats pressure","max-pressure strawman","proves superiority","deployable superiority","static evidence proves closed-loop"]; hit=[p for p in forbidden if p in low]; assert not hit, hit; print("README reproducibility entrypoint ok")'</automated>
  </verify>
  <done>Root README gives a fresh reader the project claim, status, setup path, exact block-level reproduction commands, limitations, and next experiments without overclaiming.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 5 | Repository becomes a paper artifact whose key checks, analyses, experiments, and tables/figures can be rerun and audited | 05-01,05-02,05-03 | COVERED | This plan covers the root entrypoint and environment contract.
REQ | REPR-01 | Root README states research question, contribution claim, current status, reproduction commands, known limitations, and next experiments | 05-01 | COVERED | Task 2 creates the README.
REQ | REPR-02 | Environment specification recreates CPU/SUMO/optimization workflow without GPU dependence | 05-01 | COVERED | Task 1 creates `environment.yml`.
REQ | REPR-03 | Experiment scripts organized or named by block | 05-02 | COVERED | README points to the Phase 5 block runner created in Plan 05-02.
REQ | REPR-04 | Reported experiments emit structured JSON/CSV sufficient for table/figure regeneration | 05-02,05-03 | COVERED | Plan 05-02 audits and fills structured outputs; Plan 05-03 consumes them.
REQ | REPR-05 | Paper tables and figures generated from raw artifacts by scripts | 05-03 | COVERED | README documents the generator command; Plan 05-03 creates it.
RESEARCH | None | User explicitly requested skipping external research; no Phase 5 RESEARCH/DISCOVERY artifact exists | N/A | COVERED | Planning uses current repo state and prior artifacts only.
CONTEXT | State decisions | Maintain Phase 3/4 pressure-equivalent claim discipline and CPU/SUMO workflow | 05-01,05-02,05-03 | COVERED | README and later scripts guard against overclaim language.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fresh reader -> README commands | A reader may copy commands directly; commands must be explicit and local. |
| Environment file -> local machine | Dependency spec influences local installs and must not imply GPU/secrets. |
| Prior experiment artifacts -> repository claims | README claim language must not exceed Phase 3/4 evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | README project claim | mitigate | State the current pressure-equivalent route and avoid language implying universal dual superiority. |
| T-05-02 | Information Disclosure | `environment.yml` | mitigate | Exclude AMPL license IDs, local paths containing secrets, API keys, and `.env` references as required setup. |
| T-05-03 | Repudiation | README reproduction commands | mitigate | List command-to-artifact paths so outputs can be traced by file path. |
| T-05-04 | Denial of Service | README full experiment commands | mitigate | Mark smoke/audit commands separately from longer closed-loop reruns, while keeping exact long-run commands visible. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install task is planned; environment spec only records dependencies. |
</threat_model>

<verification>
Plan 05-01 is complete when `environment.yml` and `README.md` exist, automated text checks pass, the README points to all Phase 5 reproduction blocks, and forbidden overclaim phrases are absent.
</verification>

<success_criteria>
- Root README is a usable repository entrypoint for a fresh reviewer.
- Environment spec describes CPU/SUMO/SciPy reproduction without GPU dependence or secrets.
- Claim discipline matches Phase 3/4: pressure-equivalent generalized-pressure symbolic recovery, not universal dominance.
</success_criteria>

<output>
Create `.planning/phases/05-reproducibility-and-repository-hardening/05-01-SUMMARY.md` when done.
</output>
