---
phase: 02-full-sparse-symbolic-recovery
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - scripts/run_sparse_recovery.py
autonomous: true
requirements:
  - RECV-01
  - RECV-02
  - RECV-03
user_setup: []
must_haves:
  truths:
    - "The recovery MILP can solve K-atom policies for budgets greater than one."
    - "The primary optimization objective is empirical oracle regret or value gap, not action agreement."
    - "Program size, neighbor use, genuine dual-price use, and placebo use can be controlled by hard budgets and reported soft penalties."
  artifacts:
    - path: "scripts/run_sparse_recovery.py"
      provides: "K-atom sparse recovery and budgeted MILP constraints"
      contains: "max_neighbor_atoms"
    - path: "scripts/run_sparse_recovery.py"
      provides: "Regret-first objective metadata and realized regret outputs"
      contains: "objective_mode"
  key_links:
    - from: "scripts/run_sparse_recovery.py"
      to: "scripts/run_sparse_recovery.py::ATOM_REGISTRY"
      via: "metadata-driven category indices for z-variable budget constraints"
      pattern: "requires_neighbor"
    - from: "scripts/run_sparse_recovery.py"
      to: "experiments/dual_sensitivity/block2_sparse_recovery.json"
      via: "quick validation command writes run metrics"
      pattern: "realized_mean_regret"
---

<objective>
Implement the K-atom regret-first sparse recovery solve with explicit total, neighbor, dual, and placebo budget/penalty controls.

Purpose: Satisfy RECV-01, RECV-02, and the enforcement side of RECV-03 by turning the Phase 2 metadata registry into actual MILP objective terms, constraints, and run metrics.
Output: `scripts/run_sparse_recovery.py` supports `--budgets`/`--max-atoms`, neighbor/dual/placebo hard budgets, soft penalties, configurable solver time limit, and regret-first result fields.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-full-sparse-symbolic-recovery/02-CONTEXT.md
@.planning/phases/02-full-sparse-symbolic-recovery/02-RESEARCH.md
@.planning/phases/02-full-sparse-symbolic-recovery/02-PATTERNS.md
@.planning/phases/02-full-sparse-symbolic-recovery/02-VALIDATION.md
@scripts/run_sparse_recovery.py
@.planning/phases/02-full-sparse-symbolic-recovery/02-01-SUMMARY.md

<interfaces>
Plan 01 creates or hardens the atom registry and library/family filtering. Use that registry for all category counts and constraints.
Existing solver shape to preserve:
- `solve_library()` builds feature matrices, optimization variables, linear constraints, and calls `scipy.optimize.milp`.
- Current objective coefficients on action-choice variables already represent `best oracle value - chosen oracle value`; keep this as the primary objective term.
- Existing `--budgets` remains a sweep over total atom budgets; add `--max-atoms` as a clearer single-run alias without breaking `--budgets`.
- Existing failed MILP behavior returns structured failed run payloads instead of crashing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add K/budget CLI controls and metadata-driven MILP constraints</name>
  <files>scripts/run_sparse_recovery.py</files>
  <behavior>
    - Running with `--budgets 1 2 3` attempts at least one solve with a total atom budget greater than 1.
    - Running with `--max-atoms 3` is accepted as a single-run alias for K=3 when `--budgets` is not supplied or is overridden by explicit design in the script.
    - `--max-neighbor-atoms`, `--max-dual-atoms`, and `--max-placebo-atoms` add hard linear constraints over selected atom variables using registry metadata.
    - Invalid negative budgets or penalties fail before invoking `milp()`.
  </behavior>
  <read_first>
    @scripts/run_sparse_recovery.py
    @.planning/phases/02-full-sparse-symbolic-recovery/02-01-SUMMARY.md
    @.planning/phases/02-full-sparse-symbolic-recovery/02-PATTERNS.md
    @.planning/phases/02-full-sparse-symbolic-recovery/02-VALIDATION.md
  </read_first>
  <action>Extend `main()` and `solve_library()` in `scripts/run_sparse_recovery.py` so K-atom recovery is explicit. Preserve `--budgets` for compatibility and add `--max-atoms` as a clear single-run alias. Add `--max-neighbor-atoms`, `--max-dual-atoms`, `--max-placebo-atoms`, and `--time-limit-sec`. In `solve_library()`, derive metadata category indices from the atom registry and add linear constraints over selected atom variables `z_j` for neighbor, genuine dual, and placebo budgets after the total atom budget constraint. Defaults must not make existing libraries infeasible: if a category budget is omitted, use no tighter cap than K. Validate nonnegative integer budgets and nonnegative numeric penalties before solving.</action>
  <verify>
    <automated>python3 -m py_compile scripts/run_sparse_recovery.py && python3 scripts/run_sparse_recovery.py --states experiments/dual_sensitivity/targeted_bottleneck_states.json --budgets 1 2 --max-neighbor-atoms 2 --max-dual-atoms 1 --max-placebo-atoms 1 --time-limit-sec 20 --out /tmp/phase2_budget_check.json</automated>
  </verify>
  <acceptance_criteria>
    - The script accepts `--budgets 1 2` and solves or returns structured run statuses for both K values.
    - The script accepts `--max-neighbor-atoms`, `--max-dual-atoms`, `--max-placebo-atoms`, and `--time-limit-sec`.
    - Budget constraints are enforced in the MILP using registry metadata, not atom-name string heuristics.
    - Negative budgets or penalties fail fast with a clear error.
  </acceptance_criteria>
  <done>K-atom and category-budget controls are wired through CLI validation and MILP constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve regret-first objective and expose penalty breakdown</name>
  <files>scripts/run_sparse_recovery.py</files>
  <behavior>
    - Top-level output records `objective_mode` as `oracle_regret` or `value_gap` with `oracle_regret` as default.
    - Action agreement remains a diagnostic metric and is not used as an objective coefficient.
    - Each successful run reports `objective_value_with_penalties`, `realized_total_regret`, `realized_mean_regret`, `max_regret`, and `penalty_breakdown`.
    - Complexity, neighbor, dual, and placebo penalties are recorded separately from realized regret.
  </behavior>
  <read_first>
    @scripts/run_sparse_recovery.py
    @.planning/phases/02-full-sparse-symbolic-recovery/02-CONTEXT.md
    @.planning/phases/02-full-sparse-symbolic-recovery/02-RESEARCH.md
  </read_first>
  <action>Add or clarify `--objective` with default `oracle_regret` and optional synonym `value_gap`; reject imitation/action-agreement objective modes. Keep the existing regret/value-gap action-choice objective as the primary term. Add soft selected-atom penalties `--complexity-penalty`, `--neighbor-penalty`, `--dual-penalty`, and `--placebo-penalty` on z variables using metadata category membership. Ensure successful run payloads include `max_atoms`, `max_neighbor_atoms`, `max_dual_atoms`, `max_placebo_atoms`, `objective_value_with_penalties`, `realized_total_regret`, `realized_mean_regret`, `max_regret`, `action_agreement`, and `penalty_breakdown`. Keep action agreement secondary; do not select best runs by agreement and do not claim dual-vs-pressure empirical superiority in Phase 2.</action>
  <verify>
    <automated>python3 scripts/run_sparse_recovery.py --states experiments/dual_sensitivity/targeted_bottleneck_states.json --budgets 1 2 3 --objective oracle_regret --complexity-penalty 0.0001 --neighbor-penalty 0.0 --dual-penalty 0.0 --placebo-penalty 0.0 --out /tmp/phase2_regret_check.json && python3 - <<'PY'
import json
p='/tmp/phase2_regret_check.json'
data=json.load(open(p))
assert data.get('objective_mode') in {'oracle_regret','value_gap'}
runs=data.get('runs', [])
assert any(r.get('max_atoms', r.get('budget', 0)) > 1 for r in runs)
solved=[r for r in runs if r.get('status') == 'SOLVED']
assert solved, 'no solved run'
for r in solved:
    for key in ['objective_value_with_penalties','realized_total_regret','realized_mean_regret','max_regret','action_agreement','penalty_breakdown']:
        assert key in r, key
print('ok')
PY</automated>
  </verify>
  <acceptance_criteria>
    - The default objective is regret/value-gap, not imitation accuracy.
    - Run payloads separate realized regret from penalty-adjusted objective value.
    - `penalty_breakdown` exposes total, neighbor, dual, and placebo penalty contributions or zero values.
    - At least one K > 1 run is attempted and reported by the validation command.
  </acceptance_criteria>
  <done>Recovery solves remain regret-first while exposing all complexity and atom-category tradeoff controls.</done>
</task>

</tasks>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Recover K-atom symbolic policies trading off regret, program size, neighbor use, and dual dependence | Covered by K/budget controls, regret objective, and penalty/budget reporting. |
| REQ | RECV-01 K-atom sparse symbolic recovery beyond one-atom pilots | Covered by Task 1 K controls and Task 2 K > 1 validation. |
| REQ | RECV-02 oracle regret or value-gap objective, not imitation alone | Covered by Task 2 objective and output gates. |
| REQ | RECV-03 explicit penalties or constraints for size, neighbor count, dual-price count | Covered by Task 1 hard constraints and Task 2 soft penalties. |
| RESEARCH | Keep realized regret separately from penalty objective | Covered by Task 2 payload requirements. |
| CONTEXT | Solver objective must expose complexity, neighbor-use, and dual-price penalties or budgets | Covered by both tasks. |
| CONTEXT | Preserve action agreement as secondary diagnostic output | Covered by Task 2. |
| CONTEXT | Phase 3 empirical dual-vs-pressure claim route deferred | Covered by Task 2 instruction not to claim superiority. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI budgets/penalties → MILP formulation | User-provided numeric controls alter optimization feasibility and objective coefficients. |
| Metadata registry → MILP constraints | Registry category flags determine which selected atom variables count against neighbor/dual/placebo budgets. |
| MILP result → scientific metrics | Solver objective values and realized regret become paper-facing evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Budget constraints | mitigate | Validate nonnegative budgets and add explicit linear constraints over selected atom variables. |
| T-02-05 | Repudiation | Objective semantics | mitigate | Record `objective_mode`, `objective_value_with_penalties`, realized regret metrics, and penalty breakdown in every run. |
| T-02-06 | Denial of Service | MILP solve time | mitigate | Use configurable `--time-limit-sec` and preserve structured failed-run payloads. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install is planned; Phase 2 reuses existing Python/NumPy/SciPy stack. |
</threat_model>

<verification>
Overall plan checks:
- `python3 -m py_compile scripts/run_sparse_recovery.py`
- `python3 scripts/run_sparse_recovery.py --states experiments/dual_sensitivity/targeted_bottleneck_states.json --budgets 1 2 3 --objective oracle_regret --out /tmp/phase2_regret_check.json`
- Inspect `/tmp/phase2_regret_check.json` for K > 1 attempted runs, regret-first fields, budget fields, and penalty breakdown.
</verification>

<success_criteria>
- The solver accepts and reports K > 1 recovery runs.
- Regret/value gap remains the primary objective and action agreement remains diagnostic.
- Total, neighbor, dual, and placebo budgets or penalties are enforced and reported.
- Outputs remain finite-dictionary and sample-relative with no Phase 3 empirical route decision.
</success_criteria>

<output>
Create `.planning/phases/02-full-sparse-symbolic-recovery/02-02-SUMMARY.md` when done.
</output>
