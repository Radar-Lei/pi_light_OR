---
phase: 02-full-sparse-symbolic-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run_sparse_recovery.py
autonomous: true
requirements:
  - RECV-03
  - RECV-04
user_setup: []
must_haves:
  truths:
    - "The recovery script exposes an explicit atom metadata registry instead of string-only atom lists."
    - "Required atom families are available and distinguish local, capacity, raw-neighbor, pressure, genuine-dual, and placebo-dual atoms."
    - "Executors and downstream phases can select libraries or atom families without inferring semantics from atom names."
  artifacts:
    - path: "scripts/run_sparse_recovery.py"
      provides: "Atom metadata registry and named library views"
      contains: "ATOM_REGISTRY"
    - path: "scripts/run_sparse_recovery.py"
      provides: "Full Phase 2 atom-family coverage"
      contains: "full_symbolic"
  key_links:
    - from: "scripts/run_sparse_recovery.py"
      to: "scripts/run_dual_sanity.py"
      via: "summarize_scenario remains the sole oracle/dual/value source"
      pattern: "summarize_scenario"
    - from: "scripts/run_sparse_recovery.py"
      to: ".planning/phases/02-full-sparse-symbolic-recovery/02-RESEARCH.md"
      via: "metadata fields match locked Phase 2 research schema"
      pattern: "family"
---

<objective>
Create the atom metadata and library-selection foundation for full sparse symbolic recovery.

Purpose: Make RECV-03 and RECV-04 auditable by replacing implicit atom semantics with explicit metadata while preserving the existing recovery script as the only Phase 2 implementation target.
Output: `scripts/run_sparse_recovery.py` has an atom registry, named library views including `full_symbolic`, required atom families, and CLI/schema validation hooks that downstream plans can use.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-full-sparse-symbolic-recovery/02-CONTEXT.md
@.planning/phases/02-full-sparse-symbolic-recovery/02-RESEARCH.md
@.planning/phases/02-full-sparse-symbolic-recovery/02-PATTERNS.md
@.planning/phases/02-full-sparse-symbolic-recovery/02-VALIDATION.md
@scripts/run_sparse_recovery.py
@scripts/run_dual_sanity.py
@scripts/run_sumo_sampled_recovery.py

<interfaces>
Existing contracts to preserve:
- `run_sparse_recovery.py` currently owns `LIBRARIES`, `load_examples()`, `build_example()`, `normalize_feature_matrices()`, `solve_library()`, and `main()`.
- `build_example()` already computes feature keys for upstream queues, downstream queues/slack/fullness, random price, dual sensitivity, and pressure/backpressure from `summarize_scenario()` output.
- `scenario_from_sample()` from `run_sumo_sampled_recovery.py` remains the scenario conversion path; do not duplicate SUMO topology conversion in this plan.
- `summarize_scenario()` from `run_dual_sanity.py` remains the sole oracle/value/dual source; do not implement a second LP or finite-difference path.
- Phase 3 owns empirical dual-vs-pressure claim routing; this plan only exposes metadata needed for recovery artifacts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add explicit atom metadata registry</name>
  <files>scripts/run_sparse_recovery.py</files>
  <read_first>
    @scripts/run_sparse_recovery.py
    @.planning/phases/02-full-sparse-symbolic-recovery/02-CONTEXT.md
    @.planning/phases/02-full-sparse-symbolic-recovery/02-RESEARCH.md
    @.planning/phases/02-full-sparse-symbolic-recovery/02-PATTERNS.md
  </read_first>
  <action>In `scripts/run_sparse_recovery.py`, add an explicit atom metadata registry near the existing `LIBRARIES` region. The registry must cover every feature key used by `build_example()` and must provide `name`, `family`, `requires_neighbor`, `uses_dual`, `is_placebo`, `expression`, and `description` for each atom per Phase 2 context decisions. Required family coverage: local queue/pressure atoms, downstream capacity/slack atoms, raw neighbor queue atoms, pressure/backpressure atoms, genuine dual sensitivity or price-imbalance atoms, and random/permuted dual placebo atoms. Preserve existing atom names where possible for compatibility. Do not infer family membership from string prefixes; all downstream counting must use registry metadata. Do not add corridor/service atoms because Phase 2 context excludes them unless explicit primal corridor/service constraints exist.</action>
  <verify>
    <automated>python3 -m py_compile scripts/run_sparse_recovery.py && grep -q "ATOM_REGISTRY" scripts/run_sparse_recovery.py && grep -q "requires_neighbor" scripts/run_sparse_recovery.py && grep -q "uses_dual" scripts/run_sparse_recovery.py && grep -q "is_placebo" scripts/run_sparse_recovery.py</automated>
  </verify>
  <acceptance_criteria>
    - `ATOM_REGISTRY` or equivalently named registry exists in `scripts/run_sparse_recovery.py`.
    - Every atom in named libraries has metadata with the required fields.
    - Genuine dual atoms and placebo dual atoms are separate by metadata, not name matching.
    - Corridor/service atoms are absent unless already backed by explicit primal quantities in the existing code.
  </acceptance_criteria>
  <done>Atom semantics are explicit and sufficient to audit RECV-03 and RECV-04 family membership.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Preserve named libraries and add family selection interface</name>
  <files>scripts/run_sparse_recovery.py</files>
  <read_first>
    @scripts/run_sparse_recovery.py
    @.planning/phases/02-full-sparse-symbolic-recovery/02-RESEARCH.md
    @.planning/phases/02-full-sparse-symbolic-recovery/02-VALIDATION.md
  </read_first>
  <action>Refactor the existing `LIBRARIES` constant into named views over atom names while preserving current library names: `local_only`, `raw_neighbor`, `all_neighbor`, `random_price`, `dual_sensitivity`, `dual_plus_raw`, and `pressure_backpressure`. Add a `full_symbolic` named library containing all allowed Phase 2 atom families. Add helper validation so unknown atom names, unknown libraries, and requested families outside the registry fail before solving. Add or reserve CLI arguments `--libraries` and `--atom-families` in `main()` so later plans can run named libraries and family-filtered recovery without changing this interface. This implements Phase 2 context decisions on atom library and tradeoffs while preserving action agreement as an output concern for later plans.</action>
  <verify>
    <automated>python3 -m py_compile scripts/run_sparse_recovery.py && grep -q "full_symbolic" scripts/run_sparse_recovery.py && grep -q -- "--libraries" scripts/run_sparse_recovery.py && grep -q -- "--atom-families" scripts/run_sparse_recovery.py</automated>
  </verify>
  <acceptance_criteria>
    - Existing library names remain accepted.
    - `full_symbolic` includes local, capacity, raw_neighbor, pressure, dual, and placebo families.
    - CLI/library validation fails fast on unknown libraries or atom families.
    - No new recovery pipeline or external dependency is introduced.
  </acceptance_criteria>
  <done>The recovery script has stable library and family-selection contracts for K-budget solving and output auditing.</done>
</task>

</tasks>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Recover compact auditable symbolic signal-control policies from dual-guided oracle targets with tradeoffs | This plan creates the metadata/tradeoff foundation used by Plans 02 and 03. |
| REQ | RECV-03 explicit program/neighbor/dual penalties or constraints | Covered by metadata fields needed to count neighbor, dual, and placebo atoms; Plan 02 enforces budgets/penalties. |
| REQ | RECV-04 required atom families | Covered directly by the atom registry and `full_symbolic` library. |
| RESEARCH | Extend `scripts/run_sparse_recovery.py`, reuse existing state/oracle conversion, no new pipeline | Covered by modifying only `scripts/run_sparse_recovery.py` and preserving imports from existing scripts. |
| CONTEXT | Atom families must cover local, capacity/slack, raw neighbor, pressure/backpressure, dual, and placebo | Covered by Task 1 and Task 2. |
| CONTEXT | Corridor/service atoms excluded unless backed by explicit primal constraints | Covered by Task 1 exclusion instruction. |
| CONTEXT | Phase 3 empirical dual-vs-pressure claim routing remains out of Phase 2 | Covered by context and plan scope. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI input → recovery script | User-provided library and family names determine which atoms enter MILP recovery. |
| Metadata registry → scientific artifacts | Atom metadata controls dual/placebo/neighbor counts that will be cited in later paper artifacts. |
| Existing oracle utilities → recovery features | `summarize_scenario()` output is transformed into atom feature vectors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Atom family metadata | mitigate | Require explicit metadata fields and reject unknown atoms/libraries before solving. |
| T-02-02 | Repudiation | Dual/placebo classification | mitigate | Store genuine dual and placebo flags separately in registry metadata for downstream outputs. |
| T-02-03 | Information Disclosure | Local experiment paths | accept | Plan uses only local artifact paths under `experiments/dual_sensitivity/` and does not handle secrets. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install is planned; Phase 2 reuses existing Python/NumPy/SciPy stack. |
</threat_model>

<verification>
Overall plan checks:
- `python3 -m py_compile scripts/run_sparse_recovery.py`
- `grep -q "ATOM_REGISTRY" scripts/run_sparse_recovery.py`
- `grep -q "full_symbolic" scripts/run_sparse_recovery.py`
- `grep -q -- "--libraries" scripts/run_sparse_recovery.py`
- `grep -q -- "--atom-families" scripts/run_sparse_recovery.py`
</verification>

<success_criteria>
- Atom metadata is explicit, complete, and registry-driven.
- Required Phase 2 atom families are available through named libraries and `full_symbolic`.
- Unknown libraries, atom names, and family filters are rejected before solving.
- No Phase 3 empirical claim is interpreted in this plan.
</success_criteria>

<output>
Create `.planning/phases/02-full-sparse-symbolic-recovery/02-01-SUMMARY.md` when done.
</output>
