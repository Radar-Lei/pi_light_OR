---
phase: 03-static-pressure-failure-kill-gate
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - scripts/run_static_kill_gate.py
  - experiments/dual_sensitivity/block3_static_kill_gate.json
  - experiments/dual_sensitivity/block3_static_kill_gate.csv
  - experiments/dual_sensitivity/block3_static_kill_gate_rules.txt
autonomous: true
requirements:
  - KILL-02
  - KILL-03
  - KILL-04
must_haves:
  truths:
    - "The kill-gate runner computes dual-vs-pressure metrics separately for each supported/proxy static regime."
    - "Every regime metric includes disagreement rate, dual win rate, pressure win rate, tie rate, dual and pressure mean oracle regret, dual and pressure worst-case regret, and recovered symbolic rules."
    - "Sample sufficiency is based on valid converted examples, not raw generated samples."
    - "Route decision is one of dual-improves-pressure, pressure-equivalent, or diagnostic, derived from static/sample-relative metrics only."
  artifacts:
    - path: "scripts/run_static_kill_gate.py"
      provides: "Main Phase 3 static dual-vs-pressure kill-gate runner"
      exports: ["main"]
    - path: "experiments/dual_sensitivity/block3_static_kill_gate.json"
      provides: "Machine-readable route decision and per-regime metrics"
      contains: "route_decision"
    - path: "experiments/dual_sensitivity/block3_static_kill_gate.csv"
      provides: "Per-regime metric table"
      contains: "dual_vs_pressure_disagreement_rate"
    - path: "experiments/dual_sensitivity/block3_static_kill_gate_rules.txt"
      provides: "Recovered symbolic rules grouped by regime"
      contains: "Phase 3 Static Kill-Gate Rule Text"
  key_links:
    - from: "scripts/run_static_kill_gate.py"
      to: "scripts/run_sparse_recovery.py"
      via: "imports and calls validate_atom_registry, select_library_names or equivalent, load_examples/build_example, solve_library, render_rule_text/render_rules_file"
      pattern: "from run_sparse_recovery import"
    - from: "scripts/run_static_kill_gate.py"
      to: "scripts/run_sumo_sampled_recovery.py"
      via: "valid-example conversion through scenario_from_sample indirectly via run_sparse_recovery.load_examples or explicitly for counts"
      pattern: "scenario_from_sample|load_examples"
    - from: "scripts/run_static_kill_gate.py"
      to: "experiments/dual_sensitivity/block3_static_kill_gate.json"
      via: "Path.write_text(json.dumps(payload, indent=2))"
      pattern: "route_decision"
---

<objective>
Implement the Phase 3 static kill-gate runner that reuses Phase 2 sparse recovery and produces machine-readable route evidence.

Purpose: KILL-02 through KILL-04 require fair per-regime dual-vs-pressure metrics and an explicit paper-route decision; this must not be inferred from Phase 2 diagnostics alone.
Output: `scripts/run_static_kill_gate.py` plus Block 3 JSON/CSV/rules artifacts.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-static-pressure-failure-kill-gate/03-CONTEXT.md
@.planning/phases/03-static-pressure-failure-kill-gate/03-RESEARCH.md
@.planning/phases/03-static-pressure-failure-kill-gate/03-PATTERNS.md
@.planning/phases/03-static-pressure-failure-kill-gate/03-VALIDATION.md
@.planning/phases/02-full-sparse-symbolic-recovery/02-VERIFICATION.md
@scripts/run_sparse_recovery.py
@scripts/run_sumo_sampled_recovery.py
@scripts/run_dual_sanity.py
@experiments/dual_sensitivity/block3_regime_states.json

<interfaces>
Reuse these existing contracts; do not duplicate LP, finite-difference, atom, or MILP logic.

From `scripts/run_sparse_recovery.py`:
- `validate_atom_registry()` validates atom/library metadata.
- `select_library_names(requested_libraries)` validates requested library names.
- `LIBRARIES` contains `dual_sensitivity`, `pressure_backpressure`, `all_neighbor`, `random_price`, `full_symbolic`, and related families.
- `load_examples(paths, tls, max_samples, max_movements, epsilon)` converts state samples via `scenario_from_sample()` and `summarize_scenario()` and then builds examples.
- `solve_library(examples, library, atoms, budget, complexity_penalty, neighbor_penalty, dual_penalty, placebo_penalty, max_neighbor_atoms, max_dual_atoms, max_placebo_atoms, objective_mode, time_limit_sec, min_weight, tie_margin)` runs the SciPy/HiGHS MILP.
- Solved run `results[]` rows include `scenario`, `source`, `chosen_movement`, `oracle_best_movement`, `oracle_regret`, and score diagnostics.
- Solved run metadata includes `selected_atoms`, `selected_atom_metadata`, `program_complexity`, `realized_mean_regret`, `max_regret`, and `rule_text`.

Legacy unlabeled fixture handling:
- If a state file lacks per-sample `regime`, the runner must require or accept `--default-regime` and assign that regime label while recording a caveat in `input_labeling_notes`.
- The quick validation command uses `--default-regime storage_binding_proxy` for `targeted_bottleneck_states.json`; generated Block 3 fixtures should use explicit per-sample labels.

Primary route comparison:
- Compare `dual_sensitivity` vs `pressure_backpressure` by aligned `(source, scenario)` rows.
- Treat `dual_plus_raw`, `full_symbolic`, `all_neighbor`, and `random_price` as secondary diagnostics, not proof of pure dual advantage.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build per-regime sparse-recovery runner with reusable Phase 2 core</name>
  <files>scripts/run_static_kill_gate.py</files>
  <behavior>
    - The runner imports reusable functions/constants from `run_sparse_recovery.py` and does not reimplement atom formulas, LP construction, finite-difference oracle, or MILP constraints.
    - Given a labeled state file, it groups samples by `sample["regime"]`, solves requested libraries per regime and budget, and records valid example counts after conversion.
    - Required CLI flags include `--states`, `--tls`, `--max-samples`, `--max-movements`, `--epsilon`, `--budgets`, `--libraries`, `--default-regime`, `--min-regime-count`, `--target-total-states`, `--dual-win-threshold`, `--regret-improvement-threshold`, `--equivalence-tolerance`, `--time-limit-sec`, `--out`, `--csv-out`, `--rules-out`, and `--report-out`.
  </behavior>
  <action>Create `scripts/run_static_kill_gate.py` as the main Phase 3 runner. It must split samples by regime without losing original sample fields, call the Phase 2 recovery core from `run_sparse_recovery.py`, and solve at least `dual_sensitivity` and `pressure_backpressure` for each regime. Use the exact existing `solve_library()` pathway for sparse recovery; do not write a new LP/MILP or a new pressure/dual scorer. Count `num_examples_total` and `valid_examples_by_regime` after conversion, not by raw sample length. Include schema validation for labeled samples and numeric CLI flags. For legacy state files without per-sample `regime`, support `--default-regime`; when used, assign that regime to unlabeled samples and record `input_labeling_notes` plus a preliminary/proxy caveat in JSON. Keep all language and payload fields static/sample-relative, with no closed-loop superiority or travel-time claims.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 -m py_compile scripts/run_static_kill_gate.py && grep -v '^#' scripts/run_static_kill_gate.py | grep -c 'from run_sparse_recovery import' | python3 -c 'import sys; n=int(sys.stdin.read()); assert n>=1, "run_static_kill_gate.py must import run_sparse_recovery"' && grep -v '^#' scripts/run_static_kill_gate.py | grep -c 'solve_library' | python3 -c 'import sys; n=int(sys.stdin.read()); assert n>=1, "solve_library must be used"'</automated>
  </verify>
  <done>`run_static_kill_gate.py` exists, compiles, validates inputs, groups by regime, and uses the existing Phase 2 sparse-recovery core rather than duplicating LP/MILP logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute KILL-02 metrics and KILL-04 route decision</name>
  <files>scripts/run_static_kill_gate.py</files>
  <behavior>
    - Per-regime metrics include `dual_vs_pressure_disagreement_rate`, `dual_win_rate`, `pressure_win_rate`, `tie_rate`, `dual_mean_oracle_regret`, `pressure_mean_oracle_regret`, `mean_oracle_regret_delta_pressure_minus_dual`, `dual_worst_case_regret`, `pressure_worst_case_regret`, selected atoms, recovered rule references, and `claim_scope`.
    - Route decision is exactly one of `dual-improves-pressure`, `pressure-equivalent`, or `diagnostic`.
    - If valid examples are below `--target-total-states`, JSON sets `sample_target_met=false`, lists `preliminary_regimes`, and lowers route confidence/caveats instead of hiding the shortfall.
  </behavior>
  <action>Add metric aggregation and route selection to `run_static_kill_gate.py`. Align dual and pressure result rows by `(source, scenario)`. Compute tie-aware disagreement/win/regret metrics using `--equivalence-tolerance`: different actions with equal oracle regret count as disagreement but not as a dual or pressure win. Derive route from supported binding-regime evidence: dual improvement requires dual win/regret thresholds against `pressure_backpressure`; pressure ties route to `pressure-equivalent`; weak coverage, pressure wins, unsupported core regimes, or unstable evidence route to `diagnostic`. Store `route_decision`, `route_confidence`, `route_rationale`, and `route_caveats` in JSON. Include `runs_by_regime` or equivalent so each metric is traceable to solved sparse-recovery runs.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 scripts/run_static_kill_gate.py --states experiments/dual_sensitivity/targeted_bottleneck_states.json --tls C3 --max-samples 16 --default-regime storage_binding_proxy --target-total-states 1000 --out /tmp/block3_static_kill_gate.json --csv-out /tmp/block3_static_kill_gate.csv --rules-out /tmp/block3_static_kill_gate_rules.txt --report-out /tmp/block3_static_kill_gate_report.md && python3 -c 'import json; p=json.load(open("/tmp/block3_static_kill_gate.json")); assert p["experiment"]=="block3_static_pressure_failure_kill_gate"; assert p["route_decision"] in {"dual-improves-pressure","pressure-equivalent","diagnostic"}; assert "regime_metrics" in p and p["regime_metrics"]; m=p["regime_metrics"][0]; req={"dual_vs_pressure_disagreement_rate","dual_win_rate","pressure_win_rate","tie_rate","dual_mean_oracle_regret","pressure_mean_oracle_regret","dual_worst_case_regret","pressure_worst_case_regret","recovered_symbolic_rules"}; assert req <= set(m); assert p["sample_target_met"] is False; assert "preliminary_regimes" in p; assert p.get("input_labeling_notes"), "missing legacy labeling note"; assert any(m.get("regime")=="storage_binding_proxy" for m in p["regime_metrics"]); print("kill-gate metrics ok")'</automated>
  </verify>
  <done>Quick kill-gate run creates a JSON payload with required KILL-02 metrics, KILL-03 sufficiency fields, and a valid KILL-04 route decision.</done>
</task>

<task type="auto">
  <name>Task 3: Write CSV and rule artifacts for the generated regime fixture</name>
  <files>experiments/dual_sensitivity/block3_static_kill_gate.json, experiments/dual_sensitivity/block3_static_kill_gate.csv, experiments/dual_sensitivity/block3_static_kill_gate_rules.txt</files>
  <action>Run the full static kill gate on `experiments/dual_sensitivity/block3_regime_states.json`, producing JSON, CSV, and rules artifacts under `experiments/dual_sensitivity/`. CSV columns must include every KILL-02 metric plus `regime`, `num_examples`, `sample_target_met`, `claim_scope`, selected atoms for dual/pressure, and rule text path. Rules text must group recovered symbolic rules by regime and library, state that rules are finite-dictionary sample-relative artifacts, and include selected atoms for `dual_sensitivity` and `pressure_backpressure`. Do not run or claim closed-loop SUMO performance.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 scripts/run_static_kill_gate.py --states experiments/dual_sensitivity/block3_regime_states.json --tls C3 --target-total-states 1000 --out experiments/dual_sensitivity/block3_static_kill_gate.json --csv-out experiments/dual_sensitivity/block3_static_kill_gate.csv --rules-out experiments/dual_sensitivity/block3_static_kill_gate_rules.txt --report-out experiments/dual_sensitivity/block3_static_kill_gate_report.md && python3 -c 'import csv,json,os; p=json.load(open("experiments/dual_sensitivity/block3_static_kill_gate.json")); assert os.path.exists(p["csv_out"]); assert os.path.exists(p["rules_out"]); rows=list(csv.DictReader(open("experiments/dual_sensitivity/block3_static_kill_gate.csv"))); assert rows; cols=set(rows[0]); req={"regime","num_examples","dual_vs_pressure_disagreement_rate","dual_win_rate","pressure_win_rate","tie_rate","dual_mean_oracle_regret","pressure_mean_oracle_regret","dual_worst_case_regret","pressure_worst_case_regret","rule_text_path"}; assert req <= cols; txt=open("experiments/dual_sensitivity/block3_static_kill_gate_rules.txt").read(); assert "Phase 3 Static Kill-Gate Rule Text" in txt and "sample-relative" in txt and "pressure_backpressure" in txt and "dual_sensitivity" in txt; print("full artifacts ok")'</automated>
  </verify>
  <done>Block 3 JSON/CSV/rules artifacts exist, are traceable to the generated state fixture, and expose per-regime dual-vs-pressure evidence without closed-loop claims.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 3 | Static benchmark evidence determines claim route | 03-02,03-03 | COVERED | This plan computes the evidence and machine route.
REQ | KILL-01 | Static regime coverage | 03-01 | COVERED | Consumes the generated labeled fixture from Plan 03-01.
REQ | KILL-02 | Per-regime disagreement, wins, regrets, rules | 03-02 | COVERED | Primary output of this plan.
REQ | KILL-03 | Sample target and preliminary flags | 03-01,03-02 | COVERED | This plan gates on valid converted examples.
REQ | KILL-04 | Identify dual recovers/improves/fails pressure | 03-02 | COVERED | Route decision is computed in JSON.
REQ | KILL-05 | Claim routing documented | 03-03 | COVERED | Report plan consumes this plan's JSON.
RESEARCH | reuse-sparse | Reuse `run_sparse_recovery.py`, `scenario_from_sample()`, and `summarize_scenario()` | 03-02 | COVERED | Explicitly required in tasks and grep gate.
RESEARCH | metric-pattern | Tie-aware aligned dual-vs-pressure metrics | 03-02 | COVERED | Task 2 implements formulas.
CONTEXT | Kill-Gate Purpose | Phase 3 runs own static benchmark, not Phase 2 diagnostics | 03-02 | COVERED | New runner produces Block 3 outputs.
CONTEXT | Metrics and Outputs | Structured JSON/CSV plus recovered rules | 03-02 | COVERED | This plan writes all three.
CONTEXT | Claim Discipline | Do not claim closed-loop superiority | 03-02,03-03 | COVERED | Payload caveats and report enforce static scope.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| State JSON -> kill-gate runner | Input files may be unlabeled, malformed, or inconsistent with conversion expectations. |
| Sparse-recovery results -> route decision | Metric aggregation can overstate dual advantage if ties or sample insufficiency are mishandled. |
| CLI output paths -> filesystem | User-supplied artifact paths can overwrite intended outputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | `scripts/run_static_kill_gate.py` input schema | mitigate | Validate required sample fields and regime labels before solving; fail or mark inconclusive rather than silently dropping evidence. |
| T-03-04 | Repudiation | Route decision traceability | mitigate | Store thresholds, counts, per-regime metrics, route rationale, caveats, and solved runs in JSON. |
| T-03-05 | Information Disclosure | Local file paths in artifacts | accept | Artifacts are local research outputs; no secrets or external services involved. |
| T-03-06 | Tampering | Tie handling and route logic | mitigate | Use explicit tolerance, separate disagreement from win/loss, and record `sample_target_met`/`preliminary_regimes`. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned for Phase 3. |
</threat_model>

<verification>
Overall checks:
1. `python3 -m py_compile scripts/run_static_kill_gate.py` passes.
2. Quick gate command from `03-VALIDATION.md` passes on targeted bottleneck states.
3. Full gate command passes on `block3_regime_states.json` and writes JSON/CSV/rules artifacts.
4. JSON route is one of the locked route categories and includes sample sufficiency fields.
</verification>

<success_criteria>
- `scripts/run_static_kill_gate.py` reuses Phase 2 sparse recovery and never reimplements LP/MILP scoring.
- Every supported/proxy regime has KILL-02 metrics and recovered rule references.
- `sample_target_met` is computed from valid examples, with preliminary caveats when the 1k target is not met.
- `route_decision` and `route_rationale` are static/sample-relative and do not mention closed-loop superiority.
</success_criteria>

<output>
Create `.planning/phases/03-static-pressure-failure-kill-gate/03-02-SUMMARY.md` when done.
</output>
