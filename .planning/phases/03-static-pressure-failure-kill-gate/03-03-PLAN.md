---
phase: 03-static-pressure-failure-kill-gate
plan: "03"
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - scripts/render_static_kill_gate_report.py
  - scripts/run_static_kill_gate.py
  - experiments/dual_sensitivity/block3_static_kill_gate_report.md
autonomous: true
requirements:
  - KILL-04
  - KILL-05
must_haves:
  truths:
    - "A human-readable report states the locked route decision and explains the static evidence behind it."
    - "The report includes sample sufficiency, per-regime metrics, unsupported/proxy regime limitations, and artifact links."
    - "The report preserves Phase 3 claim discipline: static/sample-relative routing only, no closed-loop superiority or deployable-control performance claims."
  artifacts:
    - path: "scripts/render_static_kill_gate_report.py"
      provides: "Deterministic renderer from kill-gate JSON to route report"
      exports: ["main"]
    - path: "scripts/run_static_kill_gate.py"
      provides: "Runner wiring that calls or delegates report generation"
      contains: "report_out"
    - path: "experiments/dual_sensitivity/block3_static_kill_gate_report.md"
      provides: "Human-readable Phase 3 route report"
      contains: "static sample-relative"
  key_links:
    - from: "scripts/render_static_kill_gate_report.py"
      to: "experiments/dual_sensitivity/block3_static_kill_gate.json"
      via: "reads gate JSON and renders route fields without manual transcription"
      pattern: "route_decision"
    - from: "scripts/run_static_kill_gate.py"
      to: "scripts/render_static_kill_gate_report.py"
      via: "runner produces report_out by calling renderer function or equivalent deterministic report generation"
      pattern: "report_out"
    - from: "experiments/dual_sensitivity/block3_static_kill_gate_report.md"
      to: "experiments/dual_sensitivity/block3_static_kill_gate.csv"
      via: "artifact link/reference"
      pattern: "block3_static_kill_gate.csv"
---

<objective>
Add the human-readable Phase 3 claim-routing report and wire it into the static kill-gate flow.

Purpose: KILL-05 requires documented route discipline before Phase 4 closed-loop results are interpreted; KILL-04 requires the chosen route to be visible to humans, not only buried in JSON.
Output: `scripts/render_static_kill_gate_report.py`, report wiring, and `experiments/dual_sensitivity/block3_static_kill_gate_report.md`.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-static-pressure-failure-kill-gate/03-CONTEXT.md
@.planning/phases/03-static-pressure-failure-kill-gate/03-RESEARCH.md
@.planning/phases/03-static-pressure-failure-kill-gate/03-PATTERNS.md
@.planning/phases/03-static-pressure-failure-kill-gate/03-VALIDATION.md
@.planning/phases/02-full-sparse-symbolic-recovery/02-VERIFICATION.md
@scripts/run_static_kill_gate.py
@experiments/dual_sensitivity/block3_static_kill_gate.json
@experiments/dual_sensitivity/block3_static_kill_gate.csv
@experiments/dual_sensitivity/block3_static_kill_gate_rules.txt

<interfaces>
Report input contract from `block3_static_kill_gate.json`:
- Required top-level fields: `experiment`, `status`, `input_states`, `tls`, `target_total_states`, `num_examples_total`, `sample_target_met`, `preliminary_regimes`, `regime_metrics`, `route_decision`, `route_confidence`, `route_rationale`, `route_caveats`, `csv_out`, `rules_out`, `report_out`.
- `route_decision` must be one of `dual-improves-pressure`, `pressure-equivalent`, `diagnostic`.
- `regime_metrics[]` must contain KILL-02 metrics and selected atom/rule references.

Required report sections:
- Route decision and confidence.
- Static-only scope and claim caveats.
- Sample sufficiency: `num_examples_total`, `target_total_states`, `sample_target_met`, `preliminary_regimes`.
- Per-regime metric table.
- Unsupported/proxy regime limitations.
- Artifact links to JSON, CSV, and rules files.
- Route alias mapping: `pressure-equivalent` means dual-recovers/ties pressure; `diagnostic` means dual-underperforms, unsupported regimes, or evidence too weak for a positive route.
- Phase 4 implications / experiment priorities derived from the selected route, without running closed-loop experiments.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic route report renderer</name>
  <files>scripts/render_static_kill_gate_report.py</files>
  <behavior>
    - Given a valid `block3_static_kill_gate.json`, the renderer writes a Markdown report containing route decision, route rationale, caveats, sample sufficiency, per-regime metrics, and artifact links.
    - The renderer fails with nonzero exit or raises a clear error if required route fields are missing when `--fail-on-missing-route` is set.
    - The report text includes explicit static/sample-relative claim discipline and excludes closed-loop superiority language.
  </behavior>
  <action>Create `scripts/render_static_kill_gate_report.py` with `main() -> None`, argparse flags `--gate-json`, `--out`, and `--fail-on-missing-route`. Read the gate JSON, validate the required fields, and render deterministic Markdown. Include the exact route category from JSON and its rationale; do not recompute route thresholds in the renderer. Include a route alias section explaining that `pressure-equivalent` means dual-recovers/ties pressure and `diagnostic` means dual-underperforms, unsupported regimes, or evidence too weak for a positive route. Include a per-regime table with KILL-02 metrics, a limitations section that preserves proxy/unsupported wording for supply-binding or corridor-bottleneck when present, and a Phase 4 implications/priorities section derived from the selected route. The report must say Phase 3 is static/sample-relative and pre-closed-loop; it must not include travel-time, throughput, deployable performance, or closed-loop superiority claims.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 -m py_compile scripts/render_static_kill_gate_report.py && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 scripts/render_static_kill_gate_report.py --gate-json experiments/dual_sensitivity/block3_static_kill_gate.json --out /tmp/block3_static_kill_gate_report.md --fail-on-missing-route && python3 -c 'txt=open("/tmp/block3_static_kill_gate_report.md").read().lower(); assert "route decision" in txt; assert "static" in txt and "sample-relative" in txt; assert "sample sufficiency" in txt; assert "phase 4" in txt and ("implications" in txt or "priorities" in txt); assert "dual-recovers/ties pressure" in txt or "dual-recovers" in txt; assert "pressure-equivalent" in txt and "diagnostic" in txt; assert "dual_vs_pressure_disagreement_rate" in txt or "disagreement" in txt; forbidden=["closed-loop superiority","travel time superiority","throughput superiority","deployable performance superiority"]; assert not any(x in txt for x in forbidden); print("report renderer ok")'</automated>
  </verify>
  <done>`render_static_kill_gate_report.py` turns gate JSON into a deterministic, static-scope Markdown report with all KILL-05 sections.</done>
</task>

<task type="auto">
  <name>Task 2: Wire report generation into the kill-gate runner</name>
  <files>scripts/run_static_kill_gate.py</files>
  <action>Update `scripts/run_static_kill_gate.py` so `--report-out` is honored by invoking the report renderer function or a shared helper after JSON/CSV/rules are written. Avoid circular imports; if necessary, expose a pure `render_report(payload, gate_json_path, out_path) -> str` helper in `render_static_kill_gate_report.py` and import it into the runner. The runner stdout should include `report_out` along with `status`, `out`, `csv_out`, `rules_out`, and `num_examples_total`. Keep `route_decision` in JSON as the single source of truth; the report must not manually diverge from it.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 -m py_compile scripts/run_static_kill_gate.py scripts/render_static_kill_gate_report.py && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 scripts/run_static_kill_gate.py --states experiments/dual_sensitivity/block3_regime_states.json --tls C3 --target-total-states 1000 --out /tmp/block3_static_kill_gate.json --csv-out /tmp/block3_static_kill_gate.csv --rules-out /tmp/block3_static_kill_gate_rules.txt --report-out /tmp/block3_static_kill_gate_report.md && python3 -c 'import json,os; p=json.load(open("/tmp/block3_static_kill_gate.json")); assert p["report_out"]=="/tmp/block3_static_kill_gate_report.md"; assert os.path.exists(p["report_out"]); txt=open(p["report_out"]).read(); assert p["route_decision"] in txt; print("runner report wiring ok")'</automated>
  </verify>
  <done>`run_static_kill_gate.py` always writes the requested report artifact and the report route matches JSON.</done>
</task>

<task type="auto">
  <name>Task 3: Generate final Phase 3 route report and validate claim discipline</name>
  <files>experiments/dual_sensitivity/block3_static_kill_gate_report.md</files>
  <action>Regenerate the full Block 3 artifacts using the command in `03-VALIDATION.md` so `experiments/dual_sensitivity/block3_static_kill_gate_report.md` matches the current JSON/CSV/rules outputs. Validate that the report includes the three possible routing meanings from ROADMAP: strong mainline for dual advantage, generalized-pressure symbolic recovery for pressure tie, and diagnostic framing for underperformance. It must document the selected route before Phase 4, include Phase 4 implications/priorities derived from that route, and explicitly say no closed-loop superiority is claimed in Phase 3.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 scripts/run_static_kill_gate.py --states experiments/dual_sensitivity/block3_regime_states.json --tls C3 --target-total-states 1000 --out experiments/dual_sensitivity/block3_static_kill_gate.json --csv-out experiments/dual_sensitivity/block3_static_kill_gate.csv --rules-out experiments/dual_sensitivity/block3_static_kill_gate_rules.txt --report-out experiments/dual_sensitivity/block3_static_kill_gate_report.md && python3 -c 'import json; p=json.load(open("experiments/dual_sensitivity/block3_static_kill_gate.json")); txt=open("experiments/dual_sensitivity/block3_static_kill_gate_report.md").read().lower(); assert p["route_decision"] in txt; assert "dual-improves-pressure" in txt and "pressure-equivalent" in txt and "diagnostic" in txt; assert "static" in txt and "sample-relative" in txt; assert "no closed-loop" in txt or "closed-loop" in txt and "not" in txt; forbidden=["closed-loop superiority is shown","travel-time superiority","throughput superiority","deployable control performance is proven"]; assert not any(x in txt for x in forbidden); print("final report ok")'</automated>
  </verify>
  <done>`block3_static_kill_gate_report.md` exists, matches the JSON route decision, documents all three routing meanings, and enforces static-only claim discipline.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 3 | Static benchmark evidence determines claim route before Phase 4 | 03-02,03-03 | COVERED | This plan makes the route human-readable.
REQ | KILL-01 | Regime coverage | 03-01 | COVERED | Report includes limitations but coverage created upstream.
REQ | KILL-02 | Metrics and recovered rules | 03-02,03-03 | COVERED | Report renders metrics generated upstream.
REQ | KILL-03 | Sample sufficiency | 03-02,03-03 | COVERED | Report shows target, counts, preliminary regimes.
REQ | KILL-04 | Identify route | 03-02,03-03 | COVERED | JSON route is rendered in report.
REQ | KILL-05 | Claim routing documented | 03-03 | COVERED | Primary output of this plan.
RESEARCH | route-report | Deterministic report from Block 3 JSON | 03-03 | COVERED | Implemented as `render_static_kill_gate_report.py`.
RESEARCH | claim-discipline | Avoid overclaiming static evidence as closed-loop performance | 03-03 | COVERED | Forbidden-language validation and report caveats.
CONTEXT | Claim Discipline | Strong/tie/underperformance routes documented; no closed-loop superiority | 03-03 | COVERED | Report documents all route meanings and selected route.
CONTEXT | Deferred Ideas | Closed-loop SUMO experiments and manuscript final positioning deferred | NONE | EXCLUDED | Report may mention Phase 4 boundary but does not implement deferred work.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gate JSON -> report renderer | Malformed or incomplete JSON can produce misleading route documentation. |
| Report text -> downstream interpretation | Overclaiming static evidence can contaminate Phase 4 design and manuscript claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | `scripts/render_static_kill_gate_report.py` input JSON | mitigate | Validate required route fields and support `--fail-on-missing-route`. |
| T-03-08 | Repudiation | Route documentation | mitigate | Render route decision, thresholds/rationale, caveats, metrics, and artifact links from JSON without manual transcription. |
| T-03-09 | Information Disclosure | Report artifact links | accept | Local research artifact paths are expected; no secrets are written. |
| T-03-10 | Spoofing | Static evidence described as closed-loop performance | mitigate | Hard-code static/sample-relative caveat and validate against forbidden superiority language. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned for Phase 3. |
</threat_model>

<verification>
Overall checks:
1. `python3 -m py_compile scripts/run_static_kill_gate.py scripts/render_static_kill_gate_report.py` passes.
2. Report renderer works as a standalone CLI on `block3_static_kill_gate.json`.
3. `run_static_kill_gate.py --report-out ...` writes a report whose route matches JSON.
4. Final report includes static/sample-relative caveats and excludes closed-loop superiority claims.
</verification>

<success_criteria>
- `scripts/render_static_kill_gate_report.py` exists and renders deterministic Markdown from JSON.
- `scripts/run_static_kill_gate.py` writes `report_out` during normal execution.
- `experiments/dual_sensitivity/block3_static_kill_gate_report.md` documents the selected route, route alias mapping, the three possible routing meanings, Phase 4 implications/priorities, sample sufficiency, caveats, per-regime metrics, and artifact links.
- Phase 3 remains static/sample-relative and does not implement or claim Phase 4 closed-loop evidence.
</success_criteria>

<output>
Create `.planning/phases/03-static-pressure-failure-kill-gate/03-03-SUMMARY.md` when done.
</output>
