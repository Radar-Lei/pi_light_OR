---
phase: 03-static-pressure-failure-kill-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_static_regime_states.py
  - experiments/dual_sensitivity/block3_regime_states.json
autonomous: true
requirements:
  - KILL-01
  - KILL-03
must_haves:
  truths:
    - "Static benchmark inputs include slack, downstream storage-binding, supply-binding/proxy-or-unsupported, corridor-bottleneck/proxy-or-unsupported, incident/capacity-drop, and demand-shift/proxy states."
    - "Every generated sample keeps the existing sample schema required by scenario_from_sample(): time, queues, vehicle_counts, capacities, tls_movements."
    - "Sample sufficiency is auditable through raw counts by regime, valid-target intent, generation config, and explicit unsupported/proxy rationale."
  artifacts:
    - path: "scripts/generate_static_regime_states.py"
      provides: "Deterministic labeled-regime state generator for Phase 3"
      exports: ["main"]
    - path: "experiments/dual_sensitivity/block3_regime_states.json"
      provides: "Combined labeled state fixture for the static kill gate"
      contains: "samples"
  key_links:
    - from: "scripts/generate_static_regime_states.py"
      to: "scripts/sample_sumo_states.py"
      via: "imports build_network_metadata"
      pattern: "from sample_sumo_states import build_network_metadata"
    - from: "experiments/dual_sensitivity/block3_regime_states.json"
      to: "scripts/run_sumo_sampled_recovery.py"
      via: "preserved sample schema consumed by scenario_from_sample"
      pattern: "time.*queues.*vehicle_counts.*capacities.*tls_movements"
---

<objective>
Create the Phase 3 labeled static-regime fixture generator and the first generated artifact.

Purpose: KILL-01 and KILL-03 require regime coverage and visible sample sufficiency before any dual-vs-pressure route can be trusted.
Output: `scripts/generate_static_regime_states.py` and `experiments/dual_sensitivity/block3_regime_states.json`.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-static-pressure-failure-kill-gate/03-CONTEXT.md
@.planning/phases/03-static-pressure-failure-kill-gate/03-RESEARCH.md
@.planning/phases/03-static-pressure-failure-kill-gate/03-PATTERNS.md
@.planning/phases/03-static-pressure-failure-kill-gate/03-VALIDATION.md
@.planning/phases/02-full-sparse-symbolic-recovery/02-VERIFICATION.md
@scripts/generate_targeted_bottleneck_states.py
@scripts/sample_sumo_states.py

<interfaces>
Use these existing contracts directly; do not create a second sampler or state schema.

From `scripts/sample_sumo_states.py` / `scripts/generate_targeted_bottleneck_states.py` patterns:
- State payload top level: `network`, optional `net_file`, optional `tls`, `num_samples`, `samples`.
- Sample fields required by downstream conversion: `time`, `queues`, `vehicle_counts`, `capacities`, `tls_movements`.
- Network metadata source: `build_network_metadata(net_file)` returns `edge_capacity` and `tls_movements`.

Phase 3 required additions per sample:
- `regime`: one of `slack`, `storage_binding`, `supply_binding_proxy`, `supply_binding_unsupported`, `corridor_bottleneck_proxy`, `corridor_bottleneck_unsupported`, `incident_capacity_drop`, `demand_shift_proxy`.
- `regime_detail`: short rationale with perturbation parameters or unsupported-by-current-model explanation.
- `generated_by`: script name and deterministic generation parameters.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create deterministic multi-regime state generator</name>
  <files>scripts/generate_static_regime_states.py</files>
  <behavior>
    - Given `--target-per-regime 10 --out /tmp/block3_regime_states.json`, the script writes JSON with all KILL-01 regime categories represented as supported, proxy, or unsupported-by-current-model entries.
    - Every emitted supported/proxy sample has `time`, `queues`, `vehicle_counts`, `capacities`, `tls_movements`, `regime`, `regime_detail`, and `generated_by`.
    - CLI rejects nonpositive `--target-per-regime`, unknown `--regimes`, and missing TLS metadata with clear errors.
  </behavior>
  <action>Create `scripts/generate_static_regime_states.py` using the `generate_targeted_bottleneck_states.py` generator pattern and `sample_sumo_states.build_network_metadata()`. Implement `main() -> None` with argparse flags `--net-file`, `--tls`, `--regimes`, `--target-per-regime`, `--seed`, `--capacity-drop-factors`, `--demand-shift-factors`, and `--out`. Generate slack, downstream `storage_binding`, `incident_capacity_drop`, and `demand_shift_proxy` samples by deterministic queue/capacity perturbations. For `supply_binding` and `corridor_bottleneck`, create supported proxy samples only when the current sample schema can encode the relevant field; otherwise emit explicit unsupported/proxy status records in top-level `regime_status` and do not invent unsupported corridor/service claims. Preserve the existing sample fields exactly so Phase 2 conversion code still works. Use compact JSON stdout with `out`, `num_samples`, and counts by regime.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 -m py_compile scripts/generate_static_regime_states.py && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 scripts/generate_static_regime_states.py --target-per-regime 10 --out /tmp/block3_regime_states.json && python3 -c 'import json; p=json.load(open("/tmp/block3_regime_states.json")); assert "samples" in p and p["samples"]; regimes={s["regime"] for s in p["samples"]}; assert {"slack","storage_binding","incident_capacity_drop","demand_shift_proxy"} <= regimes; assert "regime_status" in p; assert all({"time","queues","vehicle_counts","capacities","tls_movements","regime","regime_detail","generated_by"} <= set(s) for s in p["samples"]); print("generator schema ok")'</automated>
  </verify>
  <done>`generate_static_regime_states.py` creates deterministic, schema-compatible labeled samples and records explicit proxy/unsupported rationale for regimes not represented by current primal constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Materialize the Phase 3 regime fixture artifact</name>
  <files>experiments/dual_sensitivity/block3_regime_states.json</files>
  <action>Run the new generator from the repository root with `--target-per-regime 200 --out experiments/dual_sensitivity/block3_regime_states.json`. Ensure the output records `generation_config`, `num_samples`, `counts_by_regime`, `regime_status`, and `target_per_regime`. KILL-03 requires the artifact to expose whether the intended 1k main-analysis target can be reached by valid downstream examples; raw generated counts are not sufficient by themselves, so include a clear top-level note that final sufficiency is determined by `run_static_kill_gate.py` after `scenario_from_sample()` and `build_example()` conversion.</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && PYTHONPATH=/home/samuel/projects/pi_light_OR/scripts python3 scripts/generate_static_regime_states.py --target-per-regime 200 --out experiments/dual_sensitivity/block3_regime_states.json && python3 -c 'import json; p=json.load(open("experiments/dual_sensitivity/block3_regime_states.json")); assert p["num_samples"] == len(p["samples"]); assert p.get("target_per_regime") == 200; assert "counts_by_regime" in p and "regime_status" in p and "generation_config" in p; assert any("unsupported" in str(v).lower() or "proxy" in str(v).lower() for v in p["regime_status"].values()); print("regime artifact ok")'</automated>
  </verify>
  <done>`block3_regime_states.json` exists under `experiments/dual_sensitivity/` and covers KILL-01 regimes with explicit counts, proxy labels, and unsupported-by-current-model rationale where applicable.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 3 | Static benchmark evidence routes the paper claim | 03-01,03-02,03-03 | COVERED | This plan creates the benchmark input base.
REQ | KILL-01 | Slack, storage-binding, supply-binding, corridor-bottleneck, incident/capacity-drop, demand-shift regimes | 03-01 | COVERED | Unsupported/proxy statuses are explicit when current constraints do not support stronger labels.
REQ | KILL-02 | Per-regime dual-vs-pressure metrics and recovered rules | 03-02 | COVERED | Downstream plan computes metrics using sparse recovery.
REQ | KILL-03 | At least 1k target or explicit insufficiency | 03-01,03-02 | COVERED | This plan generates target counts; Plan 03-02 gates valid converted examples.
REQ | KILL-04 | Identify dual improves/ties/fails pressure | 03-02 | COVERED | Downstream route decision comes from metrics.
REQ | KILL-05 | Document claim routing | 03-03 | COVERED | Downstream report renders route and caveats.
RESEARCH | reuse-sparse | Reuse `run_sparse_recovery.py`; do not rewrite LP/MILP | 03-02 | COVERED | This plan preserves state schema for that reuse path.
CONTEXT | Kill-Gate Purpose | Phase 3 must run its own static benchmark analysis | 03-01,03-02 | COVERED | New Block 3 fixture and runner are planned.
CONTEXT | Static Regimes | Cover requested regimes as supported by current generators/scaffolds | 03-01 | COVERED | Proxy/unsupported limitations are recorded.
CONTEXT | Claim Discipline | No closed-loop superiority or deployable-control claim | 03-03 | COVERED | Report plan enforces static/sample-relative wording.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI user input -> generator | Untrusted flags choose output path, regime list, sample target, and perturbation factors. |
| Generated JSON -> downstream runner | Malformed state fixtures can mislead route metrics if schema is not preserved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `scripts/generate_static_regime_states.py` output paths | mitigate | Validate positive numeric flags; create only requested parent directories; never delete or rewrite unrelated files. |
| T-03-02 | Tampering | Regime labels | mitigate | Emit `regime_status` and `regime_detail` so unsupported supply/corridor evidence is not mislabeled as explicit binding evidence. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned for Phase 3. |
</threat_model>

<verification>
Wave 1 verification is complete when the generator compiles, the smoke artifact passes schema assertions, and the materialized `block3_regime_states.json` exposes counts and proxy/unsupported rationale.
</verification>

<success_criteria>
- `scripts/generate_static_regime_states.py` exists and follows existing script conventions: snake_case, `main() -> None`, argparse, `Path`, compact JSON stdout.
- `experiments/dual_sensitivity/block3_regime_states.json` exists and preserves downstream-compatible sample schema.
- KILL-01 regime coverage is explicit; KILL-03 count targeting is visible and not overstated before valid-example conversion.
</success_criteria>

<output>
Create `.planning/phases/03-static-pressure-failure-kill-gate/03-01-SUMMARY.md` when done.
</output>
