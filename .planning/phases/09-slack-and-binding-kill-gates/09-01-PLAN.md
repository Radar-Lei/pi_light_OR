---
phase: 09-slack-and-binding-kill-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run_slack_binding_gates.py
  - tests/test_slack_binding_gates.py
  - experiments/dual_sensitivity/phase9_slack_binding_gates.json
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-04
user_setup: []
must_haves:
  truths:
    - "Gate A verifies slack recovery/tie behavior using explicit finite-storage state and recomputed Phase 8 action audit."
    - "Gate B verifies binding action separation and strict one-step constrained-objective improvement using explicit finite-storage state and recomputed Phase 8 action audit."
    - "Gate outputs fail closed when explicit state, objective components, action audit fields, pressure comparator fields, or required examples are missing."
    - "Payload caveats exclude Gate C, paired-seed closed-loop dominance, baseline suites, stress scenarios, long-horizon evidence, and manuscript claims."
  artifacts:
    - path: "scripts/run_slack_binding_gates.py"
      provides: "deterministic Phase 9 Gate A/B/fail-closed runner"
    - path: "tests/test_slack_binding_gates.py"
      provides: "GATE-01/GATE-02/GATE-04 pass and fail-closed tests"
    - path: "experiments/dual_sensitivity/phase9_slack_binding_gates.json"
      provides: "machine-readable Phase 9 gate artifact"
---

<objective>
完成 Phase 9 的 GATE-01、GATE-02、GATE-04：新增一个 deterministic fail-closed gate runner，分别验证 slack recovery/tie 和 binding action/objective separation，并在缺少显式状态、目标项、动作审计或 pressure comparator 时失败。

Purpose: 让后续 Phase 10/11 在进入 baselines、stress scenarios、paired-seed dominance 之前，有一个可机器审计的 Gate A/B 前置条件；本阶段只产出静态显式状态/action-decomposition gate，不做 closed-loop dominance。
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-slack-and-binding-kill-gates/09-CONTEXT.md
@.planning/phases/09-slack-and-binding-kill-gates/09-RESEARCH.md
@.planning/phases/09-slack-and-binding-kill-gates/09-PATTERNS.md
@.planning/phases/09-slack-and-binding-kill-gates/09-VALIDATION.md
@.planning/phases/08-live-finite-storage-primal-dual-controller/08-01-SUMMARY.md
@.planning/phases/08-live-finite-storage-primal-dual-controller/08-VERIFICATION.md
@scripts/run_closed_loop_sumo.py
@scripts/finite_storage_schema.py
@experiments/dual_sensitivity/phase7_theory_separation.json

Key helper contracts to reuse:

```python
from finite_storage_schema import validate_state_objective_sample
from run_closed_loop_sumo import select_finite_storage_action_with_audit, FINITE_STORAGE_DECOMPOSITION_FIELDS
```

Expected artifact:

```json
{
  "experiment": "phase9_slack_binding_gates",
  "status": "PASSED|FAILED",
  "scope": "static_explicit_state_action_decomposition_only_no_gate_c",
  "requirements_covered": ["GATE-01", "GATE-02", "GATE-04"],
  "gate_a_slack_recovery": {},
  "gate_b_binding_separation": {},
  "fail_closed_checks": {},
  "caveats": ["No Gate C, paired-seed dominance, baseline suite, stress scenarios, long-horizon evidence, or closed-loop superiority claim."]
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 写入 Phase 9 gate 合同测试</name>
  <files>tests/test_slack_binding_gates.py</files>
  <action>新增 `tests/test_slack_binding_gates.py`，先定义 GATE-01/GATE-02/GATE-04 合同。测试应导入计划新增的 `load_phase7_examples`, `example_to_two_phase_fixture`, `validate_explicit_gate_example`, `validate_action_audit`, `evaluate_gate_a_slack`, `evaluate_gate_b_binding`, `build_payload`, `gates_pass`, `write_gate_artifact`。使用 `experiments/dual_sensitivity/phase7_theory_separation.json` canonical fixture。必须测试：Gate A slack fixture `PASSED` 且 action agreement/tie reporting 不使用 win/superiority 语言；Gate B binding fixture `PASSED`，finite-storage action differs from pressure，strict objective improvement true，changing terms intersect `{downstream_storage, spillback, switching, incident}`；missing `finite_storage_state` fails closed；missing `objective_components` fails closed；missing action audit fields fails closed；missing any component from `FINITE_STORAGE_DECOMPOSITION_FIELDS` in phase/action decomposition fails closed；missing pressure comparator fields fails closed；missing required slack/binding example fails closed；payload must not contain affirmative Gate C sections/metrics/claims, paired-seed CI, baseline coverage, stress scenario suite, long-horizon dominance, universal superiority, or manuscript-claim language; negative caveat text excluding Gate C is allowed. 不修改 runner 实现；不要调用 SUMO。</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 -m py_compile tests/test_slack_binding_gates.py</automated>
  </verify>
  <acceptance_criteria>
    - 测试文件覆盖 Gate A/B pass cases 和 fail-closed negative cases。
    - 测试只依赖 stdlib/local helpers，不调用 SUMO，不要求 paired seeds。
  </acceptance_criteria>
  <done>Phase 9 合同测试明确覆盖 slack/binding pass cases、schema/action-audit/component-level fail-closed cases、pressure comparator 缺失、required example 缺失和 scope wording guards。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 deterministic slack/binding gate runner</name>
  <files>scripts/run_slack_binding_gates.py, tests/test_slack_binding_gates.py</files>
  <action>新增 `scripts/run_slack_binding_gates.py`。实现 argparse 参数 `--phase7-json` 默认 `experiments/dual_sensitivity/phase7_theory_separation.json`，`--out` 默认 `experiments/dual_sensitivity/phase9_slack_binding_gates.json`。实现纯 helpers：`load_phase7_examples(path)` 读取 JSON、要求 top-level status PASSED、examples 非空；`example_to_two_phase_fixture(example)` 将 Phase 7 `movements` 映射到 `phase_states={"J0": ["Gr", "rG", ...]}`、`tls_movements={"J0": [...]}`、queues/capacities/state；`validate_explicit_gate_example(example)` 要求 `finite_storage_state` 与 `objective_components` 并调用 `validate_state_objective_sample`；`validate_action_audit(audit)` 要求 pressure_action、finite_storage_action、selected_action、pressure_phase_scores、phase_scores、selected_component_totals、changing_terms，且 selected_action == finite_storage_action；还必须检查 `selected_component_totals` 和每个 `phase_scores[*].component_totals` 精确包含 `FINITE_STORAGE_DECOMPOSITION_FIELDS`，缺少任一 pressure/downstream_storage/spillback/switching/service/incident/total 组件都 fail closed；`evaluate_gate_a_slack(examples)` 过滤 slack examples，重算 action audit，要求 finite-storage action matches pressure action or pressure action in tie set，报告 agreement/tie diagnostics；`evaluate_gate_b_binding(examples)` 过滤 binding/separation examples，重算 action audit，要求 finite-storage action differs from pressure，changing_terms 命中 storage/spillback/switching/incident，且 `objective_margin > 0` 或 finite objective total strictly improves pressure action；`build_payload` 汇总 status、gate sections、fail_closed_checks、scope/caveats；`gates_pass(payload)` 仅检查 Gate A/B/fail_closed checks；`write_gate_artifact` 写 JSON。实现失败时应返回 `status: FAILED` 和 diagnostics 或在 malformed input helper 中抛 `ValueError`，不得静默跳过。不要导入或运行 SUMO；不要修改 `run_closed_loop_suite.py`；不要添加 Gate C、baselines、stress scenarios、paired-seed stats 或 dominance wording。</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 -m py_compile scripts/run_slack_binding_gates.py tests/test_slack_binding_gates.py</automated>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 -m pytest tests/test_slack_binding_gates.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Canonical Phase 7 slack/binding examples produce Gate A/B `PASSED`.
    - Negative tests prove missing schema/audit/comparator/example data fail closed.
    - Negative tests prove missing any `FINITE_STORAGE_DECOMPOSITION_FIELDS` component in action/phase decomposition fails closed.
    - Payload scope and caveats exclude affirmative Gate C metrics/claims and dominance interpretation while allowing negative caveat text.
  </acceptance_criteria>
  <done>`scripts/run_slack_binding_gates.py` implements deterministic Gate A/B evaluation, component-level action-decomposition validation, fail-closed malformed input handling, JSON payload generation, and no out-of-scope Gate C/dominance machinery。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 生成 Phase 9 artifact 并运行相关回归</name>
  <files>scripts/run_slack_binding_gates.py, tests/test_slack_binding_gates.py, experiments/dual_sensitivity/phase9_slack_binding_gates.json</files>
  <action>运行 `scripts/run_slack_binding_gates.py` 生成 `experiments/dual_sensitivity/phase9_slack_binding_gates.json`。确认 artifact `status == "PASSED"`、`requirements_covered` 精确包含 GATE-01/GATE-02/GATE-04、Gate A/B sections 包含 diagnostics 和 rates、fail_closed_checks 均为 PASSED、caveats 明确排除 Gate C/paired-seed dominance/baseline suite/stress scenarios/long-horizon evidence/closed-loop superiority claims。运行 Phase 9 tests 与相关 Phase 7/8/schema 回归。完成后写 `09-01-SUMMARY.md` 与 `09-VERIFICATION.md`，并更新 ROADMAP/REQUIREMENTS/STATE/PROJECT 标记 Phase 9 complete。不要创建 manuscript 文本、长时域实验输出或 baseline/stress artifact。</action>
  <verify>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 scripts/run_slack_binding_gates.py --phase7-json experiments/dual_sensitivity/phase7_theory_separation.json --out experiments/dual_sensitivity/phase9_slack_binding_gates.json</automated>
    <automated>cd /home/samuel/projects/pi_light_OR && python3 -m pytest tests/test_slack_binding_gates.py tests/test_closed_loop_sumo.py tests/test_theory_separation.py tests/test_finite_storage_schema.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Phase 9 artifact exists, parses, and reports `PASSED`.
    - Artifact demonstrates component-level action decomposition validation and fail-closed checks in tests.
    - Relevant regression suite passes.
    - Summary/verification docs record exact commands and scope boundaries.
  </acceptance_criteria>
  <done>Canonical Phase 9 artifact is generated and verified, regression tests pass, planning docs are updated, and Phase 9 is marked complete without adding Gate C, baselines, stress scenarios, or dominance evidence。</done>
</task>

</tasks>

<verification>
必须运行：

1. `cd /home/samuel/projects/pi_light_OR && python3 -m py_compile scripts/run_slack_binding_gates.py tests/test_slack_binding_gates.py`
2. `cd /home/samuel/projects/pi_light_OR && python3 -m pytest tests/test_slack_binding_gates.py -q`
3. `cd /home/samuel/projects/pi_light_OR && python3 scripts/run_slack_binding_gates.py --phase7-json experiments/dual_sensitivity/phase7_theory_separation.json --out experiments/dual_sensitivity/phase9_slack_binding_gates.json`
4. `cd /home/samuel/projects/pi_light_OR && python3 -m pytest tests/test_slack_binding_gates.py tests/test_closed_loop_sumo.py tests/test_theory_separation.py tests/test_finite_storage_schema.py -q`
</verification>

<success_criteria>
- GATE-01: Gate A slack recovery/tie passes and reports ties as expected behavior.
- GATE-02: Gate B binding separation passes with strict one-step constrained-objective improvement and changing terms.
- GATE-04: Missing explicit state/objective/action-audit/pressure-comparator/example data fails closed.
- No Gate C, paired-seed dominance, baselines/stress suite, long-horizon evidence, or manuscript/dominance claims are introduced.
</success_criteria>

<output>
完成后创建 `09-01-SUMMARY.md`、`09-VERIFICATION.md`，并更新 planning state。
</output>
