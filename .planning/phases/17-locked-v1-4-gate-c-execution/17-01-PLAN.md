---
phase: 17
plan: 01
title: Preflight locked v1.4 Gate C spec, environment, row keys, and resume paths
wave: 1
status: pending
---

<objective>Validate the locked v1.4 protocol and create execution plumbing that derives its spec only from the lock artifact.</objective>

<files>
  - scripts/run_v14_locked_gate_c.py
  - tests/test_v14_locked_gate_c.py
  - experiments/dual_sensitivity/v1_4_locked_gate_c_protocol.json
</files>

<tasks>
1. Add a v1.4 locked Gate C runner that loads and validates `v1_4_locked_gate_c_protocol.json`.
2. Build row specs from protocol fields: selected controller, required comparators, binding scenarios, seeds, demand multipliers, main profile, steps, and warmup.
3. Expose `--dry-run`, `--execution-row-limit`, `--progress-out`, and `--resume-progress` so execution can fail closed or resume safely.
4. Verify protocol status, pre-confirmation lock, selected controller, required comparators, and fingerprint are copied into the output.
</tasks>

<verify>
pytest -q tests/test_v14_locked_gate_c.py
</verify>
