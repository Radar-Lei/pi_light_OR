---
phase: 13-complete-predeclared-gate-c-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json
  - experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.progress.json
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03, PROTO-01]
user_setup: []
must_haves:
  truths:
    - "The existing resume/progress state is compatible with the locked Phase 11 main spec before any long execution resumes."
    - "Current row counts, missing keys, failure reasons, and spec fingerprint status are reported explicitly."
    - "Fast Phase 11/Phase 12 assertion tests pass before the expensive SUMO run is attempted."
  artifacts:
    - path: "experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json"
      provides: "Current authoritative Phase 11 main evidence artifact"
      contains: "expected_row_count"
    - path: "experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.progress.json"
      provides: "Current row-level resume/progress artifact"
      contains: "completed_row_keys"
---

<objective>
Preflight the locked Phase 11 main evidence family before resuming the 2160-row execution.

Purpose: Phase 13 must resume the original main profile only when progress and source artifacts still match the predeclared spec. This plan proves the command contract and current artifacts are auditable before any long-running SUMO work.
Output: A summary recording fast test results, locked cardinalities, current row coverage, and whether the main execution can safely resume.
</objective>

<execution_context>
@/home/samuel/.codex/get-shit-done/workflows/execute-plan.md
@/home/samuel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/samuel/projects/pi_light_OR/.planning/PROJECT.md
@/home/samuel/projects/pi_light_OR/.planning/ROADMAP.md
@/home/samuel/projects/pi_light_OR/.planning/STATE.md
@/home/samuel/projects/pi_light_OR/.planning/REQUIREMENTS.md
@/home/samuel/projects/pi_light_OR/.planning/phases/13-complete-predeclared-gate-c-evidence/13-CONTEXT.md
@/home/samuel/projects/pi_light_OR/.planning/phases/13-complete-predeclared-gate-c-evidence/13-RESEARCH.md
@/home/samuel/projects/pi_light_OR/.planning/phases/13-complete-predeclared-gate-c-evidence/13-VALIDATION.md
@/home/samuel/projects/pi_light_OR/scripts/run_phase11_paired_evidence.py
@/home/samuel/projects/pi_light_OR/scripts/run_gate_c_paired_evidence.py
@/home/samuel/projects/pi_light_OR/scripts/run_phase12_reproducibility_inputs.py
@/home/samuel/projects/pi_light_OR/tests/test_phase11_paired_evidence.py
@/home/samuel/projects/pi_light_OR/tests/test_phase12_reproducibility_inputs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run fast evidence contract tests</name>
  <read_first>
    - /home/samuel/projects/pi_light_OR/tests/test_phase11_paired_evidence.py
    - /home/samuel/projects/pi_light_OR/tests/test_phase12_reproducibility_inputs.py
    - /home/samuel/projects/pi_light_OR/scripts/run_phase11_paired_evidence.py
    - /home/samuel/projects/pi_light_OR/scripts/run_phase12_reproducibility_inputs.py
  </read_first>
  <files>tests/test_phase11_paired_evidence.py, tests/test_phase12_reproducibility_inputs.py, scripts/run_phase11_paired_evidence.py, scripts/run_phase12_reproducibility_inputs.py</files>
  <action>Run `python tests/test_phase11_paired_evidence.py` and `python tests/test_phase12_reproducibility_inputs.py` from the repository root. Do not edit source unless these tests reveal a real regression in the existing Phase 11/Gate C/Phase 12 contract.</action>
  <verify>
    <automated>python tests/test_phase11_paired_evidence.py && python tests/test_phase12_reproducibility_inputs.py</automated>
  </verify>
  <acceptance_criteria>
    - Both fast assertion suites pass.
    - Any failure is fixed conservatively without changing controllers, scenarios, seeds, demand multipliers, metrics, thresholds, or claim policy.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Inspect locked main spec and current resume state</name>
  <read_first>
    - /home/samuel/projects/pi_light_OR/scripts/run_phase11_paired_evidence.py
    - /home/samuel/projects/pi_light_OR/experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json
    - /home/samuel/projects/pi_light_OR/experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.progress.json
  </read_first>
  <files>experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json, experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.progress.json</files>
  <action>Import the Phase 11 spec builder and assert the main profile cardinality is exactly 6 binding regimes, 6 controllers, 20 seeds, 3 demand multipliers, and 2160 expected rows. Load the source and progress JSON files, report `status`, `actual_row_count`, `expected_row_count`, `completed_row_count`, `attempted_row_count`, `missing_row_key_count`, `execution_mode`, and spec-fingerprint compatibility. If progress is incompatible, stop before Plan 02 and leave an explicit summary.</action>
  <verify>
    <automated>python - &lt;&lt;'PY'
import json
from pathlib import Path
import sys
sys.path.insert(0, "scripts")
from run_phase11_paired_evidence import build_phase11_spec, progress_spec_fingerprint
spec = build_phase11_spec(profile="main")
assert len(spec) == 2160, len(spec)
assert len({r["scenario_tag"] for r in spec}) == 6
assert len({r["controller"] for r in spec}) == 6
assert len({r["seed"] for r in spec}) == 20
assert len({r["demand_multiplier"] for r in spec}) == 3
source = json.loads(Path("experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.json").read_text())
progress = json.loads(Path("experiments/dual_sensitivity/phase11_long_horizon_paired_seed_evidence.progress.json").read_text())
assert source.get("expected_row_count") == 2160
assert progress.get("expected_row_count") == 2160
progress_fp = progress.get("spec_fingerprint")
if progress_fp is not None:
    assert progress_fp == progress_spec_fingerprint(spec), "progress spec fingerprint mismatch"
print({
    "source_status": source.get("status"),
    "source_actual": source.get("actual_row_count"),
    "source_expected": source.get("expected_row_count"),
    "source_missing": source.get("missing_row_key_count"),
    "progress_completed": progress.get("completed_row_count"),
    "progress_attempted": progress.get("attempted_row_count"),
    "execution_mode": source.get("execution_mode"),
})
PY</automated>
  </verify>
  <acceptance_criteria>
    - Main profile cardinality is still 2160 with the locked Phase 13 families.
    - Source/progress artifacts parse and remain compatible with the current spec.
    - The summary quotes current coverage instead of inferring Gate C readiness.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run the two fast tests and the JSON/spec preflight command above. Plan 02 may proceed only when both are green and the progress file is compatible with the current main spec.
</verification>

<success_criteria>
Plan 01 is complete when the fast contract tests pass and current Phase 11 source/progress state is explicitly reported as safe to resume or explicitly fail-closed.
</success_criteria>

<output>
Create `/home/samuel/projects/pi_light_OR/.planning/phases/13-complete-predeclared-gate-c-evidence/13-01-SUMMARY.md` when done.
</output>
